---
title: Progressive Self-Learning for Domain Adaptation on Symbolic Regression of Integer Sequences
title_zh: 整数序列符号回归中领域自适应的渐进自学习方法
authors: "Yaohui Zhu, Kaiming Sun, Zhengdong Luo, Lingfeng Wang"
date: 2025-04-11
pdf: "https://ojs.aaai.org/index.php/AAAI/article/download/32162/34317"
tags: ["query:sr"]
score: 9.0
evidence: 整数序列的符号回归
tldr: 本文针对整数序列符号回归（SRIS）中的领域适应问题，提出渐进自学习（PSL）方法。由于随机生成数据与目标序列分布差异大，导致模型泛化不足。PSL通过融入初始模板生成源域数据，并逐步伪标记目标序列来捕捉目标分布，有效提升了SRIS在目标序列上的公式发现能力。
source: AAAI-2025-Accepted
selection_source: conference_retrieval
motivation: 神经机器翻译方法的符号回归在目标序列上泛化差，因为随机生成数据无法覆盖目标分布。
method: 提出渐进自学习（PSL），先结合初始模板生成源域数据，然后迭代伪标记目标序列以逼近真实分布。
result: 在整数序列符号回归任务上，PSL显著提升了目标域公式发现的准确率。
conclusion: 渐进自学习有效解决了符号回归中领域漂移问题。
---

## Abstract
Symbolic Regression of Integer Sequences (SRIS) aims to discover precise mathematical formulas from integer sequences. The neural machine translation-based method of SRIS trains the model using randomly generated data, and directly utilizes the trained model for inference on target sequences. However, the method often fails to effectively generalize to the target sequence, since the randomly generated data can not adequately cover the distributions of target data, i.e., there are distribution differences between them. In this work, we propose a progressive self-learning (PSL) method to explicitly capture sequence-formula distributions of the target domain. Specifically, a source domain dataset is generated by incorporating initial terms of the target domain to reduce the sequence distribution gap between the source domain and the target domain. Meanwhile, a self-learning loop strategy is adopted to improve the ability of the model to capture the sequence-formula distribution of the target domain. In this strategy, a neural machine translation model is used to learn the mappings from sequences to formulas in an end-to-end fashion. Then, this model is employed to explore candidate formulas of the target sequence using beam search. After verifying these candidate formula correctness, some of them are retained as training data for the next learning. Experimental results on OEIS datasets demonstrate that the proposed method surpasses current state-of-the-art methods in accuracy, and also discovers new formulas.

---

## 论文详细总结（自动生成）

# 整数序列符号回归中领域自适应的渐进自学习方法——详细总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **任务**：整数序列符号回归（Symbolic Regression of Integer Sequences, SRIS），即从给定整数序列中自动发现其精确的数学递推公式。
- **现有方法的不足**：当前主流的神经机器翻译方法（如 d'Ascoli et al., 2022）在随机生成的源域数据上训练模型，然后直接用于目标序列（真实OEIS序列）的推理。然而，随机生成的数据无法充分覆盖目标序列的分布，导致模型泛化能力差，即存在**领域漂移**问题。
- **研究动机**：如何同时利用生成的源域数据和目标域序列，使模型能够捕捉目标域的真实序列-公式分布，是该文要解决的 **DASRIS（Domain Adaptation on SRIS）** 问题。
- **整体含义**：该文首次将领域自适应的视角引入整数序列符号回归，提出渐进自学习（PSL）方法，显著提升了目标域公式发现的准确率，并发现了一些之前未被记录的公式。

## 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程

- **核心思想**：通过源域数据构造（融入目标域初始项）缩小分布差距，再通过自学习循环逐步用目标域数据训练模型，从而显式捕捉目标域的序列-公式分布。
- **关键技术细节**：
  - **源域数据构造（Source Data Construction with ITTD）**：
    - 使用二叉树随机生成递推公式（包括常/非线性递归），并利用**目标域初始项（Initial Term of Target Domain, ITTD）**生成序列，以减少源域与目标域序列分布的差异。
    - 参数：最大算子数 \(N_o = 10\)，最大递归度 \(D_r = 12\)，序列长度 \(l\) 取自 \([13,30]\)。
    - 额外生成线性递归公式（仅含 \(+,-,*\)，不含索引 \(n\)）以匹配目标域常见形式。
  - **自学习循环（Self-Learning Loop）**：
    1. **模型学习**：使用源域数据训练 Transformer 编码器-解码器作为公式生成器，损失为交叉熵。
    2. **目标数据生成**：用训练好的模型对目标域序列进行束搜索（beam width \(k = 32\)），生成候选公式，然后验证其正确性（将公式预测的序列与真实序列逐项比对）。
    3. **数据选择**：根据**少操作优先**或**低递归度优先**策略，每个序列最多保留3个正确公式作为新训练数据。
    4. **迭代**：最多50次迭代，每次迭代用目标域伪标记数据重训模型（100 epoch），并再次生成新公式。
- **公式与算法**：
  - 训练损失：\(L = -\frac{1}{n_s}\sum_{i=1}^{n_s}\sum_{j=1}^{c} y_{ij} \log \hat{y}_{ij}\)。
  - 迭代训练策略：第一轮用源域数据（10 epoch），后续轮用目标域伪标记数据（100 epoch）。
  - 评估指标：\(\text{acc}(n_p) = \frac{1}{N}\sum_{i=1}^{N} \mathbb{I}\left(\max_{n_i \leq j \leq \text{len}} |\hat{a}_{ij} - a_{ij}| = 0\right)\)，表示预测 \(n_p\) 项全部正确的准确率。

## 3. 实验设计：使用了哪些数据集/场景，它的 benchmark 是什么，对比了哪些方法

- **数据集**：
  - **源域**：随机生成的25M条普通递归公式-序列对 + 25M条线性递归公式-序列对。
  - **目标域**：来自OEIS（在线整数序列百科全书），选取标签为“easy”且项数≥25的序列，共超过60,000条。
  - **测试集**：两个子集：
    - **OEIS Easy25**：前10,000条项数≥25的序列。
    - **OEIS Easy35**：前10,000条项数≥35的序列。
- **对比方法**：
  - **FSF**（Mathematica的FindSequenceFunction，发现非递归公式）。
  - **FLR**（FindLinearRecurrence，发现线性递归公式）。
  - **DSR**（d'Ascoli et al., 2022的深度符号回归方法，即其基线）。
- **基准**：分别报告在 \(n_i = 15\)（Easy25）和 \(n_i = 25\)（Easy35）下，预测1项（np=1）和预测10项（np=10）的准确率。

## 4. 资源与算力

- **GPU**：4块 Tesla T4（16GB内存）。
- **训练时间**：每轮迭代约40分钟，共50次迭代，总训练时间约33小时（不含首次源域数据训练的10 epoch）。
- **首次训练**：在25M+25M对数据上训练10 epoch，未明确说明耗时，但由后续迭代推断总计算量较大。

## 5. 实验数量与充分性

- **主要对比实验**：在两组测试集上对比了FSF、FLR、DSR和本文PSL，涵盖不同预测长度（np=1和np=10），共8个结果。
- **消融与参数分析**（表2）：
  - 测试了5种变体（PG1~PG5），分别控制是否使用ITTD、递归度d、数据量（5M vs 7M）以及是否加入线性递归数据。
  - 每个变体均在OEIS Easy35上评估，充分验证了各组件贡献。
- **迭代分析**（图4）：展示了随迭代次数增加，发现公式总数和新公式数量的变化趋势（50轮）。
- **公式发现与案例展示**：表3列出9个发现公式（包括easy/more/hard序列），图5展示4个序列的预测曲线。
- **充分性评价**：实验设计较为全面，覆盖了不同数据集、不同预测长度、多种消融变量和迭代演化分析。但存在以下局限：仅在一个目标域（OEIS easy序列）上验证，未测试非easy序列或跨领域适用性；对比方法较少（未与其他符号回归自学习方法如Gauthier et al. 2023比较）。

## 6. 论文的主要结论与发现

- PSL方法在OEIS Easy25和Easy35上均显著优于FSF、FLR和DSR，np=1时准确率提升约20个百分点以上（PSL 53.1% vs DSR 33.4%），np=10时提升约8个百分点。
- 使用ITTD构造源域数据可带来5.5%的提升（PG1→PG2）。
- 增加递归度 \(d\) 和数据量（尤其是线性递归数据）有助于进一步提升性能。
- 自学习迭代有效：第一轮发现约9500个公式，最终累计超过12000个公式，且每轮仍有新公式被发现，表明模型能逐步从简单到复杂学习。
- 发现了多个之前OEIS中未记录的公式（如A134342、A213685、A157795），展示了方法的实际应用价值。

## 7. 优点

- **首次提出领域自适应框架**：将整数序列符号回归问题形式化为领域自适应问题，并设计针对性的渐进自学习方法。
- **创新性的数据构造策略**：利用目标域初始项（ITTD）生成源域数据，有效缩小分布差距，简单而有效。
- **自学习循环设计合理**：结合束搜索、公式验证和最少操作/低递归度选择策略，自动生成高质量伪标签，避免了手工标注成本。
- **实验验证充分**：在大型真实数据集（OEIS）上进行多组对比和消融，且展示了发现的真实新公式，具有实际意义。
- **结果显著**：在多个评价指标上超越已有方法，且能发现人类未记录的公式，展示了系统的强大能力。

## 8. 不足与局限

- **领域局限性**：仅针对OEIS中标记为“easy”的序列（即存在易于发现规律的序列），未评估在“hard”或“more”标签序列上的表现，应用范围有限。
- **对比实验不够全面**：仅对比了FSF、FLR和DSR三种方法，缺少与同期其他神经符号回归方法（如Gauthier et al. 2023的“Alien Coding”）或遗传编程方法的对比，难以全面反映其相对优势。
- **自学习迭代的收敛性分析不足**：虽展示了新公式数量递减趋势，但未讨论何时停止迭代或是否可能引入噪声伪标签造成误差累积。
- **模型复杂度**：训练使用了4块T4 GPU和大量数据（50M对+50轮迭代），计算开销较高，限制了在资源受限环境中的实用化。
- **评价指标单一**：主要用精度评价，未提供召回率或人工验证成本，且预测10项的准确率仍然偏低（约29.5%），说明长程预测仍有很大提升空间。
- **假设限制**：依赖目标序列至少有 \(d+10\) 项（d≤12），对短序列无法应用。

（完）
