---
title: "Support-Compiled Feature Folding: More Evidence at Lower Memory Across Tabular Foundation Models"
title_zh: 支持集编译的特征折叠：在表格基础模型中于更低内存下获得更多证据
authors: "Tian Zhou, Beverly Jin, Xue Wang, Linxiao Yang, Wenwei Wang, Bingqing Peng, Mengni Ye, Jinjie Gu, Liang Sun"
date: 2026-09-23
pdf: "https://arxiv.org/pdf/2609.28208v1"
tags: ["query:tabular-fm"]
score: 9.0
evidence: 面向表格基础模型的免训练推理
tldr: 表格基础模型面临特征侧扩展困境：全宽两两交互随列数二次增长，而特征选择虽省内存却丢弃证据。SCFF是一种免训练推理框架，通过支持度排序特征、原生编码器有界叶节点路由、残差证据校验及编码消息合并，把二次特征交互转为随宽度线性、局部工作集有界。在18个宽表数据集和六个骨干上，它一致提升准确率与NLL，降低GPU内存，并在内存上限下保留更多证据。
source: arxiv
selection_source: fresh_fetch
motivation: 全宽特征交互内存开销随列数二次增长，特征选择又丢弃证据，表格基础模型难以兼顾内存与证据保留。
method: SCFF免训练，将支持度排序特征路由至原生编码器有界叶节点，校验残差证据，合并编码消息后单次上下文预测，不改冻结骨干。
result: 在18个宽表与六个骨干上，宏准确率和NLL均提升，配对GPU内存中位节省2.09–2.36倍，最大峰值比达34.3倍。
conclusion: SCFF把二次特征交互转为线性宽度工作，在峰值内存约束下保留更多证据，为表格基础模型提供免训练、低内存的推理新路径。
---

## 摘要
表格基础模型面临特征侧的扩展困境：全宽成对混合随列数呈二次增长，而特征选择则通过丢弃证据来节省内存。我们提出支持集编译特征折叠（SCFF），一种无需训练的推理框架，可在不改变冻结主干的前提下化解该困境。SCFF 将按支持度排序的特征路由至原生特征编码器的有界叶节点，对残余证据进行支持集校验，并在单次上下文预测之前合并已编码的消息。由此，它将二次的特征交互计算转化为宽度线性的计算，且局部工作集有界，无需集成预测或训练新参数。在固定的 AMLB-29、TabZilla 与 TabArena 快照的穷尽式 18 数据集宽表切片上，SCFF 在全部六个受评估主干上均提升了数据集宏平均准确率与 NLL。全部四项等宽对比在锁定折上均保持有利的 95% 数据集自助法区间，相对误差降低最高达 26.1%。配对 GPU 内存节省的中位数为 2.09 倍至 2.36 倍，单独观测的最大峰值之比达到 34.3 倍。在实测峰值内存上限下，SCFF 利用节省出的预算保留更多经支持集筛选的证据，在预先声明的 TabICLv2 与 TabPFN-3 宽 Core 分层上，相较最宽的可行单叶节点分别提升准确率 4.06 和 3.72 个百分点。

## Abstract
Tabular foundation models face a feature-side scaling dilemma: full-width pairwise mixing grows quadratically with the number of columns, whereas feature selection saves memory by discarding evidence. We introduce Support-Compiled Feature Folding (SCFF), a training-free inference framework that resolves this dilemma without changing the frozen backbone. SCFF routes support-ranked features through bounded leaves of the native feature encoder, support-checks the residual evidence, and merges the encoded messages before a single contextual prediction. It thereby converts quadratic feature-interaction work into linear-in-width work with a bounded local working set, without ensembling predictions or training new parameters. On the exhaustive 18-dataset wide-table slice of fixed AMLB-29, TabZilla, and TabArena snapshots, SCFF improves dataset-macro accuracy and NLL on all six evaluated backbones. All four matched-width comparisons retain favorable 95 percent dataset-bootstrap intervals on locked folds, with relative error reductions up to 26.1 percent. Median paired GPU-memory savings are 2.09x to 2.36x, and the ratio of separately observed maximum peaks reaches 34.3x. Under a measured peak-memory ceiling, SCFF uses the saved budget to preserve more support-selected evidence, improving accuracy by 4.06 and 3.72 points over the widest feasible single leaf on predeclared wide-Core strata of TabICLv2 and TabPFN-3.

---

## 论文详细总结（自动生成）

# 论文总结：Support-Compiled Feature Folding（SCFF）

## 1. 核心问题与整体含义

- **研究动机**：表格基础模型把表格预测视为上下文学习（ICL）任务。其推理路径通常先由特征编码器把 \(D\) 列原始特征混合为固定维行表示，再由上下文编码器在 support/query 行之间推理。全宽成对特征混合的代价随列数呈 \(\Theta(TD^2)\) 增长，宽表会带来严重 GPU 内存瓶颈。
- **两难困境**：全宽特征交互保留所有证据，但内存二次增长；特征选择虽能省内存，却永久丢弃 cutoff 之外的列，损失潜在证据。
- **核心含义**：论文提出 SCFF，试图在不改变冻结骨干、不训练新参数、不集成多个预测的前提下，把“二次特征交互”转为“宽度线性、局部工作集有界”的推理流程，并利用节省的内存保留更多支持集筛选出的证据。
- **整体定位**：SCFF 是一种免训练推理框架，面向已有表格基础模型的“行表示接口”进行宽度编译；它改变特征编码器 \(F_\theta\) 的执行方式，但保持上下文编码器 \(C_\theta\) 和标签头 \(H_\theta\) 各执行一次。

## 2. 方法论：核心思想、关键技术与算法流程

- **接口假设**：论文假设模型可写成  
  \[
  f_\theta(X_s,y_s,X_q)=H_\theta(C_\theta(F_\theta([X_s;X_q],y_s);y_s))
  \]
  其中 \(F_\theta\) 是宽度相关特征编码器，\(C_\theta\) 是上下文编码器，\(H_\theta\) 是标签
