---
title: Tabular foundation models for non-tabular tasks
title_zh: 用于非表格任务的表格基础模型
authors: "Goran Nakerst, John Brennan, Wouter Beugeling, Masudul Haque"
date: 2026-08-23
pdf: "https://arxiv.org/pdf/2608.22594v1"
tags: ["query:tabular-fm"]
score: 9.0
evidence: 研究了表格基础模型（TabPFN v3）在非表格任务上的应用与评测
tldr: 表格基础模型（TFM）主要针对表格数据设计，但许多非表格任务也能以表格形式表示。本文使用TabPFN v3在MNIST手写数字识别、法德语言识别和Tiny ImageNet图像分类上进行测试，将数据每行作为样本，分类视为预测缺失标签，且不进行额外训练或微调。结果显示，尽管模型未显式利用空间或序列结构，在某些任务上仍能达到与专用模型相当的性能，揭示了TFM向非表格任务迁移的潜力。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2608.22594v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 886, \"height\": 583, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.22594v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 859, \"height\": 578, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.22594v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 861, \"height\": 571, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.22594v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 881, \"height\": 546, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2608.22594v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 868, \"height\": 483, \"label\": \"Table\"}]"
motivation: 尽管表格基础模型在表格数据上表现优异，但它们能否迁移到图像、文本等非表格任务尚不清楚。本文旨在检验这一泛化能力。
method: 将三类非表格数据（图像、文本）表示为表格，用TabPFN v3直接预测缺失标签，不进行训练或微调，仅改变上下文样本数量。
result: TabPFN v3在MNIST、语言识别和Tiny ImageNet上部分达到与专用模型相当的精度，表现与样本数相关。
conclusion: 表格基础模型展现出超越表格任务的能力，为处理非表格数据提供了新思路，但性能依赖上下文规模。
---

## 摘要
表格基础模型（TFMs）最近已成为表格数据机器学习的一种有前景的范式，能够在不进行任务特定训练的情况下跨数据集泛化。由于许多机器学习数据集可以表示为表格，这就提出了一个问题：TFM的能力是否超越了传统上被视为表格的任务？我们通过在三个非表格分类问题中使用TabPFN v3来解决这个问题：MNIST上的手写数字识别、法语和德语单词的语言识别，以及Tiny ImageNet上的图像分类。在每种情况下，原始数据表示为表格的行，分类被表述为缺失标签的预测。我们评估性能作为提供给预训练模型的上下文样本数量的函数，不进行额外训练或微调。尽管没有显式访问表征数据的空间或顺序结构，TabPFN v3在某些情况下达到了与专门针对相应任务的模型或方法相当的准确率。

## Abstract
Tabular foundation models (TFMs) have recently emerged as a promising paradigm for machine learning on tabular data, offering the ability to generalize across datasets without task-specific training. Since many machine learning datasets can be represented as tables, this raises the question: does TFM capability extend beyond tasks traditionally regarded as tabular? We address this question by using TabPFN v3 on three non-tabular classification problems: handwritten digit recognition on MNIST, language identification of French and German words, and image classification on Tiny ImageNet. In each case, the original data are represented as rows of a table and classification is formulated as prediction of a missing label. We evaluate performance as a function of the number of context samples provided to the pretrained model, with no additional training or fine-tuning. Despite having no explicit access to the spatial or sequential structure characterizing the data, TabPFN v3 in some cases achieves accuracies comparable with that of models or methods geared specifically toward the corresponding tasks.

---

## 论文详细总结（自动生成）

# 论文详细总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **研究背景**：表格基础模型（Tabular Foundation Models, TFMs）是近年来机器学习领域的新兴范式，代表模型包括 TabPFN v3、TabICL 等。这类模型通过在大规模合成表格数据上进行预训练，能够在不进行任务特定训练的情况下，通过上下文学习（in-context learning）泛化到未见过的表格数据集。
- **核心问题**：由于几乎所有机器学习数据集都可以被表示为表格形式（每行一个样本，每列一个特征），一个自然的问题随之产生：**TFM 的能力是否仅限于传统意义上的“表格任务”，还是能够泛化到图像、文本等非表格任务？**
- **研究意义**：本文试图重新审视“表格”与“非表格”任务之间的边界。如果 TFM 确实能够处理非表格任务，则意味着因果结构预训练带来的泛化能力远超预期，可能对基础模型的跨领域应用产生深远影响。

## 2. 论文提出的方法论：核心思想、关键技术细节

- **核心思想**：将非表格数据（图像、文本）以最朴素的方式“展平”为表格形式，然后直接使用预训练好的 TFM（TabPFN v3）进行上下文学习，预测缺失的标签列，**不进行任何梯度训练或微调**。
- **数据表示方式**：
  - **MNIST**：每张 28×28 图像展平为 784 维像素强度向量，作为一个表格行，标签作为最后一列。
  - **法德语言识别**：每个单词取前 15 个字母，进行序数编码（a→1, ..., z→26，不足位置填 0），得到一个 15 维特征向量。
  - **Tiny ImageNet**：每张 64×64×3 的 RGB 图像展平为 12,288 维原始像素向量。
- **学习方式**：将带标签的上下文（支持集）行和不带标签的查询（测试）行一起放入 Transformer 的上下文窗口，通过单次前向传播完成预测（即上下文学习），不更新模型权重。
- **关键技术细节**（TabPFN v3 配置）：
  - 采用行列交替注意力机制，保持行/列置换不变性。
  - 对超出特征上限的数据，采用**集成（ensembling）**方式：每个估计器随机采样最多 200 个特征列，自动扩展估计器数量以尽量覆盖全部特征（上限 32 个）。
  - 设置 `ignore_pretraining_limits=True` 以绕过模型的输入规模限制（默认上限：500 特征 / 10,000 行）。
  - Tiny ImageNet 实验中额外增加了 PCA 降维变体（降至 200 维主成分），使所有集成估计器能够覆盖全部特征。

## 3. 实验设计：数据集、基准与对比方法

| 任务 | 数据集 | 类别数 | 测试集规模 | 特征维度 | 对比方法 |
|------|--------|--------|------------|----------|----------|
| 图像分类 | MNIST | 10 | 5,000（每类 500） | 784 | CNN（2层卷积 + 2层全连接）、LightGBM |
| 语言识别 | wordfreq 法语/德语词表 | 2 | 4,000（每类 2,000） | 15 | LightGBM（数值编码和类别编码两种变体） |
| 图像分类 | Tiny ImageNet 100 类子集 | 100 | 5,000（每类 50） | 12,288（及 PCA-200 变体） | 仅 TabPFN v3（原始像素 vs PCA 降维） |

- **实验基准设定**：对基础模型使用“上下文/支持集”进行上下文学习；对非基础模型（CNN、LightGBM）使用相同数量的样本进行梯度训练。二者使用的数据划分（同一组训练/上下文样本和测试集）保持一致，以进行公平比较。
- **上下文规模扫描**：每个任务都按数量级扫描上下文/训练样本数（MNIST：每类 1 到 2,000；语言任务：每语言 50 到 10,000；Tiny ImageNet：每类 1 到 100）。
- **重复试验**：每个数据点通过多次重新抽样训练/上下文集合取均值，误差条表示 ±1 标准差。

## 4. 资源与算力

- **论文未明确提及**：文中没有说明使用的 GPU 型号、数量、训练时长或具体硬件配置。
- 可间接推断的信息：
  - TabPFN v3 本身是预训练模型，本文所有实验仅涉及推理（前向传播），无需训练。
  - 对比方法中，CNN 在每个训练集大小上固定训练 100 个 epoch（Adam 优化器，batch size 128），但未报告实际运行时间。
  - LightGBM 使用 40 次随机搜索交叉验证进行超参数调优。
- **结论**：由于缺少明确的算力描述，无法评估实验的计算成本，这是论文在透明度方面的一个不足。

## 5. 实验数量与充分性

- **实验覆盖范围**：
  - 覆盖了 3 个差异显著的任务领域（图像识别、文本分类、自然图像分类），任务难度跨度大（二分类到 100 分类）。
  - 每个任务都扫描了 5-7 个不同的训练/上下文规模点，并进行了多次重复抽样（2-10 次）。
  - 包含了对 TabPFN v3 的消融式变体（Tiny ImageNet 上原始像素 vs PCA 降维）。
- **公平性与客观性**：
  - 优点：所有模型使用完全相同的训练/上下文样本和测试集，数据划分保持一致；对比模型进行了较为充分的超参数调优（LightGBM 在每个规模独立调优）。
  - 不足：
    - 仅使用了 **一种 TFM**（TabPFN v3），未与其他 TFM（如 TabICL、TabFM）对比。
    - 语言任务未与专门的自然语言处理模型（如基于 RNN/Transformer 的文本分类器）对比。
    - Tiny ImageNet 任务未与任何非基础模型（如 CNN、ResNet）进行基准对比，无法全面评估性能差距。
    - 每个任务的重复抽样次数偏少（尤其是大规模训练样本时仅重复 2-3 次），统计稳健性有限。

## 6. 论文的主要结论与发现

- **TabPFN v3 确实具备跨任务泛化能力**：尽管从未见过图像或文本数据，也未被显式赋予空间或序列结构先验，它仍能在三个非表格任务上达到可观甚至接近专用模型的准确率。
- **具体性能表现**：
  - **MNIST**：从每类 1 个样本的 22.5% 提升到每类 2,000 个样本的 **98.6%**，与训练的 CNN 差距较小，且优于 LightGBM。考虑到 TabPFN 无需任何训练，这一结果非常显著。
  - **法德语言识别**：从每语言 50 个词的 62.4% 提升到 10,000 个词的 **88.2%**。在少样本区间（≤100）优于调优的 LightGBM，但在数据充足后被 LightGBM 反超。
  - **Tiny ImageNet**：仅达到 **11.2%**（每类 100 样本），远超随机水平（1%）但远低于实用水平。PCA 降维仅在训练样本充足时带来约 2 个百分点的提升。
- **性能随上下文规模单调上升且未饱和**：即使在最大的上下文规模下（20,000 行），性能仍在提升，说明 TFM 还有进一步发挥潜力的空间。
- **因果结构预训练是关键**：作者推测 TabPFN 在合成因果图（DAG）上的预训练赋予其抽象理解变量间因果关系的能力，这是其能够跨领域泛化的根本原因——图像分类本质上可以理解为“像素 → 标签”的因果映射。

## 7. 优点

- **问题选择新颖且重要**：首次系统性地检验 TFM 在非表格任务上的能力，重新审视了“表格”与“非表格”的边界，对基础模型研究具有启发意义。
- **实验设计简洁而公平**：将复杂任务归一化为“表格缺失值填充”的统一框架，对比基线（CNN/ LightGBM）与 TFM 使用完全相同的训练样本和测试集，确保比较的客观性。
- **结果揭示意外性**：MNIST 上达到 98.6% 的准确率尤其令人印象深刻，因为 TabPFN 完全看不到图像的二维结构，也没有进行任何任务特定训练。
- **讨论深入**：对 Tiny ImageNet 性能不佳的原因（集成特征覆盖不足、缺乏空间先验、维度灾难）进行了细致的技术分析。
- **因果视角的提出**：将 TFM 的泛化能力与因果结构预训练联系起来，为理解基础模型的泛化机制提供了新的理论视角。

## 8. 不足与局限

- **模型覆盖单一**：仅测试了 TabPFN v3 一种表格基础模型，无法判断结论是否适用于其他 TFM（如 TabICL、TabFM），结论的外部效度有限。
- **小规模任务限制**：MNIST 和 100 类 Tiny ImageNet 子集相对简单，在更复杂的自然图像数据集（如完整 ImageNet、CIFAR-100 等）上的表现未知。
- **Tiny ImageNet 实验缺乏对标**：未与任何针对性训练的模型（如 CNN、ViT）进行比较，无法量化 TabPFN 与行业基准的真实差距。
- **算力/资源披露不足**：未报告实验的硬件配置和运行时间，降低了可复现性和工程参考价值。
- **统计功效有限**：重复抽样次数较少，特别是在大规模上下文区间，误差估计可能不够可靠。
- **应用限制未充分讨论**：TabPFN 的上下文窗口限制意味着它无法处理大规模现实数据集（百万行级别），这一实际部署瓶颈在文中提及但未深入探讨解决方案。
- **领域范围有限**：仅涉及视觉和文本领域，未测试音频、时间序列、图数据等其他非表格模态。

（完）
