---
title: "InsightSR: Refining Symbolic Regression Search Spaces via Parallel Semantic and Structural LLM Guidance"
title_zh: InsightSR：通过并行语义与结构LLM指导优化符号回归搜索空间
authors: "Yating Ling, Wenjing Cun, Zhitang Chen"
date: 2026-08-26
pdf: "https://arxiv.org/pdf/2608.25291v1"
tags: ["query:sr"]
score: 10.0
evidence: 直接研究符号回归，用LLM引导搜索空间优化
tldr: "符号回归旨在从数据中发现简洁数学定律，但传统方法受限于庞大组合搜索空间，难以高效挖掘物理有效表达式。InsightSR将大语言模型作为指导层嵌入PySR遗传编程引擎，通过语义种子路径生成量纲一致的功能骨架，结构特征路径推荐非线性特征变换，以此逐步重构搜索空间。特征变换累积使搜索从原始变量上的深表达式树，转为在丰富语义特征集上组装浅树，并借助反馈循环实现自校正。实验在Feynman基准达到95%精确恢复率，在LLM-SRBench LSR-Transform任务达到80.18%准确率，显著优于现有遗传规划与神经符号方法，同时保持真实数据上的强分布外泛化。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2608.25291v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1641, \"height\": 933, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.25291v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 750, \"height\": 374, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.25291v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 697, \"height\": 528, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.25291v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 739, \"height\": 406, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.25291v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 748, \"height\": 539, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2608.25291v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 877, \"height\": 640, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.25291v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1423, \"height\": 237, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.25291v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 571, \"height\": 452, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.25291v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 359, \"height\": 294, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.25291v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1504, \"height\": 452, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.25291v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1509, \"height\": 347, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.25291v1/table-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 1327, \"height\": 532, \"label\": \"Table\"}]"
motivation: 传统符号回归搜索空间巨大且缺乏物理导向，导致难以发现简洁可解释的数学定律。
method: InsightSR以LLM为向导，双路径迭代变换搜索空间，累积特征集并用反馈循环自校正，引导PySR进行高效符号搜索。
result: "Feynman基准精确恢复率95%，LLM-SRBench LSR-Transform准确率80.18%，均显著超越现有基线，并保持良好分布外泛化。"
conclusion: 证明LLM引导搜索空间重构可提升符号回归的精度与泛化，为神经符号发现提供了可扩展的新范式。
---

## 摘要
符号回归（SR）旨在从观测数据中发现简洁的数学规律，然而传统方法往往难以应对物理上有意义的表达式的巨大组合搜索空间。我们提出InsightSR，一个将大型语言模型（LLMs）嵌入为PySR遗传编程引擎周围引导层的框架。InsightSR不是依赖LLMs直接生成表达式，而是通过两条互补路径逐步转换搜索空间本身：提出量纲一致函数骨架的语义种子路径，和推荐非线性特征变换的结构特征路径。这些转换在迭代中累积，拓宽输入空间，并将符号搜索从在原始变量上构建深层表达式树转变为在丰富、语义信息特征集上组装浅层树。一个生成后反馈循环评估候选表达式，根据经验效用对特征进行分类，并为下一次迭代优化引导，将发现过程从开放式生成转变为迭代、自我纠正的优化。在三个基准测试中，InsightSR在Feynman基准上实现了95%的精确恢复率，在LLM-SRBench LSR-Transform任务上实现了80.18%的准确率，大幅优于最先进的遗传编程和神经符号方法，同时在实际数据集上保持了强大的分布外泛化能力。

## Abstract
Symbolic regression (SR) seeks to discover parsimonious mathematical laws from observational data, yet conventional approaches often struggle with the vast combinatorial search space of physically meaningful expressions. We present InsightSR, a framework that embeds Large Language Models (LLMs) as a guiding layer around the PySR genetic programming engine. Rather than relying on LLMs to generate expressions directly, InsightSR uses LLMs to progressively transform the search space itself through two complementary pathways: a Semantic Seed Pathway that proposes dimensionally consistent functional skeletons, and a Structural Feature Pathway that recommends nonlinear feature transformations. These transformations accumulate over iterations, broadening the input space and shifting the symbolic search from constructing deep expression trees over raw variables to assembling shallow trees over a rich, semantically informed feature set. A post-generation feedback loop evaluates candidates, categorizes features by their empirical utility, and refines the guidance for the next iteration, transforming the discovery process from open-ended generation into iterative, self-correcting refinement. Across three benchmarks, InsightSR achieves a 95% exact recovery rate on the Feynman benchmark and 80.18% accuracy on the LLM-SRBench LSR-Transform task, substantially outperforming state-of-the-art genetic programming and neural-symbolic methods while maintaining strong out-of-distribution generalization on real-world datasets.

---

## 论文详细总结（自动生成）

## 一、核心问题与整体含义

- **研究动机**：符号回归（Symbolic Regression, SR）旨在从观测数据中自动发现简洁、可解释的数学定律，是科学发现的关键工具。但该问题本质上是 NP-hard，候选表达式的组合搜索空间随复杂度呈指数增长，且纯数据驱动的方法难以融入物理约束（如量纲一致、守恒律），往往得到数值拟合好但物理上无意义的表达式。
- **现有方法局限**：
  - 传统遗传编程（GP）类方法（如 PySR）样本效率低，容易过拟合噪声，且缺乏物理导向。
  - 深度强化学习与 Transformer 类神经符号方法（如 DSR、NeSymReS、E2E）依赖训练分布，泛化能力弱，物理约束多为硬编码。
  - 已有 LLM+SR 方法（如 LLM-SR、SR-LLM、LaSR、PiSR）存在推理开销大、仅沿单一维度（结构假设或特征变换）进行引导，没有利用二者协同潜力。
- **核心贡献**：InsightSR 将 LLM 作为“引导层”嵌入 PySR 遗传编程引擎，通过并行语义与结构双路径逐步重构搜索空间，并利用反馈循环实现自校正的迭代优化。

## 二、方法论

### 核心思想
- 不让 LLM 直接生成最终表达式，而是让 LLM 渐进地转换搜索空间本身：  
  - **语义种子路径（Semantic Seed Pathway）**：提出量纲一致的功能骨架，为进化搜索提供物理预热。  
  - **结构特征路径（Structural Feature Pathway）**：推荐非线性特征变换，并跨代累积，拓宽输入空间。  
- 最终效果：将符号搜索从“在原始变量上构建深表达式树”转变为“在语义丰富的特征集上组装浅层树”，显著降低搜索复杂度。

### 关键技术细节
1. **上下文初始化与单位合成**  
   - 从数据集元数据与问题描述中提取变量量纲；不明确时由 LLM 基于语义推断，形成持续约束。
2. **并行引导策略**（核心双路径）  
   - **语义种子路径**：LLM 生成候选表达式拓扑，审计其量纲一致性后作为种群种子，同时提供每个算子的复杂度偏好。  
   - **结构特征路径**：LLM 根据历史性能推荐非线性变换（幂律项、三角映射等），并追加到输入矩阵：  
     \(X_{aug} = [x_1, \dots, x_d, \phi_1(x), \dots, \phi_k(x)]\)。  
   - 两路径在 PySR 中汇合，搜索优化目标变为：  
     \(f^* = \arg\min_{f \in \mathcal{F}_{seed}} \mathcal{L}(f(X_{aug}), D)\)。
3. **搜索过程与复杂度偏置**  
   - 搜索目标：\(\min_f \ \text{MSE}(y, f(X)) + \lambda \cdot C(f)\)，其中 \(C(f)\) 为表达式树节点数。  
   - 引入 LLM 提供的算子偏置调整复杂度：\(C'(f) = \sum_{o \in f} (c_o + \omega_o)\)，推荐算子获得负偏置（复杂度折扣），物理上不可信的算子被正偏置抑制。  
   - 采用“热启动”：保留上一代最优个体作为精英成员。
4. **多维度评估与反馈循环**  
   - LLM 在每一代结束后评估 Pareto 最优候选，综合数值精度、物理可解释性、变量覆盖完整性；同时对特征空间进行诊断，区分有效/无效变换。  
   - 评估结果存入动态知识库，用于下一轮：修正种子、调整算子偏置、优先推荐特征变换。

### 算法流程
```
for g = 1 to G do
    F_seed ← LLM.SemanticSeeds(C, U, K)        // 语义种子
    X_aug ← LLM.StructuralFeatures(C, K)       // 结构特征
    G ← MergeGuidance(F_seed, X_aug)
    candidates ← PySR.GuidedSearch(X_aug, y, G)
    eval, insights ← LLM.StrategicAnalysis(candidates)
    K ← KB.Accumulate(insights, eval)
    if Loss(f*) < 1e-10 then break
return best expression
```

## 三、实验设计

### 基准数据集
| 基准 | 规模 | 说明 |
|---|---|---|
| Feynman 100 | 100 个问题 | 来自《费曼物理学讲义》的物理方程，含经典力学、电磁学、量子力学、热力学，有真实表达式，可测精确恢复率。 |
| LLM-SRBench | 240 个问题 | 五个类别：LSR-Transform、Chemistry、Biology、Physics、Material Science，测试跨域泛化。 |
| Real-World | 4 个数据集 | Oscillator 1/2、E. coli、Stress-Strain，各有 ID（分布内）和 OOD（分布外）测试划分。 |

### 对比方法
- **GP 类**：NGGP、GP-GOMEA、gplearn、PySR、QLattice  
- **神经符号类**：PSRN、E2E、PhySO、DSR、uDSR、NeSymReS  
- **LLM 类**：Direct Prompting、SGA、LaSR、LLM-SR

### 评估指标
- **R²** 与 **NMSE**（= 1 − R²）  
- **容差精度 acc_τ**：最大相对误差在阈值 τ 内才算成功（τ = 0.1 常用）  
- **精确恢复率**：符号形式与真实表达式数学等价（τ → 0）

## 四、资源与算力

- **论文未明确说明使用的 GPU 型号、数量或训练/推理时长**。  
- 仅提供部分超参数：LLM 为 Qwen-3.5 27B，temperature=0.3；PySR 迭代 100–500；LLM 引导代数为 3–30；种群规模 50，种群数 30；早期停止阈值为 \(10^{-10}\)。  
- 从方法描述看，LLM 仅在代际之间智能查询（而非每一步），因此推理开销相对可控，但没有给出具体测量数据。

## 五、实验数量与充分性

- **实验数量**：覆盖 3 个大型基准，总计 344+ 个问题；在 Feynman 上报告了精确恢复率、R²、Pareto 前沿、噪声鲁棒性（4 个噪声水平）；在 LLM-SRBench 上跨 5 个领域报告 acc_τ 与 NMSE；在 Real-World 上报告 ID/OOD NMSE。数据采集较为丰富。
- **充分性与公平性**：
  - 优点：对比了多种类型基线（传统 GP、神经符号、LLM 方法），覆盖主流失线；评估指标多样；OOD 测试能验证泛化能力。
  - 不足：**缺少消融实验**，无法量化语义种子通路与结构特征通路的单独贡献；未分析 LLM 模型大小或提示模板的影响；未报告多次运行的标准差，无法评估稳定性；对 OOD 数据仅 4 个任务，规模较小。

## 六、主要结论与发现

- **Feynman 基准**：InsightSR 达到 **95% 精确恢复率**，平均 R²=0.9999，大幅超越现有 SOTA；噪声鲁棒性良好（噪声 0.001 时 Acc_0.1 = 79%，无噪声 97%）。
- **LLM-SRBench**：LSR-Transform 类别 Acc_0.1 = **80.18%**（此前最佳 LaSR 为 50.45%）；其他类别（Chemistry 66.67%、Biology 50.00%、Physics 40.91%、Material Sci. 92.00%）也均优于或显著优于基线；NMSE 最多低数个数量级。
- **Real-World**：在 Oscillator 1/2 上 ID NMSE 分别达 \(9.55\times10^{-11}\) 和 \(2.45\times10^{-9}\)，比 LLM-SR 提升多个数量级；在 E. coli 与 Stress-Strain 上 OOD 性能亦最好或相当。
- **核心机制验证**：语义种子物理约束 + 结构特征累积 + 复杂度偏置 + 反馈循环，能有效将搜索导向物理合理且数值精确的表达式，同时保持分布外泛化。

## 七、优点

- **方法设计有创新**：不是让 LLM 直接生成表达式，而是通过双路径重构搜索空间，降低搜索深度，思想新颖。
- **物理与数据双驱动**：语义种子融入量纲一致性先验，结构特征自下而上补充表达力，二者协同优于单一维度引导。
- **闭环自校正**：反馈机制让 LLM 从历史中学习，逐步收敛，不是一次性启发。
- **实现简洁实用**：基于成熟的 PySR 引擎，无需大量定制；LLM 查询频率低（代际间），计算开销相对可控。
- **实验结果突出**：在多个基准上全面超越现有方法，尤其精确恢复率提升显著；OOD 结果表明稳定性较好。

## 八、不足与局限

- **对初始元数据敏感**：单位合成依赖问题描述，变量语义模糊时可能出错，影响后续约束。
- **缺少消融实验**：未单独验证语义种子通路和结构特征通路的贡献，也没有分析复杂度偏置与热启动的作用，结论的因果支撑不完整。
- **LLM 引入偏差风险**：LLM 的“物理先验”可能带有先入为主的偏见，可能错过反直觉但正确的规律；不过论文未讨论这点。
- **基准覆盖有限**：Feynman 和 LLM-SRBench 多集中于已知形式的物理/科学方程，对未知或复杂动力学系统的适用性仍待验证；OOD 任务仅 4 个，代表性不足。
- **计算资源报告缺失**：文中未给出具体 GPU 型号、数量、运行时间，难以复现成本或比较效率。
- **泛化证明不足**：虽然 OOD 数据表现好，但未说明是否对所有 OOD 场景都鲁棒，也未讨论噪声更高时的表现（只测到噪声 0.001 级别）。

（完）
