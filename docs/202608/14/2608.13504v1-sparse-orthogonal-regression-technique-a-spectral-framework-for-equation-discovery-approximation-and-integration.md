---
title: "Sparse Orthogonal Regression Technique: A Spectral Framework for Equation Discovery, Approximation, and Integration"
title_zh: 稀疏正交回归技术：用于方程发现、近似与积分的谱框架
authors: "Sabin Roman, Ljupco Todorovski, Saso Dzeroski"
date: 2026-08-13
pdf: "https://arxiv.org/pdf/2608.13504v1"
tags: ["query:sr"]
score: 9.0
evidence: 方程发现与符号回归框架
tldr: 针对含噪且不规则采样数据中的方程发现与逼近问题，提出稀疏正交回归技术（SORT），通过L1正则回归直接估计正交基展开系数，避免显式求积。在动力系统实验中，SORT在基适配时优于库稀疏回归基线，且对稀疏采样、导数噪声和表示失配更稳健，并支持阶一致增长。该方法为系统辨识、逼近与积分提供可重用中间表示，使基设计成为建模显式环节。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2608.13504v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 583, \"height\": 399, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.13504v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 579, \"height\": 407, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.13504v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1226, \"height\": 693, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.13504v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1263, \"height\": 733, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.13504v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1271, \"height\": 1422, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2608.13504v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1015, \"height\": 246, \"label\": \"Table\"}]"
motivation: 现有方法依赖有限库的符号或项选择，库外非线性易致建模失败，需更稳健的谱表示途径。
method: SORT利用选定的正交基，通过L1正则回归从观测直接学习稀疏展开系数，取代显式积分。
result: 实验表明SORT在基适配时精度优于SINDy类基线，且对采样稀疏、噪声及表示失配表现更稳定。
conclusion: SORT将方程发现转为基设计与系数学习，支撑近似与积分，为系统建模提供可重用谱框架。
---

## 摘要
我们开发了稀疏正交回归技术（SORT），这是一种稀疏谱框架，用于从含噪声且不规则采样的数据中学习正交基展开。SORT使用L1正则化回归直接从观测中估计展开系数，避免了显式求积或解析内积评估。其核心应用是常微分方程的数据驱动发现：向量场在选定的正交基中表示，并作为稀疏系数展开进行学习。这为符号回归、基于语法的发现和SINDy风格的稀疏识别提供了一条补充途径，先恢复紧凑的谱表示，随后可指导搜索更简单的解析形式。在动力系统实验中，当基适应问题时，SORT匹配或优于基于库的稀疏回归基线，并且在稀疏采样、噪声导数估计和表示不匹配下表现出更稳定的退化。具体示例说明了这种表示为何有用：如果有限库遗漏了问题特定的非线性，所得模型可能失败。SORT并非对不匹配免疫，但它将问题从对通用项的脆弱选择转移到适应问题领域的基设计上。实验还表明，随着模型阶数增加，主导的低阶系数持续存在，支持阶一致性的模型增长。除了方程发现，相同的学习展开还通过系数读出支持非线性近似和复杂高维积分的估计。总体而言，SORT为系统识别、近似和积分提供了可重用的中间表示，同时使基设计成为科学建模问题的明确部分。

## Abstract
We develop the Sparse Orthogonal Regression Technique (SORT), a sparse spectral framework for learning orthonormal-basis expansions from noisy and irregularly sampled data. SORT estimates expansion coefficients directly from observations using L1-regularized regression, avoiding explicit quadrature or analytic inner-product evaluation. The central application is data-driven discovery of ordinary differential equations: vector fields are represented in chosen orthogonal bases and learned as sparse coefficient expansions. This provides a complementary route to symbolic regression, grammar-based discovery, and SINDy-style sparse identification by first recovering a compact spectral representation, which can later guide searches for simpler analytic forms. Across the dynamical-system experiments, SORT matches or improves upon library-based sparse-regression baselines when the basis is well adapted to the problem, and shows more stable degradation under sparse sampling, noisy derivative estimates, and representation mismatch. Specific examples illustrate why this representation is useful: if a finite library misses the problem-specific nonlinearity, the resulting model can fail. SORT is not immune to mismatch, but it shifts the problem away from brittle selection among generic terms to basis design adapted to the problem domain. The experiments also show that dominant low-order coefficients persist as model order increases, supporting order-consistent model growth. Beyond equation discovery, the same learned expansion supports nonlinear approximation and estimation of complex, high-dimensional integrals by coefficient readout. Overall, SORT provides a reusable intermediate representation for system identification, approximation, and integration, while making basis design an explicit part of the scientific modeling problem.

---

## 论文详细总结（自动生成）

## 论文详细中文总结

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：如何从含噪声、不规则采样的有限数据中，以稳定、可重用的形式重构未知函数，并用于下游任务（如方程发现、数值积分、函数近似）。
- **研究动机**：
  - 现有数据驱动方程发现方法（如 SINDy）依赖有限候选函数库，当真实动力学在所选字典中不稀疏时，恢复会变得病态或脆弱。若库中遗漏问题特定的非线性项（如 $J_1(x)$ ），模型可能完全失败。
  - 符号回归和基于语法的发现方法搜索空间巨大，且在有噪数据下稳定性不足。
  - 函数近似、数值积分和方程发现在传统上被视为独立任务，但共享"从有限含噪数据重建函数"这一核心需求，缺乏统一的中间表示。
- **整体含义**：论文提出 SORT，将稀疏谱表示作为样本数据与后续符号/解析描述之间的可复用中间层，将"基设计"显式纳入科学建模问题，而非视为实现细节。

### 2. 论文提出的方法论

- **核心思想**：在预设的正交归一基下，通过 L1 正则化回归直接从观测样本估计展开系数，避免显式求积或解析内积计算。学习到的稀疏正交展开作为一个可复用对象，同时支撑系统辨识、数值积分和函数近似。

- **关键技术细节与公式**：
  - **问题设定**：$(Ω, μ)$ 为域与测度，$\{φ_k\}_{k≥0}$ 为正交归一的基，函数的谱系数为 $c_k = ⟨f, φ_k⟩$。
  - **稀疏回归估计**：给定样本 $y_i = f(x_i) + ε_i$，构造设计矩阵 $Φ_{ik} = φ_k(x_i)$，通过 Lasso 估计系数：
    $$\hat{c} ∈ \arg\min_{u∈R^K} \frac{1}{2N}\|y - Φu\|_2^2 + λ\|u\|_1$$
  - **理想情况下的误差界**：若经验正交性成立（$\frac{1}{N}Φ^⊤Φ = I_K$）且噪声项满足 $\|\frac{1}{N}Φ^⊤ε\|_∞ ≤ λ$，则 $\|\hat{c} - c\|_∞ ≤ 2λ$。但论文强调此界仅作背景直觉，实际系数稳定性需数值评估。
  - **数值积分**：利用积分是线性泛函的性质。积分 $I(f)=⟨f, r_I⟩$ 的 Riesz 表示元 $r_I$ 归一化后作为基元素 $φ_I$，则 $I(f)=\|r_I\| c_I$，通过对 $c_I$ 的系数读出即可获得积分值。对 $[a,b]^d$ 上的积分，引入变换权重 $D(t)=∏_{j=1}^d(1+t_j^2)$，令 $g(t)=f(x(t))/D(t)$，以 $Φ_0 = D/\|D\|$ 为积分泛函的表示元，则积分简化为单个系数读出。
  - **方程发现**：对自治系统 $\dot{x}=f(x)$，将每个向量场分量表示为 $f_j(x)=∑_{k∈Λ_K} c_{jk}φ_k(x)$，从 $(x(t), \dot{x}(t))$ 样本中稀疏估计系数，得到稀疏谱向量场模型。
  - **基的选择**：Fourier 基用于周期/振荡结构；Legendre/Chebyshev 基用于有界区间；Hermite/Laguerre 用于无界/半无限域；高维场景使用双曲交叉截断。
  - **算法流程**：(1) 根据问题领域选择正交基；(2) 构造设计矩阵；(3) 通过 Lasso/坐标下降求解稀疏系数；(4) 将系数向量用于预测、积分或作为符号搜索的指导。

### 3. 实验设计

- **场景一：采样退化下的方程发现**（Dynobench 数据集）
  - 系统：Lotka–Volterra、Van der Pol、Stuart–Landau（多项式/Legendre 基）、Bar Magnets（Fourier 基）。
  - 设定：改变采样间隔，导数用有限差分估计（无平滑）；训练与评估使用相同状态与导数估计。
  - 对比方法：SINDy + 有限差分（多项式库或 Fourier 特征）。
- **场景二：噪声与表示不匹配下的循环系统**（3D 系统）
  - 系统：Thomas 吸引子（三角耦合 $\sin(x)$）和 Bessel 驱动变体（$J_1(x)$ 耦合，$\dot{x}_1 = J_1(x_2) - bx_1$ 等）。
  - 设定：加性高斯噪声 → 子采样 → Savitzky–Golay 滤波平滑并估计导数。SINDy 使用多项式库或多项式+三角增强库；SORT 使用 Legendre 或三角张量基。
  - 指标：短时 rollout RMSE（中位数 + IQR），随训练比例（0.1–0.8）和噪声水平（$σ=0–0.12$）变化。
- **场景三：积分估计**
  - 1D：Gaussian 积分 $\int_0^L e^{-αx^2}dx$ 和 Fresnel 型振荡积分 $\int_0^L \cos(αx^2)dx$（$L=8$，$α$ 变化）；与闭式解或参考解对比。
  - 高维：可分离的 Fresnel 和 Gaussian 被积函数在 $[0,L]^d$ 上（$d=1$–$6$，$α=2,L=3$），保证存在精确参考值。
- **场景四：谱恢复与阶一致近似**
  - 例 1：$f(x)=x$ 在 $[−π,π]$ 上，Fourier 正弦系数已知 $|b_k|=2/k$，20% 训练 / 80% 测试，噪声 $σ=0.15$。对比 OLS 与 SORT 的系数恢复和重建。
  - 例 2：$f(x_1,x_2)=\exp(x_1\cos(2x_2))$ 在 $[−1,5]^2$ 上，20% 含噪训练数据（$σ=0.1$），80% 干净测试；参数数 $k∈\{60,80,100\}$；对比 SORT 与 OLS-Legendre、OLS-单项式、RBF、RFF、梯度提升（GB）；报告 test RMSE。

### 4. 资源与算力

- 论文**未明确提及**任何 GPU 型号、数量或训练时长。
- 从实验规模推断：涉及的实验均为低维（$d≤6$）或中低规模问题，设计矩阵构建成本为 $O(NK)$，Lasso 使用坐标下降求解。涉及的参数量级在数千至数万（如 RBF 特征数可到 2 万左右），总体计算开销应属于 CPU 可承担的范围。
- 论文提供了代码可用性链接（Zenodo DOI），但未披露具体硬件配置。

### 5. 实验数量与充分性

- **实验数量**：
  - 4 个动力系统（采样退化实验）；
  - 2 个 3D 循环系统 × 2 个变量维度（训练比例、噪声水平）× 2 种表示（SINDy/SORT 及基变体）；
  - 2 类积分问题（1D + 高维各两类被积函数）；
  - 2 个近似/谱恢复示例（其中第二个有 4 个对比方法 × 3 种参数规模）。
- **充分性评价**：
  - **优点**：实验覆盖了论文声称的三个应用场景（方程发现、积分、近似），并且在噪声、采样率、训练比例、基失配等多个维度上进行了系统扫描，比较维度较全面。
  - **不足**：缺少更高维（如 $d>6$）的积分实验；方程发现仅涉及低维系统（2D 和 3D），未涉及高维或混沌系统的完整评估（论文对 Lorenz 系统做了说明性排除）；未对真实世界噪声数据（如实际测量实验）进行验证；对 SORT 超参数 $λ$ 和基截断阶的选择灵敏度未做消融分析；未说明是否有消融实验（如不同正则化强度、不同基类型对 SORT 自身性能的影响）。

### 6. 论文的主要结论与发现

- SORT 在基与问题匹配时，与 SINDy 类库稀疏回归基线表现相当或更好；在稀疏采样和有限差分导数退化的场景下，SORT 的退化更缓慢、更少灾难性（而 SINDy 在某些系统出现突然的 rollout 失败）。
- SORT 对噪声和表示不匹配的鲁棒性更强：在 Bessel 驱动的循环系统中，SINDy 无论使用多项式库还是增强三角库都随训练数据减少或噪声增加而显著退化；SORT 保持稳定，因为它不需要提前在有限字典中提供正确的解析项，正交性减少了特征共线性，稀疏性抑制了不稳定模式。
- 基设计是科学建模问题的一部分，而非实现细节。不同基的选择在有限数据、噪声和退化导数估计下会影响学习的稳定性与稀疏性。
- 积分可以作为系数读出任务完成：对光滑 Gaussian 积分和强振荡 Fresnel 型积分，SORT 在无需显式求积规则的情况下与闭式解一致；在高维可分离情况下误差随维度增加而逐渐增大。
- 稀疏性 + 正交性提供阶一致模型增长：增加截断阶会保留低阶系数的意义，SORT 在噪声和子采样下维持稳定的低阶系数层级，而 OLS、RBF、RFF 等基线的系数结构随模型规模强烈变化。

### 7. 优点

- **方法层面**：
  - 将稀疏回归与正交基结合，避免了有限库的设计困境，提供了一条区别于符号回归和 SINDy 的互补路线。
  - 统一的中间表示同时服务于系统辨识、积分和近似，具有良好的可复用性。
  - 阶一致模型增长为模型比较与稳定学习提供了实用诊断（系数持久性、激活系数集合、稀疏度等）。
  - 利用积分作为线性泛函的性质实现"系数读出"积分，避免了显式求积的困难。
- **实验层面**：
  - 对比设计公平：SINDy 和 SORT 在相同状态、相同导数估计、相同噪声与子采样条件下训练，并明确区分了"导数估计退化"和"表示失配"两种失效模式。
  - 基准选择有意义：Thomas 吸引子作为匹配场景（两者都好），Bessel 变体作为失配场景（SORT 更稳健），有效说明了表示失配的影响。
  - 实验指标报告规范：短 rollout RMSE 用中位数 + IQR，避免单一随机种子导致的偏差。
  - 对系统排除（Lorenz、非自治、有理/奇异项系统）给出了清晰的理由，增强了实验设计的透明度。

### 8. 不足与局限

- **方法层面**：
  - SORT 并非对表示不匹配免疫——若基选择严重不适合目标函数，性能仍会下降。论文承认这一点但未给出具体的失败案例或边界分析。
  - 基选择和截断阶、正则化参数 $λ$ 的确定仍依赖经验/验证误差，缺乏自适应的基选择机制。
  - 高维情况下基规模随维度迅速增长（维数灾难），虽然提及双曲交叉截断，但论文未系统评估高维场景下 SORT 的性能极限。
- **实验层面**：
  - 缺乏高维方程发现或真实世界数据（实际测量、非合成噪声）的实验验证。
  - 缺乏对 SORT 的消融研究（如 $λ$ 敏感性、不同样本量下系数恢复精度、基截断阶的影响）。
  - 混沌系统的评估只提及"需要吸引子级评估"，但未实际展示此类评估结果。
- **偏差风险**：实验选择偏向于与谱方法天然匹配的系统（光滑、可压缩的函数）。论文通过选择对 SINDy 有利的多项式系统和故意构造的 Bessel 失配系统来展示对比，但未在更广泛的问题族上做系统性的失败率或鲁棒性统计；Bessel 变体本身是为显示 SINDy 弱点而构造的，存在一定"挑选实验"的风险（尽管论文对此有清晰的动机说明）。
- **理论支撑**：论文明确表示不提供新的稀疏恢复理论，依赖现有 LASSO 和压缩感知理论作为背景；对经验正交性在有限、不规则样本下的近似程度仅靠数值评估，缺少深入的理论刻画。在系数稳定性上只有定性的理论说明。

（完）
