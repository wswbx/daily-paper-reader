---
title: Bayesian Symbolic Regression with Entropic Reinforcement Learning
title_zh: 基于熵正则化强化学习的贝叶斯符号回归
authors: "Oussama Boussif, Mohammed Mahfoud, Younesse Kaddar, Moksh Jain, Sida Li, Damiano Fornasiere, Xiaoyin Chen, Yoshua Bengio, Esmeralda S. Whitammer"
date: 2026-08-10
pdf: "https://arxiv.org/pdf/2608.09617v1"
tags: ["query:sr"]
score: 10.0
evidence: 论文直接研究符号回归问题，提出贝叶斯与强化学习结合的表达式搜索方法。
tldr: 符号回归通常寻求单个最优表达式，但无法量化不确定性。为此提出ERRLESS方法，利用最大熵强化学习训练神经策略逐步构造抽象语法树，从而从后验分布中采样表达式。在Feynman基准上，该方法生成简短可解释表达式，且后验预测均值R²优于SMC基线。展示了贝叶斯视角在符号回归中具有的优势。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2608.09617v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1763, \"height\": 659, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.09617v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 856, \"height\": 602, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.09617v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1735, \"height\": 616, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.09617v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1749, \"height\": 836, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.09617v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1753, \"height\": 468, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.09617v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 617, \"height\": 773, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.09617v1/fig-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 883, \"height\": 772, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.09617v1/fig-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 1044, \"height\": 1034, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2608.09617v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1581, \"height\": 483, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.09617v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 928, \"height\": 602, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.09617v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 405, \"height\": 688, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.09617v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 911, \"height\": 422, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.09617v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1152, \"height\": 621, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.09617v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 879, \"height\": 661, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.09617v1/table-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 819, \"height\": 404, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.09617v1/table-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 740, \"height\": 495, \"label\": \"Table\"}]"
motivation: 单一最优表达式无法刻画参数不确定性，需要贝叶斯框架进行不确定度量化。
method: 提出ERRLESS，用最大熵强化学习训练神经策略顺序构造语法树，从后验分布采样。
result: 在Feynman基准上取得有竞争力结果，表达式简短可解释，预测均值R²优于SMC基线。
conclusion: 贝叶斯符号回归能提供不确定度量化，且预测性能更好，具有实际价值。
---

## 摘要
符号回归是寻找一个代数表达式来描述目标变量对一组输入的随机依赖的问题。与假设固定模型结构并拟合参数的回归形式不同，符号回归是在表达式空间中的搜索问题，例如使用操作符库将表达式表示为抽象语法树。符号回归通常用于自然科学中有限且含噪声的数据场景。然而，搜索单个最佳拟合表达式无法捕捉关于表达式的认知不确定性，这激发了贝叶斯视角，该视角能够进行不确定性量化并指定自然先验来约束搜索空间。在这项工作中，我们提出了ERRLESS（用于表达式结构采样的熵正则化强化学习），一种使用最大熵强化学习从给定数据的表达式后验分布中采样的可扩展方法。ERRLESS学习一个神经策略，通过构建抽象语法树来顺序构造表达式。收敛时，该策略从后验中采样表达式。在测试时，可以通过该策略的展开（rollout）来采样表达式。我们证明ERRLESS在Feynman基准上取得了有竞争力的结果，同时生成简短且可解释的表达式。此外，我们证明由ERRLESS近似的后验预测均值与SMC基线相比实现了较高的决定系数（$R^2$），突显了贝叶斯视角在符号回归中的优势。

## Abstract
Symbolic regression is the problem of finding an algebraic expression describing a stochastic dependence of a target variable on a set of inputs. Unlike forms of regression that fit parameters assuming a fixed model structure, symbolic regression is a search problem over the space of expressions, represented, for example, as abstract syntax trees using a library of operators. Symbolic regression is typically used in settings with limited, noisy data in the natural sciences. However, searching for a single best-fitting expression fails to capture the epistemic uncertainty about the expression, which motivates a Bayesian perspective that enables uncertainty quantification and specification of natural priors to constrain the search space. In this work, we propose ERRLESS (Entropy-Regularized Reinforcement Learning for Expression Structure Sampling), a scalable approach for sampling from the posterior distribution over expressions given data using maximum-entropy reinforcement learning. ERRLESS learns a neural policy that constructs expressions sequentially by building up their abstract syntax trees. At convergence, the policy samples expressions from the posterior. At test time, expressions can be sampled by rollouts of this policy. We demonstrate that ERRLESS achieves competitive results on the Feynman benchmark while producing short and interpretable expressions. Additionally, we demonstrate that the mean of the posterior predictive approximated by ERRLESS achieves a high coefficient of determination ($R^2$) compared to an SMC baseline, highlighting the benefits of the Bayesian perspective in symbolic regression.

---

## 论文详细总结（自动生成）

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **研究问题**：符号回归（Symbolic Regression, SR）旨在从数据中搜索一个代数表达式来描述目标变量与输入变量之间的随机依赖关系。传统方法通常只返回一个“最佳”表达式，但在数据有限、噪声较大的自然科学场景中，这种点估计无法刻画表达式结构上的认知不确定性（epistemic uncertainty）。
- **贝叶斯视角的必要性**：论文提出，符号回归应当被看作一个贝叶斯推断问题——在给定数据后，目标是从关于表达式结构、参数和噪声方差的后验分布中采样，而非只寻找单一最优解。这样可以实现不确定性量化，并允许引入自然的领域先验（如物理单位约束、表达式结构约束）来缩小搜索空间。
- **已有方法的局限**：已有的贝叶斯符号回归方法（如基于 reversible-jump MCMC、SMC）依赖手工设计的提议分布，且计算代价高、难以扩展。而基于强化学习的符号回归方法（如 DSR、PhySO）虽然可扩展，但目标是最大化奖励，只能输出点估计，无法近似整个后验。
- **整体含义**：论文提出 ERRLESS，将贝叶斯符号回归建模为最大熵强化学习问题，训练一个神经策略顺序构造表达式的抽象语法树，从而在收敛后直接从后验分布中采样，兼顾不确定性建模、可扩展性和表达式可解释性。

## 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程

- **核心思想**：将表达式构造视为一个序列决策过程，使用最大熵强化学习（Maximum-Entropy RL）训练策略，使策略采样的联合分布 $\pi(\mathrm{T}, \theta, \sigma)$ 逼近目标后验 $p(\mathrm{T}, \theta, \sigma \mid \mathcal{D})$。
- **关键技术细节**：
  1. **表达式表示与自回归生成**：表达式树唯一对应其逆波兰表示（后序遍历序列）$W(\mathrm{T})$。生成过程从左到右逐 token 生成，包括表达式 token 和终止符 $\top$。
  2. **自底向上（bottom-up）生成**：不同于传统自顶向下生成，ERRLESS 先产生叶子节点（变量、常量），再添加操作符。这样可以在每一步检查当前部分表达式是否满足物理单位约束和 arity 约束；一些中间状态本身就是完整的表达式；生成过程可以更高效地约束搜索空间。
  3. **单位约束（Unit constraints）**：每个变量带有物理单位向量（如 7 个基本单位），操作符具有单位分配函数 $U_g$，仅当子表达式单位兼容时才能应用操作符（例如正弦只能作用于无量纲量）。
  4. **结构约束与先验**：禁止逆函数复合（如 $(\sqrt{\cdot})^2$）、禁止三角函数嵌套、禁止指数嵌套、禁止对常量直接应用一元操作符。表达式先验采用基于物理方程语料库统计的 unigram 先验（操作符频率随排名指数衰减）。
  5. **最大熵 RL 与轨迹平衡（Trajectory Balance, TB）目标**：定义奖励 $R(\mathrm{T},\theta,\sigma) = \log p(\mathrm{T},\theta,\sigma) + \sum_i \log \mathcal{N}(y_i; f_{\mathrm{T},\theta}(x_i), \sigma^2)$。训练目标为：
     $$
     \mathcal{L}_{\mathrm{TB}}(\mathrm{T},\theta,\sigma;\varphi) = \left( \log Z_\varphi + \log \pi_\varphi(\mathrm{T},\theta,\sigma) - R(\mathrm{T},\theta,\sigma) \right)^2.
     $$
     最小化此损失等价于使策略分布逼近后验 $p(\mathrm{T},\theta,\sigma \mid \mathcal{D}) \propto \exp(R)$，其中 $Z_\varphi$ 是学习到的配分函数。
  6. **策略参数化**：使用 Transformer 编码器处理部分序列，三个预测头分别输出：下一 token 的 logits（结构）、常量值的 GMM 参数（均值/协方差/权重）、噪声尺度 $\sigma$ 的 log-normal 混合参数。
  7. **训练技巧**：采用 off-policy 训练，使用 $\epsilon$-greedy 探索（线性退火）、优先级回放缓冲区、梯度裁剪（类 Huber）等稳定训练。

## 3. 实验设计：使用的数据集 / 场景、Benchmark、对比方法

- **数据集与场景**：
  1. **合成数据集（Synthetic）**：7 个已知表达式，训练 20 个点、测试 100 个点，最大节点数 $L=9$，最大常量数 $K=3$，用于研究后验建模质量。
  2. **Feynman 符号回归数据库**：标准基准，包含 100 个物理公式和 20 个 bonus 表达式；按照 SRBench 协议去除涉及 arccos/arcsin 的 4 个，共 116 个表达式。训练集 10,000 点，测试集 25,000 点；最大节点数 $L=32$，$K=3$；训练目标加入高斯噪声，噪声水平 $\gamma \in \{0.001, 0.01, 0.1\}$。
  3. **Blackbox benchmark**：来自 PMLB 的 12 个真实数据集，用于评估算法在未知真实公式数据上的表现。
- **对比方法**：包括 GP-GOMEA、Operon、SBP-GP、AFP/AFP_FE、EPLEX、FEAT、FFX、gplearn、ITEA、MRGP、DSR、BSR、AIFeynman、PhySO、PySIPS（SMC 基线）等。
- **评估指标**：
  - 预测准确率：$R^2$ 系数。
  - 鲁棒性：AUC score（取各数据集上中位数 $R^2$ 的生存函数积分）。
  - 后验质量：合成数据上比较后验预测均值 $R^2_{\mathrm{PP}}$ 和负对数似然（NLL），与 SMC 基线 PySIPS 对比。

## 4. 资源与算力

- 论文正文在方法部分未详细说明 GPU 型号、数量或训练总时长。
- 补充材料中提到 ERRLESS 在 Blackbox 基准上运行时使用了一台配备 **L40S GPU、4 CPU、48GB RAM** 的机器。
- 训练预算与 SRBench 协议一致：**每次运行允许 1 百万次奖励评估**（1M reward evaluations）。
- 论文未给出统一的 GPU 总耗时或具体能耗信息，因此算力细节不够透明。

## 5. 实验数量与充分性

- **实验组数**：
  - Feynman 基准：每个表达式 5 个随机种子 × 3 个噪声水平，共 $116 \times 5 \times 3 \approx 1740$ 次运行。
  - 合成数据：10 个随机种子 × 7 个表达式，共 70 次运行。
  - Blackbox 基准：12 个数据集，多随机种子。
  - 消融实验：表达式先验（4 种）、训练目标（TB vs DB）、回放缓冲区混合系数（9 个固定值）各在不同噪声水平下运行。
- **充分性评估**：
  - 优点：覆盖了标准基准、真实数据、合成数据；消融实验覆盖了关键设计选择；使用多种随机种子和噪声水平，统计上较稳健。
  - 不足：Feynman 基准的对比中，ERRLESS 未达到最佳 AUC（低于 GP-GOMEA、Operon 等），论文承认这可能是 1M 评估预算不足所致；Blackbox 基准上 AUC 仅 0.350，低于 XGBoost（0.625），说明在复杂真实数据上仍有明显差距。
  - 公平性方面：总体遵循 SRBench 协议，但 Baselines 的运行时数据来自原文，硬件差异难以完全对齐；论文也承认 PhySO 未提供运行时数据。

## 6. 论文的主要结论与发现

- **后验采样有效性**：在合成数据上，ERRLESS 的后验预测均值 $R^2_{\mathrm{PP}}$ 显著优于 SMC 基线 PySIPS（PySIPS 的预测均值在许多问题上崩塌，甚至出现 $-\infty$ 或极小负值），说明 ERRLESS 能更稳定地捕获后验分布，避免极端样本主导均值。
- **拟合质量**：在 Feynman 数据库上 ERRLESS 的 AUC 达到 0.924，优于 DSR（0.873）、PhySO（0.893）和 BSR（0.702），且随噪声增加几乎不波动，表现出很强的鲁棒性。
- **表达式简洁性**：ERRLESS 生成的表达式较短且可解释。相比之下，许多高 AUC 方法（如 GP-GOMEA、Operon）常产生包含数百个常量的过于复杂的表达式，违背了符号回归追求简洁性的初衷。
- **计算效率**：ERRLESS 避免了对每个候选表达式的内层常量优化循环，因此比大多数学习方法快一个数量级（Fig. 7）。
- **先验与贝叶斯框架的作用**：后验预测均值比单一最优表达式具有更好的泛化性能，在少数据和噪声大的情形下尤为明显，证明了贝叶斯视角在符号回归中的实际价值。

## 7. 优点

- **方法论创新**：首次将“自底向上 + 单位约束 + 最大熵 RL”统一到贝叶斯符号回归框架中。
- **不确定性量化**：不仅输出点估计，而是输出整个后验分布，支持置信区间、后验预测均值和下游概率推断。
- **搜索空间压缩**：通过物理单位约束、结构约束和基于语料统计的先验，显著缩小搜索空间（Fig. 2），提高探索效率。
- **端到端的参数推断**：将表达式结构和常量参数、噪声尺度同时建模在一个策略中，无需外部常量拟合模块，降低了传统符号回归的主要瓶颈——参数优化开销。
- **实验设计较全面**：覆盖多个基准、多种噪声水平、多次随机种子，并进行了消融实验（先验、训练目标、回放缓冲系数），对设计选择有清晰验证。
- **可解释性与简洁性表现好**：在 Feynman 上取得有竞争力的 AUC 同时维持短的表达式长度。

## 8. 不足与局限

- **性能上限受限**：在 Feynman 基准上 AUC 不如 GP-GOMEA、Operon 等进化算法；在 Blackbox 真实数据上明显低于 XGBoost 等 ML 方法，说明对复杂高维数据的适应能力有限。
- **对复杂表达式和后验峰度的敏感性**：论文指出在高度复杂的目标表达式上性能会退化；似然在数据量大时非常尖锐（peaky），给策略学习带来困难。
- **先验偏差风险**：强先验（如偏好简短表达式）可能导致在数据不足时选择近似简化表达式而非真实物理公式（如 $m c^2$ 的例子），尽管测试 $R^2$ 仍很高，但可能偏离真正的科学发现目标。
- **算力细节不透明**：未明确说明训练所需 GPU 总时长、能耗、具体硬件配置（除一台 L40S 外），可复现性和成本评估受限。
- **实验公平性局限**：Baselines 的运行时数据来自原始文献，硬件与实现差异无法完全对齐；Blackbox 基准上 ERRLESS 的 AUC 仅 0.350，与最好的方法差距明显，论文解释为预算限制，但未提供增加预算后的验证。
- **扩展性限制**：目前每次面对新数据集都需要重新训练策略，没有实现跨数据集的元学习；未来工作虽提及，但尚未实现。
- **应用范围**：仅考虑了标准操作符库和代数表达式，未扩展到微分方程或更一般的程序结构。

（完）
