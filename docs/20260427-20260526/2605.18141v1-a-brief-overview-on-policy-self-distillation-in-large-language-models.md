---
title: "A Brief Overview: On-Policy Self-Distillation In Large Language Models"
title_zh: 简要概述：大语言模型中的同策略自蒸馏
authors: "Fangming Cui, Sunan Li, Jiahong Li"
date: 2026-05-18
pdf: "https://arxiv.org/pdf/2605.18141v1"
tags: ["query:opd"]
score: 9.0
evidence: LLM中在策略自蒸馏的综述
tldr: "传统知识蒸馏依赖外部教师模型，资源开销高且存在分布不匹配。On-Policy Self-Distillation (OPSD) 提出统一框架，使单个LLM同时担任教师和学生，教师特权访问验证推理轨迹，学生仅见问题，通过学生自身采样轨迹最小化每标记分布差异。无需外部教师，直接利用真实解信息，GPU内存降低40%-60%，并消除离策略分布漂移。该方法为LLM自我蒸馏提供高效原则性设计，显著降低资源需求并提升训练效率。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.18141v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1323, \"height\": 1437, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2605.18141v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1573, \"height\": 261, \"label\": \"Table\"}]"
motivation: 现有知识蒸馏需外部教师模型，资源消耗大且存在离策略分布不匹配，亟需消除外部依赖并解决分布差异。
method: OPSD中单个LLM同时扮演教师和学生，教师基于真实推理轨迹指导，学生通过自身采样轨迹最小化两角色间的每标记分布差异。
result: "OPSD无需外部教师，直接利用真实解，GPU内存降低40%-60%，有效解决分布不匹配问题。"
conclusion: OPSD为LLM提供高效统一的自我蒸馏框架，降低资源消耗，提升训练效率，推动模型自主优化。
---

## 摘要
同策略自蒸馏（On-Policy Self-Distillation, OPSD）引入了一种统一的学习框架，其中单个大型语言模型同时扮演教师和学生的角色。与依赖于独立的、通常更大的教师模型的传统知识蒸馏不同，OPSD在不同的上下文角色下运行：教师策略被赋予对已验证推理轨迹的特权访问权，而学生策略仅观察问题陈述。OPSD通过最小化学生自身采样的轨迹上两个角色之间的逐令牌分布散度进行训练，从而将其自身的推理行为与有解决方案意识的合理化过程对齐。OPSD消除了对外部教师的需求，直接利用真实解决方案信息，并解决了离策略蒸馏中固有的分布不匹配问题。与标准的同策略蒸馏（OPD）相比，OPSD通常可将GPU内存消耗降低约40%-60%。在本文中，我们简要分析了近期OPSD在大语言模型中进展背后的概念基础、方法创新和原则性设计。本讨论从该领域初学者的视角出发，旨在为同样初涉此领域的研究人员提供关于大语言模型中OPSD设计原则和新兴模式的简洁概述。

## Abstract
On-Policy Self-Distillation (OPSD) introduces a unified learning framework in which a single large language model simultaneously serves as both teacher and student. Unlike conventional knowledge distillation that relies on a separate, often larger teacher model, OPSD operates under different contextual roles: the teacher policy is granted privileged access to verified reasoning traces, while the student policy observes only the problem statement. OPSD is trained to minimize per-token distributional divergence between the two roles over trajectories sampled from the student itself, thereby aligning its own reasoning behavior with solution-aware rationalizations. OPSD eliminates the need for an external teacher, directly leverages ground-truth solution information, and resolves the distribution mismatch inherent in off-policy distillation. OPSD typically reduces GPU memory consumption by approximately 40%-60% compared to standard On-Policy Distillation (OPD). In this paper, we present a brief analysis of the conceptual foundations, methodological innovations, and principled designs underlying recent advances in OPSD for large language models. This discussion, crafted from the perspective of beginners in this field, aims to provide a concise overview of the design principles and emerging patterns of OPSD in LLMs, intended for researchers who are similarly new to this area.

---

## 论文详细总结（自动生成）

# 论文结构化总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **研究动机**：现有大语言模型后训练方法存在固有缺陷：
  - **SFT（监督微调）**：离策略训练，训练分布与推理分布不匹配（exposure bias），导致泛化差和灾难性遗忘。
  - **RL（强化学习，如GRPO）**：奖励信号稀疏（仅序列级标量），难以进行逐令牌信用分配，且批内样本全对或全错时梯度消失。
  - **OPD（同策略蒸馏）**：依赖更强的外部教师模型，内存开销大，且师生能力差距过大会导致优化不稳定。
- **整体含义**：论文提出 **On-Policy Self-Distillation (OPSD)** 作为统一解决方案，使单个模型同时充当教师和学生，消除对外部教师的依赖，并解决分布不匹配问题。这是一篇面向初学者的概念性综述，梳理了OPSD的设计原理和新兴模式。

## 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程

- **核心思想**：同一模型扮演两个角色——学生仅看到问题，教师额外获得特权信息（如真实答案或推理轨迹）。学生采样轨迹，教师提供逐令牌分布监督，最小化两者之间的分布散度。
- **关键技术细节**：
  - **学生策略**：𝜋_stud(·|x)（仅条件于提示）。
  - **教师策略**：𝜋_teach(·|x, y*)（额外条件于真实答案y*）。
  - 训练过程：从学生分布采样轨迹 ŷ，计算每个位置t上两个分布的KL散度（或JSD），反向传播更新共享参数。
- **公式说明**：
  - OPSD损失：L_OPSD = E_{(x,y*), ŷ~π_stud}[ Σ_t KL(π_stud(·|x,ŷ_<t) || π_teach(·|x,y*,ŷ_<t)) ]
  - 推广的Jensen-Shannon散度（JSD_β）：JSD_β(p_T||p_S) = β·KL(p_T||m) + (1-β)·KL(p_S||m)，其中m=βp_T+(1-β)p_S。β=0.5时为标准对称JSD，β接近1或0分别对应前向/反向KL。
- **算法流程**：输入提示+真实答案 → 学生采样回答 → 教师（同模型）在特权信息下计算分布 → 计算逐令牌散度损失 → 更新共享参数。

## 3. 实验设计：数据集、基准、对比方法

- **论文本身是综述，未开展原创实验**，但列举了OPSD相关工作中常用的数据集和基准：
  - **数学推理**：GSM8K、MATH、AIME、OpenThoughts
  - **代码生成**：HumanEval、MBPP、LiveCodeBench
  - **Agent交互**：WebShop、ALFWorld
  - **安全对齐**：UltraFeedback、BeaverTails
  - **视觉多模态**：COCO、MMMU
- **对比方法**：SFT、GRPO（RL方法）、OPD（同策略蒸馏）
- **论文通过表格比较了四种方法**（SFT、GRPO、OPD、OPSD）的关键特性（监督类型、关键优势、关键弱点）。

## 4. 资源与算力

- 明确提到：实验可在 **8张A100 GPU** 上运行，覆盖大多数场景。
- OPSD与标准OPD相比，**GPU内存消耗减少约40%-60%**。
- 未提供具体训练时长或GPU型号（如A100-80G还是40G），也未说明实验次数。

## 5. 实验数量与充分性

- **该论文不是实验论文，而是文献综述**，因此没有自己的实验设计和数据。
- 充分性评价：论文汇总了大量现有工作（约60篇参考文献），并对OPSD的各种设计变体（混合策略、反馈驱动、稳定性校准、推理压缩、能力内化、多模态、轨迹蒸馏、理论框架）进行分类描述，覆盖面较广。
- 客观性与公平性：论文未进行系统性对比实验，无法评估性能优劣。作为初学者读物，描述较为中立，但缺乏实证验证。

## 6. 论文的主要结论与发现

- OPSD在训练成本上具有优势，非常适合工业界快速尝试业务场景。
- 消除了对外部教师模型的依赖，直接利用真实答案进行监督，解决了离策略分布不匹配问题。
- 内存开销显著降低（比OPD减少40%-60%），在超大规模模型场景中OPSD往往是唯一可行方案。
- OPSD提供密集的逐令牌监督，克服了RL奖励稀疏的信用分配问题。
- 通过共享参数和特权信息，模型能够将“有信息的”教师行为内化为“盲目的”学生行为。

## 7. 优点

- **概念创新**：单模型双角色，自蒸馏无需外部教师，节省资源。
- **框架统一**：将SFT、RL和蒸馏的优势结合，兼备同策略训练和密集监督。
- **设计丰富**：论文系统梳理了8类设计方向（混合策略、反馈驱动、稳定性校准、推理压缩等），展现了OPSD的灵活性和可扩展性。
- **实用性**：明确指出常见实验配置（8*A100），降低入门门槛。

## 8. 不足与局限

- **缺乏原创实验**：论文为综述，没有提供任何新的实验结果或性能对比，对实际效果缺乏定量支撑。
- **局限性讨论不足**：虽然文中提到“需要小心处理特权信息”以及“过置信退化问题”（列举了AntiSD等工作），但未深入分析OPSD的潜在风险（如教师信号退化、OOD泛化下降）。
- **覆盖可能不全面**：尽管参考文献较多，但OPSD领域仍在快速发展，可能存在未纳入的重要工作。
- **应用限制**：特权信息（真实答案）在真实场景中不一定易得，限制了方法的通用性。

（完）
