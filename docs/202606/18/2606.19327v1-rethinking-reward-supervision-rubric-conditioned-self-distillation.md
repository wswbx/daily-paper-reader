---
title: "Rethinking Reward Supervision: Rubric-Conditioned Self-Distillation"
title_zh: 重新思考奖励监督：基于评分准则的条件自蒸馏
authors: "Siyi Gu, Jialin Chen, Sophia Zhou, Arman Cohan, Rex Ying"
date: 2026-06-17
pdf: "https://arxiv.org/pdf/2606.19327v1"
tags: ["query:opd"]
score: 9.0
evidence: 提出基于评分标准的在线自蒸馏方法，用于推理语言模型
tldr: 当前推理语言模型后训练依赖昂贵的思维链标注或标量奖励信号，存在噪声和细粒度不足的问题。本文提出评分标准条件自蒸馏（Rubric-Conditioned Self-Distillation），通过结构化细粒度评分标准为学生模型采样轨迹提供token级指导，将准则级反馈转化为推理过程中的精确信用分配。两阶段流程先学习生成任务特定评分标准，再训练评分引导的推理器。在科学推理基准上，该方法平均超越GRPO 1.0分、OPSD 0.9分，显著提升了推理过程的学习质量。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.19327v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 696, \"height\": 514, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.19327v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1440, \"height\": 539, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.19327v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1293, \"height\": 440, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.19327v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1432, \"height\": 337, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.19327v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1291, \"height\": 240, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.19327v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1325, \"height\": 313, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.19327v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1261, \"height\": 204, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.19327v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 704, \"height\": 279, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.19327v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1230, \"height\": 225, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.19327v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1306, \"height\": 240, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.19327v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1242, \"height\": 314, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.19327v1/table-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 1218, \"height\": 355, \"label\": \"Table\"}]"
motivation: 现有监督蒸馏依赖昂贵且有噪声的思维链标注，强化学习仅提供标量奖励，缺乏细粒度指导方向。
method: 提出Rubric-Conditioned Self-Distillation，利用结构化评分标准作为教师模型的细粒度条件，对学生采样轨迹提供token级指导，分两阶段生成评分标准和训练推理器。
result: 在科学推理基准上平均超越GRPO 1.0分、OPSD 0.9分。
conclusion: 将评分标准级反馈转化为token级推理指导，实现更精准的信用分配，有效提升推理模型性能。
---

## 摘要
推理语言模型的后训练通常依赖于有监督蒸馏和基于可验证奖励的强化学习。蒸馏往往需要昂贵的思维链标注，而这些标注本身可能存在噪声、不完整或部分错误；即使最终解答正确，不完美的推理过程也会干扰学习。另一方面，基于验证奖励的强化学习通常将评估反馈压缩为标量信号，掩盖了响应中哪些方面需要改进。我们提出**基于评分准则的条件自蒸馏**框架，该框架将评分准则作为结构化细粒度反馈，用于在线策略自蒸馏。我们的方法使教师模型以准则级别的评分条件为输入，并提供对学生自身采样轨迹的词元级指导。这一设计避免了将单一参考推理作为唯一监督目标。相反，评分准则明确了强响应应满足的条件，从而比标量奖励优化实现了更细粒度推理过程的信用分配。我们通过两阶段流程实例化该框架：首先学习生成任务特定的评分准则，然后训练评分引导的推理器。我们在多个科学推理基准上进行评估，结果表明，基于评分准则的条件自蒸馏能有效地将评分准则级别的标准转化为推理过程中的词元级指导，平均超过GRPO 1.0个点，超过OPSD 0.9个点。

## Abstract
Post-training of reasoning language models is commonly driven by supervised distillation and reinforcement learning with verifiable rewards. Distillation often relies on chain-of-thought annotations that are expensive to obtain and may themselves be noisy, incomplete, or partially incorrect; even when the final solution is correct, an imperfect rationale can interfere with learning. Reinforcement learning with verified rewards, on the other hand, typically compresses evaluative feedback into a scalar signal, obscuring which aspects of a response should be improved. We propose \textbf{Rubric-Conditioned Self-Distillation}, a framework that incorporates rubrics as structured, fine-grained feedback for on-policy self-distillation. Our method conditions the teacher model on criterion-level rubrics and uses it to provide token-level guidance on the student's own sampled trajectories. This design avoids treating a single reference rationale as the sole supervision target. Instead, rubrics specify what a strong response should satisfy, enabling more fine-grained credit assignment over the reasoning process than scalar reward optimization. We instantiate this framework with a two-stage pipeline that first learns to generate task-specific rubrics and then trains a rubric-guided reasoner. We evaluate on a diverse suite of science reasoning benchmarks and results show that rubric-conditioned self-distillation effectively converts rubric-level criteria into token-level guidance over the reasoning process, surpassing GRPO by 1.0 points and OPSD by 0.9 points on average.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **问题**：现有推理语言模型的后训练方法存在两种典型局限：
  - **蒸馏方法**（如 Off-Policy Distillation）依赖昂贵的思维链标注，这些标注可能带有噪声、不完整，即使最终答案正确，其推理过程也可能误导学生。
  - **强化学习方法**（如 GRPO）仅使用稀疏的标量奖励信号（最终答案是否正确），缺乏对推理过程中具体步骤的细粒度信用分配，无法明确告知模型“哪里需要改进”。
- **整体含义**：本文试图用**结构化、细粒度的评分准则（Rubric）** 代替单一的参考推理链或标量奖励，将其作为教师模型的监督接口，通过在线自蒸馏（on-policy self-distillation）的方式，为学生模型自身生成的轨迹提供**词元级（token-level）指导**，从而提升推理质量。

### 2. 方法论：核心思想与关键技术细节

- **核心思想**：评分准则（rubric）不应只在生成后用于评分，而应直接塑造优化过程中的词元级学习信号。本文设计**Rubric-Conditioned Self-Distillation（RCSD）**，保持评分准则的细粒度结构，将其作为教师端的特权信息，对学生的在线采样轨迹进行基于前向KL散度的蒸馏。
- **关键技术细节**：
  - **两阶段流程**：
    - **Stage I：学习评分准则生成器**  
      训练一个模型将问题 `x` 映射为结构化评分准则 `r`（包含多个准则，每个有标题、描述、重要性权重）。教师端使用问题+参考答案作为特权信息，学生端仅使用问题，通过对学生自身轨道的在线蒸馏（式(4)）学习生成准则。
    - **Stage II：训练评分条件推理器**  
      推理器的学生端仅接受问题 `x`，教师端接受问题 `x` 和第一阶段生成的评分 `r`。学生先生成采样轨迹 `ŷ`，教师在此基础上提供词元级指导，通过最小化前向KL散度（式(5)）进行蒸馏。
- **公式/算法**：  
  - Stage I 目标：\( \mathcal{L}_{\text{rubric}} = \mathbb{E}_{\hat{r} \sim p_R^S} [\sum_t D_{KL}(p_R^T(\cdot|x, y^*, \hat{r}_{<t}) \parallel p_R^S(\cdot|x, \hat{r}_{<t}))] \)  
  - Stage II 目标：\( \mathcal{L}_{\text{reason}} = \mathbb{E}_{\hat{y} \sim p_Y^S} [\sum_t D_{KL}(p_Y^T(\cdot|x, r, \hat{y}_{<t}) \parallel p_Y^S(\cdot|x, \hat{y}_{<t}))] \)  
  - 算法流程（Algorithm 1）：先循环更新评分生成器，再循环更新推理器，均使用在线采样。

### 3. 实验设计

- **数据集**：
  - 训练数据：RaR-Science、RubricHub 用于评分准则学习（约10k样本）；NaturalReasoning 过滤后用于推理训练（约30k样本）。
  - 评估基准（6个科学推理任务）：
    - **可验证任务**：GPQA-Diamond、SciBench、PIQA、ResearchQA（部分可验证）。
    - **非可验证任务**：RaR-Science、RubricHub（需要 LLM-as-a-Judge 评分）。
    - **泛化任务**：MedMCQA、PubMedQA（医学问答）。
- **Benchmark 对比方法**：
  - SFT（监督微调）
  - GRPO（标准标量奖励强化学习）
  - GRPO-Rubrics（用评分准则打分后再聚合为标量奖励）
  - OPSD（On-Policy Self-Distillation，教师条件为参考答案）
  - RCSD（本文方法）
- **骨干模型**：Qwen3-8B（主要实验），同时进行模型规模消融（1.7B、4B、8B）。
- **实现细节**：LoRA (r=64, α=128)，AdamW lr=5e-6，batch size=32，FlashAttention 3。评分生成器训练1000步，推理器训练100步，GRPO训练500步。评估时温度=1.0，top-p=0.95，结果取4次生成平均。LLM-as-a-Judge 使用 gpt-4.1-mini。

### 4. 资源与算力

- **文中明确说明的配置**：LoRA、AdamW、batch size 32、FlashAttention 3。训练步数：评分生成器1000步，推理器100步。
- **未明确说明的内容**：未提及使用的GPU型号、数量、训练总时长（小时数）以及最大序列长度（仅提及评分生成器最大长度2048，推理器4096）。因此无法准确评估算力开销。

### 5. 实验数量与充分性

- **实验数量**：
  - 主表（Table 1）在6个基准上比较了5种方法。
  - 泛化实验（Table 2）在2个医学基准上比较4种方法。
  - 消融实验：
    - 损失类型（Table 3）：Forward KL、Reverse KL、JSD。
    - 模型规模（Figure 4）：1.7B、4B、8B 在3个基准上。
    - 评分来源（Table 4）：真实评分 vs. 生成评分。
    - 第一阶段必要性（Table 5）：直接使用8B/14B教师生成 vs. 学习生成器。
    - 评分质量退化（Table 6）：通用、随机、噪声、精简 vs. 学习的评分。
    - 训练步数分析（Figure 5）：GPQA-D和SciBench上的检查点曲线。
    - 案例分析（C.1）：理想气体定律问题对比。
    - 失败分析（C.2）：学习到的评分与参考评分的对比。
- **充分性与公平性**：
  - 对比方法设置统一（SFT、GRPO、OPSD等均在同一骨干上训练，GRPO多训练步数但已控制）。
  - 消融覆盖全面，包括损失函数、模型规模、评分质量等关键因素。
  - 测试时使用多次生成取平均，减少随机波动。
  - 但是所有实验均基于同一系列模型（Qwen3），未与其他架构（如 LLaMA）或更大规模模型对比，泛化性验证有限。

### 6. 论文的主要结论与发现

- **核心发现**：RCSD 在科学推理基准上平均达到70.6%，超过 GRPO（1.4点）和 OPSD（0.9点）；在非可验证的评分准则任务（ResearchQA、RubricHub）上提升尤为显著。
- **关键结论**：
  - 前向KL散度优于反向KL和JSD，能更好地保留教师的分支覆盖能力。
  - 学习到的评分生成器可接近真实评分效果，且对评分质量退化具有一定鲁棒性。
  - RCSD 在 1.7B~8B 模型上均有效，且规模越大增益越明显（例如8B在RaR上提升8.9点）。
  - 提出的 Framework 无需外部奖励模型，避免了标量奖励的局限性。

### 7. 优点

- **方法创新**：将评分准则作为结构化监督界面，在蒸馏过程中保留细粒度信用分配，避免了标量化信息损失和单一路径偏差。
- **理论支撑**：附录D提供了对评分近似误差的鲁棒性界限（Proposition 1），说明生成评分不需要完全匹配真实评分，只要引起的条件分布相近即可。
- **实验全面**：覆盖了可验证、非可验证、医学泛化等多种场景；消融实验系统，包括损失函数、模型规模、评分质量等；并包含定性案例分析和失败分析。
- **实用性**：无需大规模手动标注评分，通过两阶段自动生成评分，降低了应用门槛。

### 8. 不足与局限

- **实验覆盖**：主实验仅基于 Qwen3-8B（以及规模消融），未在其他模型家族（如 LLaMA、DeepSeek）或更大规模（70B+）上验证，可能限制结论的泛化性。
- **资源未明确**：计算资源（GPU型号、数量、训练时长）未详细报告，难以复现或评估成本。
- **评分生成器质量**：失败分析指出学习到的评分有时冗余、重叠，且包含非关键项，虽然实验显示对结果影响不大，但仍有提升空间。
- **评估偏差**：非可验证任务依赖 LLM-as-a-Judge（gpt-4.1-mini），可能存在偏好或校准误差，尽管实验使用了参考评分对比。
- **应用限制**：主要针对科学推理和医学领域，对开放领域、创意生成等任务的效果尚未探索。依赖问题前需要一定数量的参考答案进行评分生成器训练，对低资源场景可能不友好。

（完）
