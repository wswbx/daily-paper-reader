---
title: "OPD+: Rethinking the Advantage Design for On-Policy Distillation"
title_zh: OPD+：重新思考在线策略蒸馏中的优势设计
authors: "Hanyang Zhao, Haoxian Chen, Han Lin, Genta Indra Winata, David Yao, Wenpin Tang"
date: 2026-05-31
pdf: "https://arxiv.org/pdf/2606.01039v1"
tags: ["query:opd"]
score: 9.0
evidence: 重新设计优势函数的在线策略蒸馏方法
tldr: 在线蒸馏(OPD)是大型语言模型能力迁移的重要技术，但其优势估计因stop-gradient操作存在偏差。本文基于f-divergence提出通用优化框架，从数学上证明stop-gradient导致奖励目标与梯度估计有偏，并给出修正版本OPD+。在数学推理和工具使用基准上，OPD+显著优于基线KL方法，且支持多种f-divergence选择。该工作为在线蒸馏的理论基础提供了重要修正。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.01039v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1467, \"height\": 496, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.01039v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 721, \"height\": 563, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.01039v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 798, \"height\": 190, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.01039v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1483, \"height\": 510, \"label\": \"Table\"}]"
motivation: 现有在线蒸馏方法中stop-gradient设计导致优势估计偏差，缺乏理论保证。
method: 基于f-divergence构建通用优化框架，修正stop-gradient带来的梯度偏置，提出OPD+。
result: 在数学推理和工具使用任务上，OPD+优于基线KL方法，且支持多种f-divergence。
conclusion: OPD+纠正了在线蒸馏的优势估计偏差，提供了更稳定的理论框架和性能提升。
---

## 摘要
在线策略蒸馏（OPD）是一种广泛使用的技术，用于将能力强大的教师语言模型的能力迁移到基础学生模型，并且可以通过使用学生生成的轨迹以强化学习风格的目标函数进行形式化。然而，尽管散度奖励依赖于学生模型的可能性，现有工作通常采用停止梯度设计主要是为了稳定性，这使得得到的优势估计存在问题。在这项工作中，我们提供了一个基于学生和教师之间f-散度的通用优化框架，并从数学上重新审视这样的设计空间是否有效。我们证明，对于一般的散度函数，通用的停止梯度操作会导致奖励目标及其相应梯度的有偏估计。我们提出了OPD+，这是OPD的修正版本，在基线KL方法上展示了改进的性能，并且还支持各种f-散度的选择。我们在数学推理和工具使用基准上验证了我们的发现。

## Abstract
On-policy distillation (OPD) is a widely used technique to transfer capabilities from capable teacher language models to the base student models, and can be formulated in a reinforcement learning style objective using student generated rollouts. Yet, despite the divergence reward being dependent on student model likelihood, existing works usually adopt a stop gradient design primarily for stability, which makes the resulting advantage estimation questionable. In this work, we provide a generic optimization framework based on f-divergence between the student and teacher, and mathematically revisit whether such design space is valid. We prove that general stop-gradient operation would lead to biased estimates of the reward objective and corresponding gradient for general divergence functions. We propose OPD+, the corrected version of OPD that demonstrates improved performance over the baseline KL approach and also supports the choice of various f-divergence. We validate our findings on mathematical reasoning and tool-use benchmarks.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）

在线策略蒸馏（OPD）是大型语言模型（LLM）后训练阶段常用的能力迁移技术，它通过让学生模型在学生自身生成的序列上模仿教师模型的分布（通常使用KL散度作为奖励）来学习。然而，现有OPD方法普遍采用“停止梯度（stop-gradient）”操作，即将散度奖励视为固定值（不随学生模型参数变化），这一设计主要出于数值稳定性考虑。论文指出，这种处理方式会导致奖励目标和对应梯度的有偏估计，因为奖励函数本身是依赖于学生模型参数的（通过密度比）。作者希望从理论上重新审视这一设计，并给出更通用的f-散度优化框架，从而纠正偏差，提升OPD的性能。

### 2. 论文提出的方法论：核心思想、关键技术细节

- **核心思想**：将OPD的目标推广到一般的f-散度（不仅是KL散度），并推导出正确的梯度表达式，指出stop-gradient下的优势函数估计存在系统性偏差。
- **关键技术细节**：
  - 定义token级别的密度比 \( u_{\theta,n} = q(y_n | y_{<n}, x) / p_{\theta}(y_n | y_{<n}, x) \)，f-散度目标为 \( J_f(\theta) = E[\sum_{n} f(u_{\theta,n})] \)。
  - 若采用stop-gradient，即奖励 \( r = -f(u_{\theta,n}) \) 视为常数，梯度为：\( E[\nabla \log p_{\theta} \cdot (-f(u_{\theta,n}))] \)。
  - 正确的梯度应包含奖励对θ的直接导数，修正后的梯度权重为 \( w_f(u_{\theta,n}) = -f(u_{\theta,n}) + u_{\theta,n} f'(u_{\theta,n}) \)。
  - 对于Reverse KL（\( f(u) = -\log u \)），\( w_f(u) = -\log u + 1 \)，额外的常数1在期望下消失（由于score identity），因此stop-gradient对Reverse KL并无理论偏差，但实践中添加该常数可降低方差。
  - 对于Forward KL和JSD，停止梯度会导致严重偏差，造成训偏甚至坍塌。
  - 论文给出表1对比不同散度下stop-gradient优势与修正优势的表达式。

### 3. 实验设计：数据集、场景与对比方法

- **任务场景**：
  - 竞赛数学：在DeepMath数据集上蒸馏，评估基准为AIME 24、AIME 25、HMMT 25。
  - 工具使用（Tool-Use）：在ToolUse任务上蒸馏，评估其68题的测试集。
  - 自蒸馏（OPSD）：使用Qwen3-1.7B Instruct作为学生，教师模型是自身但使用特权信息（问题+标准答案）。
- **对比方法**：
  - 基线：标准的OPD（使用stop-gradient优势），包括Reverse KL、Forward KL、JSD。
  - 提出的OPD+：修正后的优势（使用正确的权重\( w(u) \)）。
  - 此外还对比了SFT初始化的学生基线。
- **评估指标**：avg@32（平均每个样本32次采样后的准确率估计）。

### 4. 资源与算力

论文中未明确说明使用的GPU型号、数量或训练时长。仅提到数学任务上学生模型训练100个on-policy step（batch size为64组），工具使用任务训练80个step（每步256个rollout）。未提及硬件配置。

### 5. 实验数量与充分性

- **实验数量**：主要实验结果见表2，涵盖了3种数学基准、1种工具使用基准，以及自蒸馏（OPSD）场景，对应不同散度类型（Forward KL、JSD、Reverse KL）的stop-gradient和OPD+版本。此外还提供了图1展示训练动态，图2展示OPSD训练曲线。
- **充分性分析**：实验覆盖了多种散度、多个任务，并加入了自蒸馏设定，比较全面。但论文作者在局限性中也承认：未进行详细的模型规模分析（仅使用了Qwen3-1.7B和8B），未在Qwen模型族之外验证泛化能力，未比较单token蒸馏与logit蒸馏的性能差异。整体实验设计较充分，但泛化性测试和超参数敏感性分析略缺。

### 6. 论文的主要结论与发现

- stop-gradient操作会导致f-散度奖励的梯度估计偏差，对于Forward KL和JSD，偏差引发训练完全崩溃（准确率降至基线或0%）。
- OPD+通过使用正确的权重\( w(u) \)完全消除了这种崩溃，并显著提升了性能：
  - JSD+在AIME 25上达到50.42%，超过Reverse KL的48.54%；在Tool Use上达到60.29%，超过反向KL的59.97%。
  - Reverse KL+虽然在理论上梯度无偏，但实际训练中因方差降低而加速收敛，并在多数指标上略优于Reverse KL。
- 修正后的JSD表现出持续的后程优化韧性，训练后期性能仍能提升，而其他方法开始下降。
- 研究表明，历史上其他f-散度在OPD中表现不佳完全是因优势设计错误，而非散度本身的缺陷。

### 7. 优点

- **理论贡献**：首次从数学上严格证明了stop-gradient在OPD中造成的偏差，并给出通用修正公式，澄清了“Reverse KL特殊”的本质。
- **方法简洁**：修正仅需一行代码改变，即替换优势函数为\( w(u) \)。
- **实验效果显著**：不仅恢复了Forward KL和JSD的可训练性，而且超越了现有行业标准（Reverse KL），尤其JSD+展现出优异的长时训练稳定性。
- **内容丰富**：涵盖了两种训练范式（标准OPD和自蒸馏OPSD），验证了修正方法的通用性。

### 8. 不足与局限

- **模型规模分析不足**：仅测试了1.7B和8B的Qwen模型，未在更大模型或不同模型族上验证，难以判断结论的泛化性。
- **缺少logit蒸馏对比**：未与更复杂的top-k logit蒸馏方法进行比较，而现有实际部署中常用logit蒸馏。
- **超参数敏感性未知**：未报告不同学习率、batch size等对OPD+的影响。
- **理论分析未覆盖折扣因子**：仅推导了γ=0（立即奖励）的情况，γ>0的情况未讨论。
- **实验评估指标单一**：仅使用avg@32，未提供其他指标如方差或pass@k等。

（完）
