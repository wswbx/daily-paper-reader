---
title: "Select-to-Act: Hierarchical Reinforcement Learning via Adaptive Language Guidance"
title_zh: 选择即行动：通过自适应语言引导的分层强化学习
authors: "Hanping Zhang, Adam Koziak, Yuhong Guo"
date: 2026-06-21
pdf: "https://arxiv.org/pdf/2606.22350v1"
tags: ["query:skill-rl"]
score: 9.0
evidence: 基于语言指导的分层强化学习，专注于技能学习
tldr: 强化学习在复杂环境中样本效率低，现有方法将整个指令作为单一条件输入，忽视了指令的阶段依赖性。本文提出HRLLI分层强化学习框架，将指令分解为可动态选择的语义指导元素，高层策略选择当前最相关的指令片段来引导低层决策，两层策略联合学习。在RTFM基准上，HRLLI显著优于指令条件化的RL基线，证明显式建模自适应指令选择能有效提升RL性能。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.22350v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1087, \"height\": 274, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.22350v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1166, \"height\": 637, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.22350v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1199, \"height\": 409, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.22350v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1261, \"height\": 394, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.22350v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1259, \"height\": 376, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.22350v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1257, \"height\": 374, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.22350v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 905, \"height\": 258, \"label\": \"Table\"}]"
motivation: 现有指令引导的RL方法将完整指令作为单一条件，无法适应复杂环境中指令的阶段相关性，导致样本效率低。
method: 提出HRLLI框架，将指令分解为片段，高层策略按状态选择最相关指导，低层策略基于所选指导执行动作，两层联合优化。
result: 在RTFM基准上，HRLLI一致超越指令条件化的强基线，验证了自适应指令选择的有效性。
conclusion: 显式建模指令的阶段选择性可显著提升RL的样本效率和任务表现，为语言引导的层次化决策提供了新范式。
---

## 摘要
强化学习已广泛应用于序贯决策，但由于与环境交互的成本高昂，其样本效率往往较低。近期有限的工作开始探索利用自然语言指令中的外部知识来提高强化学习效率。然而，现有方法通常将整个指令作为单一条件输入，未能考虑语言引导的阶段依赖性，尤其是在复杂环境中。本文提出了一种分层强化学习框架HRLLI（带语言指令的分层强化学习），该框架在决策过程中显式地将自然语言指令建模为可动态选择的语义引导。HRLLI将指令分解为一组分段引导元素，每个指令片段可能在与环境交互的不同阶段变得相关。随后，在“选择即行动”范式下构建了一种新颖的分层强化学习策略结构：高层语义策略作为引导选择器，选择与当前状态最相关的指令片段以指导低层智能体的决策，而低层策略则基于所选引导执行环境动作。两层策略同时学习，以最大化与环境交互的增强期望回报。这种设计使智能体能够在交互过程中自适应地将语言指令落地为阶段特定决策。在指令密集型RTFM基准上的实验表明，HRLLI始终优于强指令条件强化学习基线，证明了显式建模自适应指令选择显著提高了强化学习的有效性。

## Abstract
Reinforcement Learning (RL) has been widely applied to sequential decision-making, yet it often suffers from poor sample efficiency due to costly interactions with the environment. A limited line of recent work has started exploring improving RL efficiency by leveraging external knowledge expressed in natural-language instructions. However, the few existing approaches typically treat the entire instruction as a single conditioning input, failing to account for the stage-dependent nature of language guidance, especially in complex environments. In this paper, we propose \emph{Hierarchical Reinforcement Learning with Language Instructions (HRLLI)}, a hierarchical RL framework that explicitly models natural-language instructions as dynamically selectable semantic guidance during decision-making. HRLLI decomposes instructions into a set of piecewise guidance elements, where each instruction piece may become relevant at different stages of interaction with the environment. A novel hierarchical RL policy structure is then formulated in a \emph{Select-to-Act} paradigm: a high-level semantic policy acts as a guidance selector that selects the most relevant instruction piece to the current state to guide the low-level agent's decision, while a low-level policy executes environment actions conditioned on the selected guidance. The two-level policies are learned simultaneously to maximize augmented expected returns from interactions with the environment. This design enables the agent to adaptively ground language instructions into stage-specific decisions during interaction. Experiments on the instruction-intensive RTFM benchmark show that HRLLI consistently outperforms strong instruction-conditioned RL baselines, demonstrating that explicitly modeling adaptive instruction selection significantly improves the effectiveness of RL.

---

## 论文详细总结（自动生成）

# 论文《Select-to-Act: Hierarchical Reinforcement Learning via Adaptive Language Guidance》详细中文总结

## 1. 核心问题与整体含义（研究动机和背景）

- **背景**：强化学习（RL）在序贯决策中广泛应用，但样本效率低是主要瓶颈。近期有工作尝试利用自然语言指令中的外部知识来提升效率，例如将整个指令集编码为单一条件输入来指导策略。
- **核心问题**：现有方法未考虑指令的**阶段依赖性**——在复杂环境中，不同指令片段仅在任务的不同阶段才相关。将全部指令无差别地作为条件，会导致语言落地不准确，限制了语言引导对RL训练效率的提升潜力。
- **研究目标**：提出一种能自适应选择当前最相关指令片段来指导决策的分层RL框架，从而更有效地利用语言指令集，提升样本效率和任务表现。

## 2. 方法论：核心思想与关键技术细节

- **核心思想**：将语言指令集分解为多个独立的语义片段（句子级），通过**选择-执行（Select-to-Act）**的分层范式，让高层策略根据当前状态从指令集中选择最相关的一条，随后低层策略基于所选指令执行动作。两层策略联合优化，实现自适应的语言落地。

- **关键技术细节**：
  - **指令与状态编码**：
    - 使用冻结的预训练语言模型（MiniLM）提取指令嵌入，再通过可训练的MLP投影头映射到共享的d维空间。
    - 状态编码器采用类似txt2π的残差CNN结构，将状态（文本+视觉特征）映射到同一d维空间。
  - **高层指令选择策略（Selector）**：
    - 每隔T个环境步骤执行一次选择（固定影响范围）。
    - 计算当前状态嵌入与每条指令嵌入的兼容性分数（可学习打分函数），通过softmax得到选择分布。
    - 训练时从分布中采样，评估时选择最高分指令。
    - 训练目标：最大化**预测的段奖励**（由奖励模型给出的T步累积奖励估计）与**辅助奖励**（所选指令嵌入与段平均状态嵌入的余弦相似度）的加权和，使用REINFORCE算法优化。
  - **低层动作执行策略（Executor）**：
    - 以当前状态嵌入和所选指令嵌入为条件，输出动作。
    - 训练目标：最大化环境奖励与预测步奖励加权和的累积折扣回报，使用PPO算法优化。
  - **奖励模型（Reward Model）**：
    - 学习一个MLP，输入为 (st, at, st+1)，输出预测的即时奖励，用于提供密集训练信号。
    - 通过MSE损失从收集的过渡中学习。
  - **联合训练**：状态编码器同时接收高层和低层目标的梯度，确保共享表示既支持指令选择又支持动作执行。

- **算法流程**（文字描述）：
  1. 对每个指令编码得到嵌入集合。
  2. 在每个高层决策点（kT时间步），根据当前状态计算分数并采样一个指令。
  3. 设定该指令的嵌入作为引导向量，低层策略基于此执行T步，收集过渡数据。
  4. 每段结束后，通过奖励模型计算预测段回报，并计算余弦相似度辅助奖励。
  5. 将高层过渡存入缓冲，更新奖励模型。
  6. 使用REINFORCE更新高层选择器（含状态编码器和指令投影头），使用PPO更新低层执行器。
  7. 共享状态编码器从两个目标同时获取梯度。

## 3. 实验设计

- **数据集/场景**：使用**RTFM（Read to Fight Monsters）**基准，这是一个文本格式的网格世界，包含玩家、怪物、物品，并提供任务描述和指令集（Wiki文档）。环境具有稀疏奖励和过程生成的指令。
- **维度设置**：
  - 怪物数量：**单怪物（One）**和**双怪物（Two）**。
  - 语言风格：简单语言（SL）和自然语言（NL）。
  - 指令顺序：固定和打乱（Shuffle）。
- **对比方法**：
  - **txt2π**（中文学会）：基于指令条件化策略的基线，训练1M和100M步两个版本。
  - **Reader**：基于模型的语言指令RL方法，使用MCTS规划。还包含一个Transformer基线的变体。
  - **Oracle MCTS**：上界基线，假设可访问真实环境模拟器进行MCTS规划。
- **评估指标**：**胜率（Win rate）**，即成功完成任务的比率。结果报告为五次运行的平均值±标准差。

## 4. 资源与算力

- 论文中**未明确说明**使用的GPU型号、数量或训练时长。仅提到训练运行10^6环境步，每5000步评估一次。低层策略更新使用Stable-Baselines3实现，奖励模型使用Adam优化器，学习率3×10⁻⁴。
- 可以推测实验在单GPU或小型计算集群上完成，但具体算力信息缺失。

## 5. 实验数量与充分性

- **实验数量**：
  - 主要结果：单怪物设置（4种配置）和双怪物设置（4种配置），共8组完整比较。
  - 消融研究（表3）：在双怪物NL设置上比较了3种变体（无硬选择、无高层编码器更新、替代辅助奖励）。
  - 另外进行奖励模型设计的消融（图3）：5种变体。
  - 超参数敏感性分析（图4）：对高层选择间隔T和辅助奖励权重λsel进行扫描。
- **充分性与公平性**：
  - 实验覆盖了主要难度维度（怪物数、语言风格、指令顺序），较为全面。
  - 多次运行报告均值和标准差，结果统计可靠。
  - 与强基线（txt2π, Reader, Oracle MCTS）公平对比，且使用了相同的环境设置。
  - 消融实验设计合理，验证了各组件的贡献。
  - 不足：仅在一个基准（RTFM）上评估，缺乏其他领域（如机器人操作、导航）的泛化实验。此外，对比方法中未包含近期的基于LLM的方法（如LESR, Yan et al.），但论文将其归入不同范畴（语言辅助RL vs. 语言指令RL），有一定合理性。

## 6. 主要结论与发现

- **核心发现**：在指令密集型RL环境中，显式建模指令的阶段选择性（即动态选择当前最相关的指令片段）相比将整个指令集作为全局条件输入，能显著提升样本效率和任务成功率。
- **具体结果**：
  - 在单怪物设置中，HRLLI在4个配置上均取得最高或接近最高的胜率（如One NL Shuffle中0.79 vs. Reader 0.63）。
  - 在更具挑战的双怪物设置中，HRLLI全面超越所有基线（如Two SL中0.60 vs. Reader 0.50；Two NL中0.30 vs. Reader 0.24），尤其在自然语言和打乱指令场景下优势更大。
  - 消融实验证明：共享状态编码器接受高层和低层联合更新至关重要；余弦相似度辅助奖励比价值差辅助奖励更有效；奖励模型正确设计（输入st, at, st+1）对低层学习不可或缺。
  - 超参数分析显示：选择间隔T=10左右性能最佳；辅助奖励权重λsel需适中选择（论文取0.01）。

## 7. 优点

- **方法创新**：首次将指令集分解为可动态选择的片段，并提出分层选择-执行框架，将语言落地从全局条件化提升为自适应阶段指导，思路新颖且合理。
- **技术设计合理**：共享编码空间、分段奖励预测、余弦相似度辅助奖励等设计均具有理论动机和实证支持。
- **实验设计全面**：覆盖多个难度维度、多个基线、多种消融变体，并进行了超参敏感性分析，可信度高。
- **基线选择恰当**：包括经典的txt2π、模型基础的Reader以及上界Oracle MCTS，对比充分。
- **可解释性**：高层指令选择过程提供了决策可解释性（可追踪每个阶段所用的指令）。
- **代码开源**：论文未明确说明但通常此类工作会提供代码（附录中未提及，但可推断）。

## 8. 不足与局限

- **评估基准单一**：仅使用RTFM环境，缺乏在更广泛的语言指导RL任务（如机器人操控、室内导航）上的验证，结论的泛化性有待证明。
- **忽略近期方法**：未与基于LLM的指导方法（如LESR, Yan et al. 2025）进行对比，虽然论文将其归为“语言辅助RL”，但其实它们也能处理指令，直接比较可进一步体现层次选择的价值。
- **算力信息缺失**：未报告训练所需的GPU时间、型号等，不利于复现和资源评估。
- **参数敏感性**：T和λsel的选取依赖于经验调优，论文未展示跨不同环境的泛化鲁棒性（只在Two NL Shuffle上分析）。
- **假设限制**：假设指令可分解为独立的句子级片段，且数量有限（如≤64），但在真实场景中可能难以分解或指令数量极大，扩展性需验证。
- **奖励模型依赖**：奖励模型的学习质量直接影响高层和低层训练，若环境奖励非常稀疏且噪声大，模型可能不准确，带来偏差风险。
- **训练复杂度**：相比直接条件化策略，分层结构增加了训练开销和超参数调整难度。

（完）
