---
title: A Data-Free Symbolic Regression Approach for Solving Equations
title_zh: 一种无数据的符号回归方法用于求解方程
authors: "Sergei Garmaev, Vinay Sharma, Olga Fink"
date: 2026-06-05
pdf: "https://arxiv.org/pdf/2606.07152v1"
tags: ["query:sr"]
score: 10.0
evidence: 无数据符号回归用于方程求解
tldr: 许多科学方程无法解析求解，只能数值计算，无法得到显式符号表达式。现有符号回归方法需要大量输入-输出数据，但许多问题仅有方程本身。本文提出符号方程求解器（SES），将方程求解转化为可微符号模型的优化问题，直接从方程和初始/边界条件构造目标函数，无需任何成对数据。SES在代数方程、常微分方程和偏微分方程上成功恢复出紧凑的显式符号表达式，与解析解一致。该方法为无监督的符号方程求解开辟了新路径。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.07152v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1553, \"height\": 1287, \"label\": \"Figure\"}]"
motivation: 现有符号回归求解方程需输入-输出数据，但许多科学问题仅有方程，无法提供配对样本。
method: 提出SES，将方程求解建模为可微符号模型的优化，目标函数直接由方程和初边值条件构造。
result: 在代数方程、ODE和PDE上，SES恢复出与解析解匹配的紧凑显式符号表达式。
conclusion: SES无需训练数据即可从方程直接符号求解，为自动发现解析解提供了有效方法。
---

## 摘要
当今科学中出现的许多方程无法用现有的解析技术求解，因此只能通过数值方法求解，无法得到显式的符号表达式。现有的符号回归方法能够恢复符号表达式，但需要从底层过程中获取训练数据，而不仅仅是控制方程。我们提出了符号方程求解器（SES），这是一个将方程求解表述为对可微符号模型的优化问题的框架。SES 从方程以及初始条件或边界条件中构建其目标函数，从而消除了对配对输入-输出数据的需求。学习到的模型以显式符号形式表示，便于进一步分析。我们在典型的代数方程和微分方程上评估了 SES，包括代数方程组、含超越项的方程、常微分方程以及具有不同初始或边界条件的偏微分方程。在这些设置中，SES 恢复了与相应解析解匹配的紧凑符号表达式。

## Abstract
Many equations arising in science currently cannot be solved by available analytical techniques and are therefore solved numerically, without yielding explicit symbolic expressions. Existing symbolic regression approaches can recover symbolic expressions, but require training data obtained from the underlying process, rather than the governing equation alone. We propose the Symbolic Equation Solver (SES), a framework that formulates equation solving as an optimization problem over differentiable symbolic models. SES constructs its objective from the equation together with initial or boundary conditions, eliminating the need for paired input-output data. The learned model is expressed in explicit symbolic form, enabling further analysis. We evaluate SES on representative algebraic and differential equations, including a system of algebraic equations, an equation with transcendental terms, an ordinary differential equation, and partial differential equations with different initial or boundary conditions. Across these settings, SES recovers compact symbolic expressions that match the corresponding analytical solutions.

---

## 论文详细总结（自动生成）

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：科学和工程中的大量方程（如含超越项的方程、非线性微分方程）无法用现有解析方法获得闭式解，只能依靠数值方法得到近似数值解，但数值解缺乏显式符号表达式，难以揭示函数依赖、对称性等结构信息。
- **研究动机**：现有的符号回归方法（如遗传编程、EQL等）能够恢复显式符号表达式，但它们依赖于成对的输入‑输出数据（即需要已知函数值样本）；而许多方程求解问题中，我们仅有方程本身及初始/边界条件，没有监督数据。另一方面，基于残差的神经求解器（如PINNs）虽然能从方程直接学习，但输出的是隐含在神经网络参数中的数值近似，不是显式符号形式。
- **整体含义**：本文试图填补一个空白——在无需任何训练数据的前提下，直接从控制方程和辅助条件出发，通过优化可微符号模型来恢复显式符号解。这为将符号推理融入科学机器学习提供了新思路。

## 2. 论文提出的方法论

- **核心思想**：将方程求解形式化为在可微符号函数空间中的优化问题。候选解由一个可微符号网络（基于EQL架构）表示，网络的参数通过最小化由方程残差和初边值残差构成的目标函数进行学习。
- **关键技术细节**：
  - **符号网络架构**：采用EQL型网络，将常规激活函数替换为符号算子库，包括 `id, const, (.)^2, exp(.), tanh(.), ×`（每个算子每层实例化两次）。网络深度固定为2层（最大树深度为2）。
  - **目标函数构造**：将所有方程项移到左边形成残差 \(F_k\)，初边值条件形成残差 \(B_j\)。总损失为：
    \[
    L(\theta) = \frac{1}{N_F}\sum_{i=1}^{N_F}\sum_{k=1}^K F_k^2 + \frac{1}{N_B}\sum_{i=1}^{N_B}\sum_{j=1}^J B_j^2
    \]
    其中配点（collocation points）每轮独立采样256个点。
  - **导数计算**：通过自动微分（automatic differentiation）计算网络输出对输入的导数。
  - **优化过程（三阶段）**：
    1. 仅使用残差损失训练50,000 epochs；
    2. 加入L1惩罚并周期剪枝（删除对损失贡献最小的25%权重），训练200,000 epochs，直至权重数降至预设最小值（通常5个，泊松方程保留更多）；
    3. 移除L1惩罚，微调20,000 epochs。
  - **符号提取**：训练完成后，将学习到的权重（四舍五入至5位小数）反向传播为显式符号表达式。
- **关键公式**：目标函数式(4)直接体现了方程驱动，无需标签数据。对于非微分方程，使用辅助变量t作为输入；对于微分方程，输入为时空坐标。

## 3. 实验设计

- **使用的方程/场景**（共5组）：
  - 线性代数方程组：\(2x+3y=7, x-y=1\) → 解 \(x=2, y=1\)。
  - 超越方程：\(e^x + x^3 = e + 1\) → 解 \(x=1\)。
  - 非线性ODE：\(dy/dt = 1 - y^2, y(0)=0\) → 解 \(y=\tanh t\)。
  - 线性输运方程（PDE）：\(\partial u/\partial t + \partial u/\partial x = 0, u(x,0)=x^2\) → 解 \(u=(x-t)^2\)。
  - 二维泊松方程（两组混合边界条件）：
    - Case A: \(u_{xx}+u_{yy}=4+2x\)，边界条件对应解 \(u=x^2+y^2+xy^2\)。
    - Case B: 同上PDE，但边界条件不同，解为 \(u=2x^2+xy^2\)。
- **Benchmark**：本文未设置传统基准方法进行定量对比，仅将恢复的符号表达式与已知解析解比较。在讨论中概念性对比了EQL（需数据）、PINNs（返回数值解）和解析求解器。
- **对比方法**：无定量实验对比，仅对现有方法分类（解析法、数据驱动符号回归、残差神经求解器）进行了定性分析。

## 4. 资源与算力

- **文中未明确说明**：论文未提及使用的GPU型号、数量、训练总时长、显存占用等具体计算资源信息。仅提到每轮采样256个配点，总训练epoch数为270k（50k+200k+20k），但未报告实际训练时间或硬件配置。这一点是本文实验报告的一个不足。

## 5. 实验数量与充分性

- **实验数量**：共5个测试案例（代数系统1个、超越方程1个、ODE 1个、PDE 1个并含两组边界条件）。未做消融实验（如不同算子库、不同剪枝策略、不同深度等），也未与现有算法进行定量对比。
- **充分性分析**：
  - **优点**：案例覆盖了代数、超越、ODE、PDE等主要方程类型，展示了方法跨类泛化能力。
  - **不足**：所有解都较为简单（常数、二次函数、tanh等），符号复杂度低；未测试更复杂非线性PDE（如Navier-Stokes）、高阶导数、多变量耦合系统（实验中仅2变量线性系统）；未验证噪声或近似解情况；未进行统计重复实验（如随机初始权重下的成功率）。
  - **客观性/公平性**：由于没有对比方法，无法评估SES相对于现有方法（如PINNs、EQL）在恢复解精度、效率、可解释性上的优势。实验设计偏向展示可行性，而非全面性能比较。

## 6. 论文的主要结论与发现

- SES能够在无需任何监督数据的情况下，直接从方程和初边值条件恢复出与解析解匹配的紧凑显式符号表达式。
- 该方法统一处理代数方程和微分方程，包括含超越项的方程及偏微分方程。
- 恢复的符号表达式具有正确的函数结构（如平方形式、tanh形式），而不仅仅是数值拟合。
- 框架自然支持多变量系统（如代数方程组）的联合求解。
- 结论认为：方程求解可以被视为符号函数空间中的优化问题，这为从方程自动发现解析解提供了一条有效路径。

## 7. 优点

- **无监督学习**：完全不需要成对的输入‑输出数据，仅依赖方程和约束，大大降低了数据获取成本。
- **显式可解释性**：输出为显式符号公式（如 \(2.0x^2 + 1.0xy^2 + 1.0y^2\)），便于进一步理论分析、简化或嵌入其他物理模型。
- **统一框架**：能处理代数方程、ODE、PDE等多种类型，且可以通过增加辅助变量处理常数解。
- **梯度优化高效**：利用可微符号网络和自动微分，可使用标准优化器（Adam）进行端到端训练。
- **结构紧凑**：通过L1正则化和剪枝自动稀疏化，最终表达式往往非常简洁。

## 8. 不足与局限

- **实验规模有限**：仅测试了5个简单案例，未在高维、强非线性、混沌、多尺度等挑战性问题（如Kuramoto‑Sivashinsky、Navier‑Stokes）上验证。
- **符号表达能力受限**：算子库固定且深度仅2层，无法表示更复杂的函数（如三角函数、Bessel函数、积分算子）或更深层次的组合。
- **未与现有方法定量对比**：缺乏与PINNs在精度、收敛速度上的比较，也未与EQL在数据驱动场景下的效果对比，无法评估SES的实际优劣。
- **收敛保证缺失**：优化过程可能陷入局部最优，对初始权重、配点采样敏感，文中未报告多次运行的成功率或方差。
- **数值近似与精确解差距**：虽然恢复的表达式与解析解高度一致，但部分案例（如输运方程）系数存在微小偏差（如0.95184），严格来说不属于精确解。
- **应用限制**：实际科学问题中常涉及复杂边界形状、间断解、高振荡解等，本文方法目前仅适用于规则域（矩形）和光滑解。
- **资源说明缺失**：未提供训练硬件和时间信息，不利于他人复现和评估计算成本。

（完）
