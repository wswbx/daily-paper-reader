---
title: "AutoSpec: Safety Rule Evolution for LLM Agents via Inductive Logic Programming"
title_zh: AutoSpec：通过归纳逻辑编程实现LLM智能体安全规则演化
authors: "Pingchuan Ma, Zhaoyu Wang, Zimo Ji, Yuguang Zhou, Zhantong Xue, Zongjie Li, Shuai Wang, Xiaoqin Zhang"
date: 2026-06-23
pdf: "https://arxiv.org/pdf/2606.24245v2"
tags: ["query:cli-agent"]
score: 8.0
evidence: AutoSpec 为执行命令的LLM智能体演化安全规则
tldr: "针对LLM代理的手工安全规则误报/漏报高、神经网络缺乏可解释性的问题，本文提出AutoSpec框架，基于反例引导归纳综合和归纳逻辑编程自动演进规则。从专家规则和标注轨迹出发，通过ILP高效识别区分安全与不安全行为的谓词，剪枝规则编辑搜索空间。在代码执行和具身代理任务上，规则F1达0.98和0.93，假阳性减少94%，收敛于4-5次迭代，生成可读可审计且泛化性强的规则。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.24245v2/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 884, \"height\": 440, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.24245v2/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 629, \"height\": 475, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.24245v2/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 745, \"height\": 393, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.24245v2/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 867, \"height\": 365, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.24245v2/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 878, \"height\": 667, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.24245v2/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 661, \"height\": 368, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.24245v2/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 839, \"height\": 297, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.24245v2/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 558, \"height\": 289, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.24245v2/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 887, \"height\": 196, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.24245v2/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 810, \"height\": 566, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.24245v2/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 834, \"height\": 256, \"label\": \"Table\"}]"
motivation: 现有LLM代理安全规则要么手工设计导致误报/漏报平衡差，要么神经网络缺乏可解释性。
method: 通过CEGIS反复挖掘误报/漏反应例，利用ILP学习判别性谓词来剪枝规则编辑搜索空间。
result: "在两类任务上规则F1达0.98/0.93，假阳性减少94%，收敛于4-5次迭代，优于启发式CEGIS 4.8倍。"
conclusion: AutoSpec能自动演进可解释、高精度的安全规则，有效保障LLM代理安全且泛化性好。
---

## 摘要
大型语言模型（LLM）智能体通过将语言模型与外部工具和环境集成，日益自动化地完成复杂任务。然而，它们的自主性带来了显著的安全风险：智能体可能执行破坏性命令、泄露敏感数据或违反领域约束。现有安全方法面临一个基本权衡：手工规则可解释但脆弱，过于保守的规则会阻止安全操作（高误报率），而宽松的规则会遗漏不安全行为（高漏报率）。神经网络分类器缺乏安全关键部署所需的可解释性。我们提出AutoSpec框架，该框架通过反例引导的归纳综合（CEGIS）并由归纳逻辑编程（ILP）指导，从用户的安全/不安全标注中自动演化已部署的专家设计安全规则。从专家规则和带注释的轨迹流开始，AutoSpec迭代评估规则，挖掘误报和漏报的反例，使用ILP学习哪些谓词能够区分它们，生成候选规则编辑，并验证候选规则以选择最佳修订。关键见解在于，ILP能够高效识别那些在漏报中频繁出现但在误报中很少出现（或反之）的谓词，从而大幅剪枝规则编辑的指数级搜索空间。这一过程持续进行直至收敛，产生平衡精确率和召回率的可解释规则。我们在涵盖代码执行和具身智能体领域的291条执行轨迹上评估了AutoSpec。AutoSpec在两个领域将规则F1分数分别提升至0.98和0.93，实现了高达94%的误报减少，同时保持高召回率，并在4-5次迭代内收敛。与启发式CEGIS相比，ILP引导的方法使F1分数提高高达4.8倍。学习到的规则具有人类可读性、可审计性，并能泛化到未见场景。

## Abstract
Large language model (LLM) agents increasingly automate complex tasks by integrating language models with external tools and environments. However, their autonomy poses significant safety risks: agents may execute destructive commands, leak sensitive data, or violate domain constraints. Existing safety approaches face a fundamental tradeoff: hand-crafted rules are interpretable but brittle, with overly conservative rules blocking safe operations (high false positives) while permissive rules miss unsafe behaviors (high false negatives). Neural classifiers lack the interpretability required for safety-critical deployments.   We present AutoSpec, a framework that automatically evolves deployed expert-designed safety rules from user safe/unsafe annotations through counterexample-guided inductive synthesis (CEGIS) guided by inductive logic programming (ILP). Starting from the expert rules and a stream of annotated traces, AutoSpec iteratively evaluates rules, mines false-positive and false-negative counterexamples, uses ILP to learn which predicates discriminate them, generates candidate rule edits, and verifies candidates to select the best revision. The key insight is that ILP efficiently identifies predicates that appear frequently in false negatives but rarely in false positives (or vice versa), dramatically pruning the exponential search space of rule edits. This continues until convergence, producing interpretable rules that balance precision and recall.   We evaluate AutoSpec on 291 execution traces spanning code execution and embodied agent domains. AutoSpec raises rule F1 to 0.98 and 0.93 across the two domains, achieving up to 94% false positive reduction while maintaining high recall, and converges within 4-5 iterations. The ILP-guided approach achieves up to 4.8x higher F1 than heuristic CEGIS. The learned rules are human-readable, auditable, and generalize to unseen scenarios.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 核心问题与整体含义（研究动机和背景）

- **核心问题**：大型语言模型（LLM）智能体在集成外部工具、自动执行复杂任务时面临严峻安全风险，可能执行破坏命令、泄露敏感数据或违反领域约束。现有安全方法存在根本性权衡：
  - 手工规则（如 AgentSpec）可解释但脆弱，容易产生高误报（保守规则）或高漏报（宽松规则），且规则静态不变，无法适应模型更新、工具集成、提示修订等环境变化。
  - 神经网络分类器（如 Llama Guard）适应性更强，但缺乏可解释性，不适合安全关键部署，且无法直接利用累积的安全/不安全标注来更新部署的专家规则。
- **整体含义**：需要一种能够利用智能体运行中不断产生的用户标注（安全/不安全）来自动演化专家设计的安全规则的方法，在保持可解释性的同时实现自适应优化。

## 2. 方法论：核心思想、关键技术细节、算法流程

- **核心思想**：将安全规则维护问题建模为**反例引导的归纳综合（CEGIS）**问题。用户标注充当反例：漏报（该拦截未拦截）和误报（误拦截）都是规则与真实标签不一致的实例。通过**归纳逻辑编程（ILP）**高效识别区分安全/不安全行为的谓词，剪枝规则编辑的指数级搜索空间。
- **关键技术细节**：
  - **谓词库（Predicate Library）**：每个领域预定义一组布尔谓词（如`destructive_os_inst`, `near_danger_zone`），用于描述事件属性；安全规则由谓词组合构成。
  - **规则定义**：每条规则是一个逻辑公式，在单个事件上评估；若规则在任意事件上“触发”，则整条轨迹被判定为不安全。
  - **编辑操作**：四种基本操作（表2）：`AddConjunct`（添加合取）、`AddException`（添加例外，即取反）、`AddDisjunct`（添加析取分支，在规则集层面）、`Relax`（删除文字以放宽）。
  - **ILP引导谓词学习**：给定当前规则产生的误报和漏报，用ILASP求解器学习最简假设（一组Horn子句的析取），该假设应覆盖所有漏报而不覆盖任何误报。输出的谓词即为区分性谓词，并附带编辑方向（如漏报→用`AddDisjunct`添加；误报→用`AddConjunct`或`AddException`收紧）。
  - **候选生成与选择**：从ILP建议出发，生成三种候选：单步编辑、混合修复链（同时处理精调和回调）、失败驱动分支（高精度析取分支）。用评分`𝑆=(F1, Precision, −Unresolved, −|R|)`按字典序选择最佳候选。
  - **多轮收敛**：引入软新颖性压力，避免重复使用已有谓词；当分数无法提升或达到迭代预算时终止。
- **算法流程（Alg.1）**：
  1. 从初始规则集`R0`和带标注轨迹数据`DL`开始。
  2. 对当前规则集`Ri`：评估→挖掘误报/漏报作为反例`CE`。
  3. 若`CE`为空或分数达标则返回。
  4. 使用ILP从`CE`学习区分性谓词建议。
  5. 根据建议生成候选编辑规则集。
  6. 在`DL`上评估所有候选，选择最优者`best`。
  7. 若`best`分数优于当前规则，则更新`R←best`；否则终止。
  8. 循环直至最大迭代次数`K`或目标分数`θ`达成。

## 3. 实验设计

- **数据集/场景**：
  - **代码执行域**：191条轨迹（91条不安全来自RedCode-Exec，覆盖25个风险类别；100条安全由GPT-5.1-Codex生成）。轨迹长度1-9事件（平均1.4）。
  - **具身智能体域**：100条轨迹（52%不安全），来自SafeAgentBench（AI2-THOR模拟家庭机器人），使用GPT-5.1-Codex + zero-shot ReAct提示。轨迹长度1-15事件（平均5.1）。
- **Benchmark**：无特定公开benchmark；对比基准包括：
  - B1：专家设计规则（AgentSpec）——既是起点也是专家参考。
  - B2：LLM分类器（5-shot提示分类）。
  - B3：CEGIS without ILP（启发式谓词排序）。
  - B4：随机搜索（固定评估预算内随机采样）。
- **评估指标**：精确率（Precision）、召回率（Recall）、F1分数，以及收敛迭代次数、规则复杂度、候选效率。

## 4. 资源与算力

- 论文**未明确说明使用了专门的GPU或分布式训练资源**。实验在一台MacBook Pro（M2 Max，32GB RAM）上进行。
- ILP求解器ILASP v4.4.1设置120秒超时。AutoSpec运行时间：代码域20.1秒（ILP版）vs 4.4秒（启发式版）；具身域11.8秒 vs 4.3秒。属于轻量级计算。

## 5. 实验数量与充分性

- **主要实验**：表3（三个领域对比基线）、表4（ILP vs 启发式对比）、表5（消融实验，去除ILP/多步编辑/失败驱动分支）。
- **收敛曲线**：图4展示两个领域F1/Precision/Recall随迭代变化。
- **泛化实验**：图5，70%初始任务训练，30%新任务测试。
- **用户研究**：10位从业者对AutoSpec规则与LLM分类器在可解释性、可操作性、信任度上评分（图6），共120次两两比较。
- **充分性评价**：实验覆盖两个不同特性的领域（短轨迹代码执行 vs 长轨迹具身代理），对比了四种有代表性的基线（专家规则、神经网络、无ILP的CEGIS、随机搜索），做了消融分析验证每个组件贡献，并进行了泛化测试和用户研究。实验设计较为全面，但领域数量有限（只两个），且未在实际生产环境部署测试。

## 6. 论文的主要结论与发现

- 在两个领域上，AutoSpec的F1分别达到0.98（代码执行）和0.93（具身代理），相比专家规则基线提升40.4%和89.2%。
- ILP引导相比启发式CEGIS提高F1高达4.8倍（代码域从0.203到0.980），且收敛速度快（4-5次迭代）。
- 学习到的规则能泛化到未见任务（泛化缺口：代码1.6%，具身4.7%）。
- 用户研究显示AutoSpec规则在可解释性（4.97 vs 3.45）、可操作性（4.83 vs 2.73）、信任度（4.80 vs 2.77）上显著优于LLM分类器（p<0.01）。
- 消融表明ILP是最关键组件（移除后F1降至0.20），多步编辑和失败驱动分支对效率和最终性能也有重要贡献。

## 7. 优点

- **概念创新**：将安全规则维护正式化为反例引导的归纳综合问题，巧妙利用运行中累积的标注作为反例。
- **技术亮点**：利用ILP高效学习判别性谓词，大幅剪枝规则编辑搜索空间，同时保持输出可解释。
- **实用性强**：无需从头编写规则，而是演化已有专家规则；可异步运行，不影响智能体在线执行。
- **全面评估**：跨领域、多基线、消融实验、泛化测试、用户研究，证据充分。
- **可解释性优势突出**：输出为透明、可审计的符号规则，从业者能理解、修改并信任。

## 8. 不足与局限

- **领域覆盖有限**：仅测试代码执行和具身代理两个领域，未涉及Web代理、数据库代理等其他重要场景。论文自承此为威胁之一。
- **谓词库依赖专家设计**：谓词库需要预先构建，若库不完整或缺乏关键谓词，则规则演化能力受限。论文指出未解决的反例可揭示新谓词需求，但未实现自动谓词发现。
- **不保证全局最优**：底层综合问题为NP-hard，AutoSpec采用贪心策略，只保证局部最优和单调改善。
- **时间状态处理有限**：当前只评估单个事件和局部上下文，未原生支持时序属性（如LTL/MTL）。论文承认此为可扩展方向，但未实现。
- **标注依赖**：需要用户提供轨迹级安全/不安全标注，标注质量直接影响演化效果。论文未讨论标注噪声或不一致问题。
- **收敛性分析**：提供了单调改善和有限终止保障，但未给出理论收敛速度，实际表现依赖问题规模。
- **资源与算力细节不充分**：未报告模型训练或推理的具体GPU配置，仅提及在笔记本上运行，可能影响可复现性。

（完）
