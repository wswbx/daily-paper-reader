---
title: "Sample Complexity of Scientific Discovery: PAC Learnability of Compositional Function Trees"
title_zh: 科学发现的样本复杂度：组合函数树的PAC可学习性
authors: "Şuayp Talha Kocabay, Talha Rüzgar Akkuş, Kerem Yalçın"
date: 2026-06-28
pdf: "https://arxiv.org/pdf/2606.29331v1"
tags: ["query:sr"]
score: 9.0
evidence: 直接研究符号回归的样本复杂度
tldr: 符号回归被视为统计难处理，因为假设空间随深度指数增长。本文在PAC学习框架下分析组合函数树，证明其Rademacher复杂度受深度d和基算子Lipschitz常数L控制，而非符号结构数量。导出的风险界为O(L^d/√n)，避免了指数爆炸。实验表明泛化差距与复杂度项正相关，为科学发现提供了统计可学习性理论支持。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.29331v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 757, \"height\": 558, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.29331v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 758, \"height\": 553, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.29331v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 756, \"height\": 551, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.29331v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 745, \"height\": 400, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.29331v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 756, \"height\": 550, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.29331v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 756, \"height\": 441, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.29331v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 911, \"height\": 459, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.29331v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1783, \"height\": 1076, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.29331v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1053, \"height\": 428, \"label\": \"Table\"}]"
motivation: 科学发现的符号回归常因组合爆炸被认为统计上不可行，缺乏严格的理论保证。
method: 使用PAC学习与Rademacher复杂度分析，结合Maurer向量收缩和有限并集界，推导风险上界。
result: 风险界为O(L^d/√n)，其中d为深度，L为算子Lipschitz常数，复杂度不随结构数量指数增长。
conclusion: 理论证明组合函数树可高效学习，实验验证了复杂度项对泛化差距的预测力。
---

## 摘要
通过符号回归进行科学发现通常被视为统计和计算上难以处理的，因为表达式的假设空间随着深度组合增长。本文通过PAC学习的视角重新审视统计方面，关注由有限光滑算子（例如$\{+,\times,\sin,\exp\}$和仿射映射）词汇表构建的组合函数树。我们证明相关泛化量——Rademacher复杂度，因此超额风险，不一定随不同符号结构的数量指数爆炸，而是由(i)深度$d$和(ii)沿组合计算图的基本算子的Lipschitz常数控制。具体地，在算子的温和Lipschitz条件和有界仿射叶节点下，词汇表大小$K=|\mathcal{H}_{\mathrm{base}}|$上的有限并界与Maurer型向量收缩结合得到$\mathfrak{R}_n(\mathcal{H}_{\mathrm{comp}}^{d}) \leq (Kb\sqrt{2}L)^{d-1}\mathfrak{R}_n(\mathcal{H}_{\mathrm{comp}}^{1})$，元数界为$b$；相应的高概率风险界当$K,b=O(1)$且$\mathfrak{R}_n(\mathcal{H}_{\mathrm{comp}}^{1})=O(n^{-1/2})$时按$\mathcal{O}(L^{d}/\sqrt{n})$缩放。我们用一个模块化代码库补充理论，该代码库在受控深度的合成“类物理”目标上训练可微算子树（非MLP），并显示经验泛化差距与预测复杂度项$(\widehat{L}^{d})/\sqrt{n}$正相关。

## Abstract
Scientific discovery via symbolic regression is often viewed as statistically and computationally intractable because the hypothesis space of expressions grows combinatorially with depth. This paper revisits the statistical side through the lens of PAC learning, focusing on compositional function trees built from a finite vocabulary of smooth operators (e.g., $\{+,\times,\sin,\exp\}$ and affine maps). We prove that the relevant generalization quantity, Rademacher complexity, hence the excess risk, does not necessarily blow up exponentially with the number of distinct symbolic structures, but is controlled by (i) the depth $d$ and (ii) the Lipschitz constants of the base operators along the composed computation graph. Concretely, under mild Lipschitz conditions on operators and bounded affine leaves, a finite-union bound over a vocabulary of size $K=|\mathcal{H}_{\mathrm{base}}|$ together with Maurer-type vector contraction yields $\mathfrak{R}_n(\mathcal{H}_{\mathrm{comp}}^{d}) \leq (Kb\sqrt{2}L)^{d-1}\mathfrak{R}_n(\mathcal{H}_{\mathrm{comp}}^{1})$ with arity bound $b$; corresponding high-probability risk bounds scale as $\mathcal{O}(L^{d}/\sqrt{n})$ when $K,b=O(1)$ and $\mathfrak{R}_n(\mathcal{H}_{\mathrm{comp}}^{1})=O(n^{-1/2})$. We complement the theory with a modular codebase that trains differentiable operator trees (not MLPs) on synthetic "physics-like" targets of controlled depth and shows that the empirical generalization gap correlates positively with the predicted complexity term $(\widehat{L}^{d})/\sqrt{n}$.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）

符号回归（Symbolic Regression, SR）在科学发现中常用于寻找可解释的数学模型，但常被视为统计上难以处理的问题，因为候选表达式数量随深度指数增长，且寻优是NP难的。已有观点将计算困难性与统计可学习性混为一谈。本文通过PAC学习理论将二者分离，专门分析组合函数树（Compositional Function Trees）的统计泛化行为。核心贡献在于：证明深度为d的组合函数树的泛化误差受**(i) 深度d**和**(ii) 沿计算图积累的算子Lipschitz常数L**控制，而非受符号结构数量的指数增长控制。这为科学发现从**小样本中统计可行**提供了理论基础，澄清了统计瓶颈并非不可逾越。

### 2. 方法论：核心思想、关键技术细节与公式

- **假设空间定义**：  
  从有限基算子词汇表 \(\mathcal{H}_{\mathrm{base}} = \{\text{add}, \text{mult}, \sin, \cos, \exp, \text{affine}\}\) 出发，递归构建深度d的假设空间 \(\mathcal{H}_{\mathrm{comp}}^{d}\)，每个内部节点为基算子，叶节点为输入或仿射映射（参数有界）。元数b ≤ 2（本文主要考虑二叉树，但可推广到b元树）。
- **关键技术**：
  1. **Rademacher复杂度**：用于度量函数类的均匀收敛速度。
  2. **Maurer向量收缩不等式**（Lemma 4.4）：对Lipschitz向量值函数，可将Rademacher复杂度上界缩小到\(\sqrt{2}L\)乘子。
  3. **有限并界**（Lemma 4.6）：词汇表大小K = |\(\mathcal{H}_{\mathrm{base}}\)|，每一深度递归时复杂度最多乘以K（而非乘以所有不同树形状的数量）。
  4. **乘积类复杂度不乘性**（Lemma 4.5）：双子树输出类的复杂度为两者之和，而非乘积。
- **核心公式**（Theorem 4.7 / Corollary 4.8）：
  \[
  \mathfrak{R}_n(\mathcal{H}_{\mathrm{comp}}^{d}) \leq (K b \sqrt{2}L)^{d-1} \mathfrak{R}_n(\mathcal{H}_{\mathrm{comp}}^{1}),
  \]
  其中 \(b\) 为最大元数（二叉树b=2），\(L = \max_{h\in\mathcal{H}_{\mathrm{base}}} L_h\) 为算子全局/局部Lipschitz常数。进一步，在字典大小和元数视为常数时，高概率风险界为：
  \[
  R(f) \leq \widehat{R}_S(f) + \mathcal{O}\!\left(\frac{B L^{d}}{\sqrt{n}}\right) + B^{2}\sqrt{\frac{\log(1/\delta)}{2n}}.
  \]
  这表明样本复杂度按 \(O(L^{2d}/\epsilon^2)\) 缩放，而非指数于树形状数量。
- **实验方法论**：使用可微算子树（非MLP），通过梯度范数在batch上估计局部Lipschitz代理 \(\widehat{L}\)，并可视化泛化差距与 \((\widehat{L}^{d})/\sqrt{n}\) 的关系。

### 3. 实验设计：数据集、场景、基准对比

- **数据集**：合成数据，输入 \(x \sim \text{Unif}([-1,1])\)（一维），输出为真实函数加噪声（可实现设定，即真实函数属于假设类）。深度d ∈ {1,2,3,4}，对应具体表达式（如d=2: sin(wx+b0)+b1；d=4: exp(-(ax+b0))·sin(wx+b1)+(cx+d)）。
- **模型**：可微计算树，节点为 \(\text{add},\text{mult},\sin,\cos,\exp,\text{affine}\)，参数通过Adam优化。
- **基准对比**：本文主要验证理论预测（泛化差距与 \((\widehat{L}^{d})/\sqrt{n}\) 的一致性），并未与已有符号回归方法（如GP、PySR、AI Feynman）做直接性能比较。属于**理论导向的验证实验**，而非应用导向的性能评测。
- **训练与评估**：对每个 (d, n) 组合进行训练，n ∈ {50,100,500,1000,5000}，测试集大小10^5。使用20个随机种子，总共400个独立训练模型。

### 4. 资源与算力

文中**未明确说明**使用的GPU型号、数量或训练总时长。仅提及优化使用Adam、学习率10^{-2}、batch size 256、最多5000 epochs、早停机制。考虑到实验规模（400次训练，样本量最大5000，一维输入），可以推断算力需求不大，可能在单GPU或CPU即可完成，但论文未报告具体硬件。

### 5. 实验数量与充分性

- **实验数量**：4种深度 × 5种样本量 × 20随机种子 = 400次训练。对每个setting报告了中位数、均值、标准差、IQR等统计量。绘制了图1（全能图）、图2（深度分层）、图3（分解图）、图4（热图和箱线图）。计算了log-log Pearson相关系数0.48以及幂律拟合（α≈0.20, R²≈0.23）。
- **充分性与客观性**：
  - **优点**：覆盖了多个深度和样本量，重复种子充分，可视化多样且直观，提供了定量拟合指标。
  - **不足**：  
    - 所有实验均在**可实现假设**下进行（真实函数在假设类中），无法反映真实科学发现中模型误设（agnostic）场景。  
    - 输入只有一维均匀分布，缺乏多维、非均匀、含有噪声异质性或分布偏移的实验。  
    - 未与任何现有符号回归方法（如PySR、Eureqa）或基于正则化的方法（如SINDy）进行对比，因此无法评估理论界在实际系统中的紧致性或实用性。  
    - Lipschitz常数估计 \(\widehat{L}\) 基于batch梯度范数，并非全局最坏情况常数，可能低估真实复杂度。  
    - 未进行统计显著性检验（如置信区间覆盖概率检验）。  
    - 未讨论不同优化随机性带来的方差是否完全被种子数覆盖。

总体而言，实验在**验证理论定性预测**方面设计合理且充分，但**不能直接推广到真实科学发现任务**，也未能严格证明上界的紧致性。

### 6. 主要结论与发现

- **理论结论**：组合函数树的Rademacher复杂度被 \((K b \sqrt{2}L)^{d-1} \mathfrak{R}_n(\mathcal{H}_{\mathrm{comp}}^{1})\) 所控制，从而风险界以 \(\mathcal{O}(L^{d}/\sqrt{n})\) 衰减，**不依赖于符号结构数量的指数增长**。这表明在算子稳定且深度适中的情况下，从有限样本中学习科学规律在统计上是可行的。
- **实验发现**：泛化差距（test MSE - train MSE）与预测复杂度项 \((\widehat{L}^{d})/\sqrt{n}\) 呈正相关（Pearson r=0.48），且log-log拟合斜率约0.20，说明该复杂度项捕获了相当一部分方差。定性趋势：深度增加或样本减少时差距增大，与理论一致。
- **启发性**：论文将SR的统计困难与计算困难分离，为后续设计更高效的搜索算法（如结合PAC-Bayes先验或贝叶斯优化）提供了理论基础。

### 7. 优点

- **理论创新**：首次在PAC学习框架下系统分析符号回归中组合函数树的样本复杂度，给出显式的深度相关上界，打破了“组合爆炸=统计不可行”的直觉。
- **技术精巧**：综合运用有限并界、Maurer向量收缩、乘积类解耦等工具，将复杂度控制转化为对Lipschitz乘积和词汇表大小的依赖，方法具有推广性（可扩展到任意bary树和更多算子）。
- **实验设计简洁有力**：在可控可实现设定下直接验证了理论预测的方向性，避免了外变量干扰。可视化丰富（图1-4），并且提供了定量关联统计量（相关系数、幂律拟合），增强了说服力。
- **代码开源**：提供PyTorch实现和合成数据生成器，有助于复现和后续研究。

### 8. 不足与局限

- **实验覆盖狭窄**：仅在一维均匀分布、可实现设定中测试，未涉及多维、非可实现、含噪声异质性、真实科学数据等更复杂场景。局限性在论文中也有讨论。
- **Lipschitz估计偏差**：使用梯度范数 \(\widehat{L}\) 作为代理，未采用区间算术或最坏情况分析，可能低估真实复杂度；文中也明确指出该估计是数据相关的局部代理而非全局常数。
- **未与现有算法对比**：无法评估其理论界在实际符号回归系统中的实用价值（如相对于PySR、AI Feynman的样本效率）。
- **常数未显式跟踪**：理论界中的常数（如4B、\(\sqrt{2}\)因子、\(K^{d-1}\)等）在实验中均被吸收，因此只能验证**缩放趋势**而非紧致性。
- **没有考虑计算开销**：虽然区分了统计与计算，但未给出任何关于搜索算法的复杂度分析或计算资源需求。
- **缺乏消融实验**：未单独测试不同算子（如sin vs exp）、不同元数、不同参数界限等对复杂度项的影响，使得对Lipschitz乘积贡献的验证间接。

（完）
