---
title: "TabRank: Chain-of-Thought Distillation for Table Re-Rankers"
title_zh: "TabRank: 用于表格重排序器的思维链蒸馏"
authors: "Adarsh Singh, Kushal Raj Bhandari, Jianxi Gao, Soham Dan, Vivek Gupta"
date: 2026-07-28
pdf: "https://arxiv.org/pdf/2607.25182v1"
tags: ["query:tabular-fm"]
score: 8.0
evidence: 表格重排序与思维链蒸馏，与表格基础模型相关
tldr: "表格检索的多阶段系统依赖重排序器提升候选列表质量。本文提出TabRank框架，通过从大型推理模型蒸馏链式思维推理轨迹，训练紧凑的表格重排序模型。在Natural Questions Tables上构建了6728条推理轨迹数据集，并探索显式CoT蒸馏与提示条件化两种变体。在多个域外泛化测试中，Acc@10提升显著：HybridQA 30.5%、SQA 15.2%、TabFact 52.9%、TATQA 13.1%，并有效推广到多表推理场景。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.25182v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1655, \"height\": 566, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.25182v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1627, \"height\": 718, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.25182v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1663, \"height\": 1369, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.25182v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 785, \"height\": 821, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.25182v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1380, \"height\": 1823, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.25182v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 542, \"height\": 263, \"label\": \"Table\"}]"
motivation: 现有表格重排序方法缺乏显式推理能力，尤其是在多表复杂查询场景下表现不佳。
method: 收集推理轨迹数据集，通过蒸馏大型推理模型的CoT推理到紧凑学生模型，包括显式CoT蒸馏和提示条件化两种训练方式。
result: "在多项表格检索基准上Acc@10提升最高达52.9%，并展现跨域多表推理的泛化能力。"
conclusion: TabRank验证了推理蒸馏对表格重排序的有效性，为结构化信息检索提供了新思路。
---

## 摘要
检索相关表格以回答问题的能力是结构化信息检索的关键任务。多阶段检索系统严重依赖重排序器来优化由高效的第一阶段检索器生成的候选列表。因此，与传统的稀疏或稠密检索模型相比，神经重排序器和基于大语言模型的重排序方法因其卓越的语义理解和推理能力而变得越来越重要。最近，配备显式思维链推理的大型推理模型在非结构化段落检索中显示出排序质量的显著提升。在这项工作中，我们提出了TabRank，一个用于训练表格检索推理重排序器的框架。我们首先在Natural Questions Tables数据集上构建了一个包含6728条表格重排序推理轨迹的综合数据集。然后，我们探索了在这些推理轨迹上训练紧凑推理模型的两种变体：显式思维链蒸馏，以及在提示中使学生重排序器以教师的推理轨迹为条件。我们在多个领域和多表场景的分布外泛化设置中对TabRank进行了压力测试。我们的方法在各种表格检索数据集上显著提升了性能，与基础模型相比，在Multi-Table QA基准测试的HybridQA子集上Acc@10提高了30.5%，在SQA上提高了15.2%，在TabFact上提高了52.9%，在TATQA上提高了13.1%。值得注意的是，TabRank有效地泛化到多表推理。我们的代码、数据和模型可在https://github.com/AdarshSingh7647/TabRanker获取。

## Abstract
The ability to retrieve relevant tables for answering questions is a key task for structured information retrieval. Multi-stage retrieval systems rely heavily on rerankers to refine candidate lists produced by efficient first-stage retrievers. As a result, neural rerankers and LLM-based reranking methods have become increasingly important due to their superior capacity for semantic understanding and reasoning compared to conventional sparse or dense retrieval models. Recently, Large Reasoning Models (LRMs) equipped with explicit chain-of-thought (CoT) reasoning have shown strong improvements in ranking quality in unstructured passage retrieval. In this work, we present TabRank, a framework for training reasoning rerankers for Tabular Retrieval. We first present a comprehensive dataset of 6728 reasoning traces for tabular reranking on the Natural Questions Tables dataset. We then explore two variants of training a compact reasoning model on these reasoning traces: explicit CoT distillation and conditioning the student reranker on the teacher's reasoning trace within the prompt. We stress-test TabRank on several out-of-distribution generalization settings on diverse domains and multi-table scenarios. Our approach significantly improves performance across a variety of table retrieval datasets, increasing Acc@10 by 30.5% on HybridQA, 15.2% on SQA, 52.9% on TabFact, and 13.1% on TATQA subsets of the Multi-Table QA Benchmark compared to the base model. Notably, TabRank generalizes effectively to multi-table reasoning. Our code, data and models are available at https://github.com/AdarshSingh7647/TabRanker

---

## 论文详细总结（自动生成）

## 详细中文总结

### 1. 论文的核心问题与整体含义（研究动机和背景）
-   **核心问题**：在多阶段表格检索系统中，重排序器是关键瓶颈，但现有重排序研究主要针对非结构化文本，缺乏针对结构化表格的、能利用显式推理能力的有效方法。特别是，直接将大型推理模型的思维链蒸馏到重排序模型会带来过拟合和跨域泛化能力下降的风险。
-   **背景与动机**：表格检索比段落检索更复杂，其语义不仅体现在词面上，更体现在列模式、行对齐和单元格间关系等结构属性中。近年来的大型推理模型通过思维链推理展现了卓越能力，其链式推理可应用于重排序。但现有工作发现，让模型生成长篇推理轨迹（CoTGen）会导致过拟合和域外泛化能力下降，且推理输出易产生极化分数。因此，本文旨在探索一种既能利用推理优势，又能避免上述问题的表格重排序蒸馏策略。

### 2. 论文提出的方法论
-   **核心思想**：条件推理蒸馏。不同于让学生模型生成推理轨迹，TabRank 将教师模型（DeepSeek-R1）生成的推理轨迹作为**输入上下文**，仅监督其输出最终的排序结果。这样既利用了推理信号，又避免了模仿教师思维轨迹带来的过拟合。
-   **关键技术细节**：
    -   **数据生成**：使用教师模型 DeepSeek-R1 生成推理轨迹。构建包含查询和候选表格的提示，让教师模型在`<think>`标签内进行推理，然后输出最终排序（JSON格式）。最终得到包含6728个训练实例的过滤后数据集。
    -   **训练范式**：对比三种蒸馏策略：
        -   **Naive SFT (基线)**：仅用最终排序结果进行监督，不包含任何推理轨迹。
        -   **CoTGen (显式CoT蒸馏)**：将推理轨迹和最终排序结果作为完整目标序列，联合计算自回归损失。
        -   **CoTCond (本文提出的条件蒸馏)**：将推理轨迹拼接到输入提示中，但损失函数仅计算在最终排序输出上。模型被训练为在给定推理上下文的情况下预测排序，而不是生成推理。
-   **公式/算法流程**：
    -   Naive SFT损失：`L_rank = -Σ log p(排序|查询, 候选表)`
    -   CoTGen损失：`L_CoTGen = L_reason + L_rank` （对推理和排序都计算损失）
    -   CoTCond损失：`L_CoTCond = -Σ log p(排序|查询, 候选表, 推理轨迹)` （推理轨迹用于条件，不参与损失计算）

### 3. 实验设计
-   **数据集与场景**：
    -   **训练集**：Natural Questions Tables。
    -   **评估集（域外泛化）**：Multi-Table QA Benchmark中的4个子集，包括：
        -   HybridQA (Wikipedia来源)
        -   SQA (Wikipedia来源)
        -   TabFact (Wikipedia来源)
        -   TAT-QA (金融年报来源，与Wikipedia领域不同)
-   **基准 (Benchmark)**：使用`Recall@K`、`nDCG@K`和`Accuracy@K`等标准检索指标，在K=5和K=10上进行评估。所有重排序器对第一阶段检索器返回的前25个候选表进行重新排序。
-   **对比方法**：
    -   **Base Model**：在NQ-Tables上微调的Qwen3-8B，但不使用任何推理数据。
    -   **Naive SFT**：基于Base Model，仅使用教师模型的最终排序输出进行微调。
    -   **CoTGen**：基于Base Model，使用教师模型的推理和排序输出进行标准CoT蒸馏微调。
    -   **CoTCond (TabRank核心方法)**：本文提出的条件推理蒸馏方法。

### 4. 资源与算力
-   **模型**：所有学生模型均基于 `Qwen3-8B` 初始化。
-   **训练配置**：使用 `LLaMA-Factory` 框架，采用 `LoRA` 适配器进行微调。
-   **计算资源**：在 `2张 NVIDIA H200 GPU` 上训练，使用了 `约18小时`。有效批次大小为64，学习率为5×10⁻⁵。

### 5. 实验数量与充分性
-   **实验数量**：论文报告了在4个不同数据集上，针对4种方法（Base, Naive SFT, CoTGen, CoTCond）的 `Recall@5/10`、`nDCG@5/10` 和 `Acc@10`，共多个维度的实验结果。此外，还进行了错误分析，将预测结果分为正确排序、解析失败、空输出和重复输出四类。
-   **充分性与公平性**：
    -   **充分性**：实验覆盖了多个跨域（Wikipedia与金融）和多表场景，且都采用域外测试，充分检验了方法的泛化能力。错误分析深入揭示了不同方法失败的根源，使结论更有说服力。
    -   **公平性**：所有模型均从同一个预训练检查点初始化，使用相同的LoRA和优化设置，确保了比较的公平性。对比了具有不同监督信号的方法（无推理、隐式推理、显式推理、条件推理），设计合理。
    -   **不足**：没有进行消融实验，例如测试不同教师模型（如GPT-4等）或不同数据量下的效果。

### 6. 论文的主要结论与发现
-   **条件蒸馏优于显式生成**：`CoTCond` 在所有4个数据集的所有指标上均优于 `CoTGen`。这表明将推理信号作为条件上下文而非生成目标，能提供更强的泛化能力和更好的排序效果。
-   **推理信号的实质性帮助**：`CoTGen` 和 `CoTCond`（特别是后者）都显著优于 `Naive SFT`，证明了教师提供的推理轨迹比单纯依赖排序标签蕴含了更丰富、更有效的信息。
-   **提升来自消除结构性错误**：错误分析表明，CoTCond 的大部分性能提升是通过消除解析失败、空输出和重复预测等结构性错误来实现的，而非单纯地微调排序顺序。推理蒸馏教会模型生成了更连贯的排序列表。
-   **泛化能力强且成本低**：CoTCond 在跨领域（如金融领域的TAT-QA）和跨场景（大模型评估）泛化能力强，同时推理产生的token数远少于 CoTGen，实现了更好的准确率与成本权衡。

### 7. 优点
-   **方法创新性**：提出**条件推理蒸馏**这一新颖且有效的范式，为解决CoT蒸馏中的过拟合和泛化问题提供了新思路。
-   **显著的性能提升**：在多个域外数据集上实现了显著的性能提升（例如TabFact上Acc@10提升52.9%），验证了其有效性。
-   **高效的推理过程**：`CoTCond` 的推理开销低于 `CoTGen`，同时性能却更强，实现了更好的性能-效率平衡。
-   **可靠性高**：`CoTCond` 几乎消除了所有结构性的生成失败（如重复预测、解析失败），生成的排序列表更可靠。
-   **开源贡献**：代码、数据和模型已开源，有助于推动该领域的研究。

### 8. 不足与局限
-   **数据与模型局限性**：训练数据仅来自单一教师模型（DeepSeek-R1），可能继承了其特定偏见或推理模式。训练仅限于文本形式的表格，未考虑多模态表格（如扫描文档、PDF、图像中的表格）。
-   **评估范围有限**：仅在英文QA和事实核查任务上进行了评估，未测试其在跨语言、半结构化企业数据或其他检索任务（如对话式检索、多轮检索）中的泛化能力。
-   **未与完整系统集成**：论文聚焦于重排序器本身，没有将其与下游阅读器或生成器集成，也未展示其对端到端任务（如问答）的最终影响。
-   **潜在偏差风险**：依赖合成推理轨迹，可能引入教师模型的偏差或事实错误，需要谨慎评估后在真实场景中部署。

（完）
