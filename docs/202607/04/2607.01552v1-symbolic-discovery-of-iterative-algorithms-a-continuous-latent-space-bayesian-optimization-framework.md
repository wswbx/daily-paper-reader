---
title: "Symbolic Discovery of Iterative Algorithms: A Continuous Latent Space Bayesian Optimization Framework"
title_zh: 迭代算法的符号发现：连续潜空间贝叶斯优化框架
authors: "Tongjia Liu, Ilias Mitrai"
date: 2026-07-02
pdf: "https://arxiv.org/pdf/2607.01552v1"
tags: ["query:sr"]
score: 9.0
evidence: 通过贝叶斯优化发现符号形式的算法，类似于符号回归
tldr: 针对迭代优化算法的自动发现，本文将其建模为离散优化问题，利用变分自编码器学习更新函数的连续隐空间表示，将离散搜索转化为连续搜索，再结合贝叶斯优化高效探索新算法。在两类案例中，该方法能以符号形式发现新更新函数，无需任何功能形式假设，且发现速度快于传统数学规划方法。该框架显著提升了算法发现的自动化程度和效率。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.01552v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1782, \"height\": 649, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.01552v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 908, \"height\": 545, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.01552v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1891, \"height\": 556, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.01552v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 913, \"height\": 544, \"label\": \"Figure\"}]"
motivation: 手动设计迭代优化算法耗时且依赖专家经验，急需自动化方法发现新算法。
method: 通过VAE学习离散更新函数的连续隐空间表示，再以贝叶斯优化在该空间中搜索优化算法。
result: 成功符号形式发现新更新函数，计算时间低于现有数学规划方法。
conclusion: 该框架高效且无需假设函数形式，拓展了自动算法发现的边界。
---

## 摘要
在本文中，我们考虑迭代优化算法的自动发现。我们将算法发现任务形式化为一个离散优化问题，并使用潜空间贝叶斯优化搜索新的更新函数。所提出的框架首先使用变分自编码器学习离散更新函数空间的连续表示，将算法发现任务从离散搜索问题转化为连续搜索问题。随后，利用该连续表示通过贝叶斯优化搜索新算法。应用于两个案例研究表明，所提出的方法能够在不对更新函数的函数形式做任何假设的情况下，以符号形式发现新的更新函数。此外，发现新更新函数所需的计算时间低于现有的基于数学规划的方法。

## Abstract
In this paper, we consider the automated discovery of iterative optimization algorithms. We formulate the algorithm discovery task as a discrete optimization problem and search for new update functions using latent space Bayesian Optimization. The proposed framework first learns a continuous representation of the discrete space of update functions using variational autoencoders, transforming the algorithm discovery task from a discrete to a continuous search problem. The continuous representation is subsequently used to search for new algorithms using Bayesian optimization. Application to two case studies shows that the proposed approach can discover new update functions in symbolic form without any assumptions on the functional form of the update function. Moreover, the computational time required to discover the new update functions is lower than existing mathematical programming-based approaches.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **研究动机**：迭代优化算法（如梯度下降）在化学工程等领域广泛应用，但其更新函数（update function）通常依靠领域知识手动设计，耗时且依赖专家经验。现有自动化发现方法面临可解释性（如黑箱神经网络）与可扩展性（如遗传编程计算效率低）之间的权衡。
- **核心问题**：如何在不预设更新函数形式的前提下，高效、可解释地自动发现新的迭代算法。
- **整体含义**：本文提出一种结合变分自编码器（VAE）与贝叶斯优化（BO）的框架，将离散的算法空间映射到连续潜空间，从而实现样本高效的符号级算法发现，显著降低计算成本。

## 2. 论文提出的方法论

- **核心思想**：将算法发现任务建模为离散优化问题，先通过VAE学习更新函数（用表达式树表示）的连续潜空间表示，再在该连续空间中利用贝叶斯优化搜索最优更新函数。
- **关键技术细节**：
  - **表达式树表示**：使用深度为 \(d\) 的完全二叉树表示更新函数 \(g\)，每个内部节点分配二元/一元运算符，叶子节点分配自变量、梯度或常量。通过二进制变量 \(y_{on}\) 和连续变量 \(c_n\) 精确参数化。
  - **VAE学习连续表示**：将二进制变量转化为整数编码 \(\hat{y}\)，输入编码器得到潜变量分布参数 \((\mu, \sigma^2)\)，通过重参数化采样得到潜变量 \(z\)，解码器重构整数表示。训练损失为重构损失 + β × KL散度。
  - **算法性能评估**：对给定候选更新函数，通过求解一个非线性优化问题（式9）确定其最佳常数参数 \(c\)，并计算性能指标（如最终迭代梯度范数）。若不可行则赋大常数。
  - **潜空间贝叶斯优化**：以高斯过程为代理模型，用对数期望改进（log Expected Improvement）作为采集函数，在潜空间 \(Z\) 中迭代搜索，每次将候选点解码为更新函数进行评估，更新代理模型。
- **算法流程（文字描述）**：
  1. 随机生成合法表达式树数据集，训练VAE。
  2. 用少量随机合法树初始化贝叶斯优化（评估性能）。
  3. 在潜空间中优化采集函数，得到候选点 \(z\)。
  4. 解码 \(z\) 为表达式树，检查合法性；若合法则求解常数优化问题得到性能值 \(m(z)\)；若不合法则赋大常数。
  5. 更新高斯过程模型，重复步骤3-4直至达到最大迭代次数。

## 3. 实验设计

- **数据集/场景**：
  - **案例1**：最小化两个非凸函数（式10）：\(F_1(x) = (x_1-2)^2 + 2x_2^2 - x_1x_2\) 和 \(F_2(x) = (x_1-2)^4 + 2(x_1-2x_2)^2\)。每个函数使用2个随机起始点（共4个评估实例）。
  - **案例2**：线性回归问题 \(\min 0.5\|Ax-b\|^2\)，生成两组随机数据 \((A_1,b_1)\) 和 \((A_2,b_2)\)，条件数分别为 \(\kappa_1=16.87, \kappa_2=7.88\)。每个问题2个起始点。
- **基准方法**：
  - 最速梯度下降（GD），其步长通过优化确定。
  - 数学规划方法：基于MINLP的确定性搜索（文献[6]），使用BARON和SCIP求解器。
- **对比指标**：最终迭代梯度范数（性能函数 \(\phi\)），计算时间（CPU秒数），以及能否发现新算法。
- **参数设置**：表达式树深度 \(d=2\)（7个节点），算子集 \(B=\{+,-,*\}, L=\{x_k, \partial_k f, cst\}\)。VAE潜空间维度设为7（经敏感性分析确定）。BO迭代次数：案例1初始10个随机树+21次迭代（共31次评估），案例2初始20个随机树+25次迭代（共45次评估）。

## 4. 资源与算力

- 文中未明确提及使用的GPU型号或数量。
- 所有实验在CPU上完成：VAE训练耗时约 **1.66秒**；案例1总计算时间 **121秒**（评估112秒+搜索9秒）；案例2总计算时间 **176秒**（评估157秒+搜索18秒）。
- 数学规划方法（MINLP）在3600秒内未达到本文方法的性能。
- **说明**：本文方法主要依赖CPU计算（表达式树评估需要调用SCIP求解非线性规划），未涉及大规模并行GPU训练，表明方法在普通计算资源上即可运行。

## 5. 实验数量与充分性

- **实验数量**：
  - 两个独立案例，每个案例包含2个目标函数和2个起始点（仅4个评估实例），但BO实验本身是多次迭代（约30-45次函数评估）。
  - VAE训练使用了4000个随机合法表达式树，潜空间维度从2到14做了敏感性分析（图2）。
  - 对比了与GD和两种MINLP求解器（BARON、SCIP）的性能。
  - 未提及消融实验（如不同VAE架构、不同BO替代模型等）。
- **充分性与公平性**：
  - 优点：案例虽小但具体、可复现，展示了方法发现新算法（状态依赖步长）的能力，并与现有最优方法（GD的调优版本）和数学规划方法对比，结果客观。
  - 不足：
    - 评估实例数较少（每个案例仅4个优化问题），可能不足以推广到更复杂的算法搜索空间。
    - 未评估方法在不同维度、不同算子集下的表现。
    - 未与遗传编程、大语言模型等其他自动化发现方法进行直接性能对比（仅在引言中提及）。
    - 未进行统计显著性检验或多次重复实验。

## 6. 论文的主要结论与发现

- 提出的VAE+BO框架可以成功发现新的符号形式的迭代更新函数，且无需任何功能形式先验。
- **案例1**发现了一个状态依赖步长规则：\(x_{i+1} = x_i - 0.035(2.18 + x_i)\nabla F(x_i)\)，在5次迭代内比GD（需要12次）更快达到低梯度范数。该框架还能重新发现梯度下降，表明潜空间学习有意义。
- **案例2**发现了一个带有正则项的梯度下降变体：\(x_{i+1} = x_i - 0.11\nabla F(x_i) - 1.61\times10^{-3}x_i\)，在高条件数问题上表现优于GD。
- **计算效率**：本文方法在案例1中仅用121秒就找到了比MINLP方法在3600秒内更好的解，计算时间显著降低。
- 该框架是应用无关的，可扩展到其他迭代计算任务。

## 7. 优点

- **方法创新性**：将VAE与BO结合用于算法发现，将离散搜索转化为连续搜索，兼具可解释性（符号形式）和样本效率（BO适合昂贵评估）。
- **无需先验假设**：不预设更新函数的函数形式，自动从表达式树超结构中发现新算法。
- **计算高效**：相比数学规划方法（MINLP），所需计算时间大幅降低（从小时级降至分钟级）。
- **可复现性**：给出了关键参数设置，如树深度、算子集、VAE架构、BO迭代次数等。
- **应用灵活性**：框架可应用于任意迭代计算问题（方程组求解、PDE等），只需定义评估函数。

## 8. 不足与局限

- **实验覆盖有限**：
  - 仅测试了2个深度为2的表达式树场景，复杂函数或更高深度时的可扩展性未验证。
  - 评估问题数量少（每个案例仅4个实例），存在过拟合风险。
  - 未进行消融实验（如不同VAE结构、不同BO采集函数、不同常优化求解策略）。
- **方法局限性**：
  - VAE解码出的树不保证符合操作数规则（文中虽未发生，但作者承认这是常见问题）。
  - 常数优化（式9）需要全局求解，若问题维度高或约束复杂可能同样昂贵。
  - 对于收敛性差或发散算法，赋大常数惩罚可能导致搜索偏向保守区域。
- **偏差风险**：随机种子固定（1234），未报告多次运行的平均结果，可能掩盖随机性影响。
- **应用限制**：框架依赖完整的评估函数可计算，若性能指标本身无闭式表达（如实际求解时间），仍需进一步处理。
- **缺乏与最新方法的对比**：仅对比了数学规划方法，未与遗传编程、LLM-based方法或 learn-to-optimize 方法进行比较。

（完）
