---
title: Machine Can Automatically Discover Parametric Functions to Model HEP Data
title_zh: 机器可自动发现参数函数以建模高能物理数据
authors: "Ho Fung Tsoi, Dylan Rankin, Cecile Caillol, Miles Cranmer, Sridhara Dasu, Javier Duarte, Philip Harris, Elliot Lipeles"
date: 2026-07-22
pdf: "https://arxiv.org/pdf/2607.19750v1"
tags: ["query:sr"]
score: 10.0
evidence: 使用符号回归自动发现HEP数据分析函数
tldr: 高能物理数据分析中，拟合分箱数据通常依赖手动迭代：凭直觉选函数、拟合、检查、重复。本文用符号回归自动化该过程，无需先验知识。SymbolFit包结合符号回归与不确定性建模，在CMS和ATLAS Run 2双喷注谱上测试：560次独立运行生成超1000个函数，χ²/NDF≈1，其中111次重新发现已发表的dijet和UA2函数。结果表明机器可自动发现参数化函数，大幅提升效率。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.19750v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 707, \"height\": 380, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.19750v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 866, \"height\": 449, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.19750v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1188, \"height\": 482, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.19750v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1184, \"height\": 543, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.19750v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1408, \"height\": 602, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.19750v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1561, \"height\": 778, \"label\": \"Table\"}]"
motivation: 传统手动函数拟合效率低且依赖经验，需要自动化方法来减少人力开销。
method: 采用符号回归进行数据驱动搜索，结合不确定性建模，开发SymbolFit包。
result: 在真实双喷注谱上，560次运行生成1000+函数，111次自动复现人工选用的函数。
conclusion: 符号回归可替代手动迭代，为高能物理数据分析提供自动化函数发现工具。
---

## 摘要
在高能物理数据分析中，寻找一个合适的函数来建模分箱数据在很大程度上依赖于手动过程：通过直觉猜测一个函数形式，拟合，检查，然后重复直到成功。我们表明，这一迭代过程可以通过机器使用符号回归自动完成，它在函数空间中进行数据驱动的搜索，而不需要事先知道一个合适的函数应该是什么样子。我们提出了SymbolFit包，它将符号回归与不确定性建模相结合，针对高能物理分析用例，并在CMS和ATLAS Run 2双喷注能谱上进行了演示：在七个简单的拟合配置中，560个独立的种子运行生成了超过1000个拟合谱的函数，$χ^2/\text{NDF}\approx 1$，其中111个运行重新发现了已发表的双喷注搜索中使用的双喷注和UA2函数。

## Abstract
In HEP data analyses, finding an adequate function to model binned data has largely relied on a manual process: guess a functional form by intuition, fit, examine, then repeat until successful. We show that this iterative process can be automated by a machine using symbolic regression, which performs a data-driven search over function space without requiring prior knowledge of what an adequate function should look like. We present the SymbolFit package, which pairs symbolic regression with uncertainty modeling to target HEP analysis use cases, and demonstrate it on the CMS and ATLAS Run 2 dijet spectra: 560 independent seeded runs across seven simple fit configurations generated over 1000 functions fitting the spectra with $χ^2/\text{NDF}\approx 1$, and 111 of the runs rediscovered the very dijet and UA2 functions used in published dijet searches.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：在高能物理（HEP）数据分析中，对分箱数据（binned data）进行参数化建模时，寻找合适的函数形式长期依赖手动迭代过程：直觉猜测函数形式 → 拟合 → 检查残差 → 修改参数 → 重复，直到获得可接受的拟合质量。该过程耗时、依赖经验，且最终选出的函数往往只适用于特定分析，难以复用。
- **研究动机**：自动化上述手动迭代过程，消除对先验知识（即合适的函数形式）的依赖，并大幅提升建模效率与可重复性。
- **整体含义**：通过机器学习中的符号回归（Symbolic Regression, SR），机器可以数据驱动地自动发现与人工精心设计的函数（如dijet函数、UA2函数）性能相当甚至更优的参数形式，从而有可能取代传统的“试错法”，成为HEP分析中参数化建模的标准工具。

## 2. 论文提出的方法论：核心思想、关键技术细节

- **核心思想**：使用符号回归在函数空间中执行数据驱动的搜索，将函数形式本身视为一个“可训练参数”，在搜索过程中动态调整以最小化残差损失，无需预先指定具体函数形式。
- **关键技术细节**：
  - **SymbolFit包**：由PySR（符号回归搜索引擎）和LMFIT（非线性优化与不确定性估计）集成而成的API，自动化三个步骤：
    1. **函数搜索**：PySR基于多群体进化算法，在由基本数学运算符（如+、-、×、/、^、exp、ln、tanh等）构成的函数空间中进行搜索。
    2. **重新优化与不确定性估计**：PySR返回的精确函数没有不确定性度量，因此通过LMFIT对数值常数进行重新优化，并利用协方差矩阵计算参数不确定性。
    3. **评估**：对所有候选函数进行评分（如χ²/NDF、p值、RMSE、R²），绘制含参数不确定性变化和总不确定性覆盖的图，结果保存到输出文件。
  - **搜索配置**：定义了7种不同的搜索配置（表1），包括4种自由形式配置（运算符集合从宽到窄）和3种模板配置（固定函数外层结构，仅搜索子表达式）。例如：
    - `ops-broad`：运算符 {+,-,×,/,^,exp,ln,tanh}
    - `ops-minimal`：运算符 {-,×,/,^}
    - `tpl-exp-log`：模板 f = exp( h(ln x, ln(1-x)) )，h由{+,-,×}构成
  - 通用设置：迭代次数200（自由形式）/100（模板），最大树深40，损失函数为每bin的 (y - ŷ)²/σ²。

## 3. 实验设计：使用的数据集/场景、基准、对比方法

- **数据集**：两个公开数据集（来自HEPData）：
  - CMS Run 2 观测双喷注质量谱（√s=13 TeV），对应文献[1]的图5。
  - ATLAS Run 2 观测双喷注质量谱（√s=13 TeV），对应文献[2]的图3a。
  这两个谱在公开发表的搜索结果中分别使用dijet函数和UA2函数进行背景建模。
- **基准（目标）**：检验数据驱动搜索能否自动重新发现与人工设计的dijet函数（式1）和UA2函数（式1）代数等价的函数形式。dijet函数：f(x)=p0 (1-x)^p1 / x^(p2+p3 ln x)；UA2函数：f(x)=p0 e^{-p2 x - p3 x^2} / x^p1，其中x=m_jj/√s。
- **对比方法**：没有与其他自动符号回归方法或手工方法对比，而是将自动发现的结果与已知的人工函数进行等价性比较。本质上是一种“重现已知”的验证性实验。

## 4. 资源与算力

- **文中未明确说明使用的GPU型号、数量、训练时长等具体算力资源**。仅提到使用了PySR和LMFIT，但未提及硬件配置。从实验规模（560次独立运行，每次200或100次迭代，每次返回数十个函数）推测，整体计算量适中，可能仅需单个CPU或少量GPU即可完成，但论文未提供确切信息。

## 5. 实验数量与充分性

- **实验数量**：
  - 7种搜索配置，每种配置在CMS和ATLAS两个数据集上各运行40次（不同随机种子）→ 7×40×2 = 560次独立运行。
  - 每次运行返回数十个候选函数，总候选函数数量超过1000个（拟合χ²/NDF≈1）。
- **充分性评估**：
  - **正面**：实验覆盖了从高度自由到受模板约束的多种搜索空间，随机种子变化增加了探索的覆盖度；在两个不同实验（CMS与ATLAS）的谱上验证，具有一定泛化性；使用χ²/NDF作为主要拟合优度指标，合理且常用。
  - **不足**：
    - 仅针对两个特定谱（双喷注QCD背景），未在其他类型的HEP数据（如信号建模、尺度因子提取、不同物理过程）上测试，泛化性有限。
    - 未与手动拟合或传统自动拟合方法（如多项式/样条拟合、高斯过程等）进行定量比较（如速度、成功率、拟合质量）。
    - 未进行消融实验（如改变迭代次数、树深、损失函数等）以分析各超参数影响。
    - 评价指标仅使用χ²/NDF，未考虑函数复杂性（如AIC/BIC）或物理可解释性，可能遗漏过拟合或不可解释的函数。
    - 重新发现率在自由形式配置中较低（如ops-minimal在CMS上3/40，在ATLAS上2/40），样本量较小可能导致统计波动。

## 6. 论文的主要结论与发现

- **主要发现**：
  1. 在560次运行中，共有111次成功重新发现了dijet函数或UA2函数（图1），其中模板配置（如tpl-exp-log）的重新发现率最高（CMS上36/40，ATLAS上24/40）。
  2. 几乎所有运行都返回了多个非dijet/UA2的函数，且大量候选函数的χ²/NDF≈1（图2），说明数据驱动搜索能发现多种等价的参数形式，而人工设计的已知函数只是其中一小部分。
  3. 重新发现的dijet函数中，多数缺少p3 ln x项（p3=0），与直接拟合结果一致——CMS谱中该参数不显著，ATLAS谱中虽有改进但不足以增加复杂度。
- **核心结论**：SymbolFit结合符号回归与不确定性建模，能够自动、有效地发现拟合HEP数据的参数函数，其结果在拟合优度上可与传统手动设计函数媲美，甚至更丰富。这证明传统手工迭代的“猜-拟-改”过程可以被机器的数据驱动搜索所替代，适用于HEP中任何需要参数化建模的分析环节。

## 7. 优点

- **自动化程度高**：无需先验的函数形式知识，完全数据驱动。
- **不确定性建模嵌入**：与LMFIT结合，每个候选函数都获得参数不确定性和覆盖区间（图4），满足HEP统计分析的基本需求。
- **搜索空间灵活可控**：用户可自定义运算符集合和/或模板，从自由形式到限制形式，适应不同复杂度的需求。
- **结果丰富多样**：不局限于单一“最佳”函数，而是提供多种性能相近的可选形式，便于分析员选择。
- **可复现性强**：随机种子可控，结果易于复现；输出包含所有候选函数及其拟合细节。
- **实验设计合理**：采用独立种子运行+多种配置，覆盖不同假设空间，且通过“重新发现已知”验证方法的有效性。

## 8. 不足与局限

- **实验覆盖有限**：仅测试了双喷注质量谱，且谱本身点数有限（约30-50个bin），未在更复杂（如多峰、尖锐共振、平滑背景+信号）分布上验证。方法是否适用于高维数据或大数据集尚不明确。
- **未与其他方法系统对比**：没有与多项式回归、核密度估计、高斯过程、神经网络拟合等基线方法比较拟合质量、计算成本、泛化能力。
- **缺少复杂度惩罚**：仅用χ²/NDF评估可能导致过拟合复杂函数（如包含许多嵌套运算），未引入AIC/BIC或MDL等准则。论文中虽提到“抑制嵌套操作避免过拟合”，但未量化分析。
- **搜索效率与计算成本**：560次运行的具体计算耗时未给出，且符号回归在大规模数据或高维问题上的可扩展性未知。文中未提及硬件配置，难以评估实际应用成本。
- **重新发现定义较宽松**：代数等价可能容忍微小差异（如常数因子、变体形式），且对CMS谱重新发现的dijet函数多数缺少ln项，严格来说并不完全等价于发表的函数形式。
- **手动模板依赖风险**：模板配置（如tpl-exp-log）虽提高重新发现率，但本质上是将领域知识（即已知函数结构）注入搜索，削弱了“全自动”的意义。对于真正未知的分布，如何设计合适模板仍是开放问题。
- **拟议应用范围未验证**：论文声称可应用于尺度因子提取、背景建模、信号模型等，但实际仅演示了背景拟合，其他应用场景缺乏实证。

（完）
