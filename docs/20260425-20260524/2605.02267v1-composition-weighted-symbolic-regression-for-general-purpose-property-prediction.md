---
title: Composition-Weighted Symbolic Regression for General-Purpose Property Prediction
title_zh: 面向通用属性预测的组成加权符号回归
authors: "Yang Huang, Jingrun Chen"
date: 2026-05-04
pdf: "https://arxiv.org/pdf/2605.02267v1"
tags: ["query:sr"]
score: 9.0
evidence: 符号回归用于材料性质预测
tldr: 提出了一种成分加权符号回归框架，直接从化学成分预测材料属性，联合学习解析函数和元素权重，利用max/min算子强制约束，通过混合蒙特卡洛树搜索-遗传编程算法高效搜索。在MatBench任务上精度接近黑箱模型，输出显式表达式，应用于III-V半导体获得平滑趋势和化学有意义的元素权重，为材料发现提供可解释方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.02267v1/fig-001.webp\", \"caption\": \"TABLE I: Benchmark performance across three representative MatBench tasks[5, 27–29]. Values in\", \"page\": 3, \"index\": 1, \"width\": 508, \"height\": 410}, {\"url\": \"assets/figures/arxiv/2605.02267v1/fig-002.webp\", \"caption\": \"FIG. 1: Hybrid Monte Carlo tree search and genetic programming.\", \"page\": 2, \"index\": 2, \"width\": 533, \"height\": 263}, {\"url\": \"assets/figures/arxiv/2605.02267v1/fig-003.webp\", \"caption\": \"FIG. 4: Band gap comparison for selected III–V semiconductor alloys (adapted from Ref. [33]). Experimental data (open black circles) and interpolation fits (black solid line) [34–57]; MatBench training data [5] (open orange circles). Predictions: DARWIN [30, 31] (cyan dashed), MODNet [11, 12] (purple dashed), CrabNet [15, 16] (green dashed), the ReLU model [21] (brown dashed) and this work (red solid). Data sources are detailed in the Supporting Materials.\", \"page\": 5, \"index\": 3, \"width\": 1051, \"height\": 499}]"
motivation: 现有黑箱模型缺乏可解释性，需要能从化学成分直接预测材料属性的可解释框架。
method: 联合学习解析函数形式和任务依赖的元素权重，结合max/min算子约束，使用混合蒙特卡洛树搜索-遗传编程算法进行高效搜索。
result: 在MatBench基准测试中取得与最先进黑箱模型相当的精度，同时生成显式解析表达式。
conclusion: 该框架为材料发现和属性筛选提供了可扩展且可解释的途径。
---

## 摘要
我们提出了一种组成加权符号回归框架，用于直接从化学成分可解释地预测材料属性。该方法无需预定义描述符，即可联合学习解析函数形式和任务相关的元素权重。通过引入最大值/最小值运算符，它自然地强制执行非负带隙和有界分类概率等约束，从而统一了回归和分类任务。通过混合蒙特卡洛树搜索-遗传编程算法结合基于梯度的优化和并行计算，实现了高效搜索。在MatBench任务上的基准测试显示，与最先进的黑箱模型相比，该方法具有竞争性的准确度，同时产生显式解析表达式。应用于III-V族半导体合金时，该模型产生平滑的组分依赖趋势和具有化学上有意义的周期性行为的学习元素权重。该框架为材料发现和属性筛选提供了一条可扩展且可解释的途径。

## Abstract
We introduce a composition-weighted symbolic regression framework for interpretable prediction of materials properties directly from chemical composition. The method jointly learns analytical functional forms and task-dependent elemental weightings without predefined descriptors. By incorporating max/min operators, it naturally enforces constraints such as non-negative band gaps and bounded classification probabilities, unifying regression and classification tasks. Efficient search is achieved through a hybrid Monte Carlo tree search--genetic programming algorithm with gradient-based refinement and parallel computation. Benchmarks on MatBench tasks show competitive accuracy relative to state-of-the-art black-box models while yielding explicit analytical expressions. Applied to III--V semiconductor alloys, the model produces smooth composition-dependent trends and learned elemental weights with chemically meaningful periodic behavior. This framework provides a scalable and interpretable route for materials discovery and property screening.

---

## 论文详细总结（自动生成）

以下是对论文《Composition-Weighted Symbolic Regression for General-Purpose Property Prediction》的详细中文总结：

## 1. 论文的核心问题与整体含义（研究动机和背景）
- **核心问题**：现有材料属性预测模型主要分为结构基和组成基两类。结构基模型（如Equiformer、MODNet）依赖可靠的结构信息，计算成本高且在某些情况下不可得；组成基模型（如CrabNet、MODNet）可直接从化学式预测，但多为神经网络等黑箱模型，缺乏可解释性，难以揭示物理规律。
- **整体意义**：论文旨在开发一种**可解释、通用且无需预定义描述符**的材料属性预测方法，能从化学成分直接学习解析函数形式，在保持竞争性精度的同时输出显式表达式，并自然嵌入物理约束（如非负带隙、概率有界）。

## 2. 论文提出的方法论
- **核心思想**：将属性 \(P\) 表示为 \(P = F(\mathbf{x}; \theta)\)，其中 \(\mathbf{x}_k = \sum_i w_{k,i} c_i\) 是元素组成分数的加权和（学习元素权重），\(F\) 是通过符号回归发现的解析函数。联合优化元素权重 \(w\) 和函数参数 \(\theta\)。
- **关键技术细节**：
  - **运算符集**：标准连续运算符（exp, log, 乘、加） + **非光滑运算符 max/min**，用于强制物理约束（如带隙非负）和概率范围（[0,1]），统一回归与分类任务。
  - **搜索算法**：混合**蒙特卡洛树搜索（MCTS）与遗传编程（GP）**，在MCTS扩展节点后生成候选表达式，根节点维护全局表达式队列进行遗传操作（交叉、变异），双向传播信息（回传奖励、前向更新树节点统计）。
  - **参数优化**：使用**L-BFGS**梯度方法进行连续参数优化（元素权重和系数），采用**多起点策略**（两个随机初始化和两个正随机初始化）提高鲁棒性。
  - **并行化**：在GP阶段批量选择表达式并行优化；在MCTS阶段同时扩展多个节点进行rollout和参数优化。

## 3. 实验设计
- **数据集**：MatBench v0.1的三个任务：
  - `matbench_expt_gap`（带隙回归，MAE）
  - `matbench_expt_is_metal`（金属性分类，ROC-AUC）
  - `matbench_glass`（玻璃分类，ROC-AUC）
- **基准对比**：与MatBench leaderboard上的方法比较，包括：
  - 黑箱模型：Darwin（LLM, ~7B参数）、CrabNet（注意力网络, ~10⁶参数）、MODNet（深度网络, ~10⁶参数）、AMMExpress、RF-SCM（随机森林）、GPTChem（LLM, ~10⁹参数）
  - 解析基线：ReLU模型（\(P = \max(0, \sum w_i c_i)\)，~10²参数）
- **额外验证**：对III-V族半导体合金（如InAs-InSb, AlP-GaP等）的带隙进行预测，与实验数据、插值拟合及Darwin/CrabNet/MODNet/ReLU模型对比。

## 4. 资源与算力
- **未明确说明**：论文未提及使用的GPU型号、数量、训练时长或总计算资源。仅指出所提模型参数量约为10²（远小于神经网络的10⁶~10⁹），但未报告具体训练能耗或时间。

## 5. 实验数量与充分性
- **主实验**：三个标准基准任务，每个任务报告均值与标准差（来自多次运行或交叉验证？论文未详述）。实验覆盖回归与分类两种类型，展示了模型通用性。
- **补充实验**：III-V合金带隙预测（定性对比），提供了可视化趋势。
- **公平性**：与公开leaderboard结果直接比较，使用同一数据集，对比方法均为已发表的最佳结果。但**缺少消融实验**（如移除max/min运算符、不同搜索策略、不同初始化等），也未分析超参数敏感性。
- **局限性体现**：论文主动指出低数据量（312样本的钢数据集）可能导致过拟合，说明实验设计有一定自省。
- 整体而言，实验在标准基准上充分且客观，但消融研究不足，可进一步验证各组件的贡献。

## 6. 论文的主要结论与发现
- **性能**：所提方法在MatBench任务上**精度接近**最先进黑箱模型（带隙MAE 0.471 vs 最佳0.287；金属分类ROC-AUC 0.873 vs 最佳0.960；玻璃分类0.816 vs 最佳0.960），但参数数量少2-4个数量级。
- **表达式可解释性**：发现的解析表达式（式(3)-(5)）包含嵌套指数和max/min，自然实现**金属-绝缘体区分**和概率门控，并学习到**任务依赖的元素权重**（如卤素和过渡金属在元素周期表上呈现极性相反的权重模式）。
- **平滑外推**：在III-V合金预测中，符号模型产生连续的带隙-组成曲线，而对比的神经模型出现跳跃或不连续，表明解析形式更适合数据稀疏区域的插值/外推。
- **物理意义**：学习到的元素权重呈现周期性趋势，反映特定属性的化学相似性，为材料设计提供可解释指导。

## 7. 优点
- **完全可解释性**：输出显式解析表达式，能揭示输入与输出之间的数学关系，优于黑箱模型。
- **自然嵌入物理约束**：通过max/min和指数函数自动满足非负性、概率有界等条件，无需额外输出层或后处理。
- **统一回归与分类**：同一框架处理连续值和概率预测，无需任务特定调整。
- **参数高效**：仅需少量元素权重和符号系数，训练成本低，适合小数据场景（训练数据少时不易过拟合？但论文指出低数据仍有风险）。
- **平滑外推能力**：解析形式保证了组成空间内的连续梯度，避免神经网络的不稳定外推。

## 8. 不足与局限
- **可解释性不保证简单**：复杂表达式仍需后续分析，可能难以直接理解物理机制。
- **优化不保证全局最优**：MCTS+局部梯度优化无法保证找到全局最优表达式，且随搜索深度和元素种类增加计算成本上升。
- **低数据过拟合风险**：在样本极少（如312个）时符号回归过于灵活，易拟合噪声而非物理趋势。论文未提供该场景的缓解策略。
- **适用性有限**：对强多尺度或尖锐不连续现象（如金属-玻璃相界）可能失败，未测试此类任务。
- **实验覆盖不足**：未与其他符号回归方法（如Eureqa、PySR）对比；未进行消融实验；未分析运算符集选择、树深度等超参数敏感性。
- **数据偏差**：所基于的MatBench数据集本身与某些实验值存在系统偏差（如AlP-GaP带隙），模型复现了这些偏差，可能误导预测。

（完）
