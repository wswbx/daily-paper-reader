---
title: Why Architecture Choice Matters in Symbolic Regression
title_zh: 为什么架构选择在符号回归中很重要
authors: Chakshu Gupta
date: 2026-04-25
pdf: "https://arxiv.org/pdf/2604.23256v1"
tags: ["query:sr"]
score: 9.0
evidence: 符号回归架构的比较
tldr: 本文研究固定树结构的符号回归中架构选择的影响。通过比较三种不同变量输入方式的树结构，在12700次训练中发现，表达力强的结构反而在简单目标上失败，而受限结构成功。切换操作符或改变梯度轮廓可改变恢复结果，平衡树从未被恢复。表明优化景观而非仅表达力决定梯度符号回归的恢复能力。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.23256v1/fig-001.webp\", \"caption\": \"Figure 1: Recovery by architecture, shape, and operator (— = inferred by symmetry).\", \"page\": 3, \"index\": 1, \"width\": 834, \"height\": 415}]"
motivation: 探究固定树结构的选择是否以及如何影响梯度符号回归对目标公式的恢复效果。
method: 比较三种操作符树（共享相同操作符但变量输入方式不同，其中一种严格更具表达力），进行大量训练实验。
result: "不同树结构在不同目标上恢复成功率差异显著（100% vs 0%），且表达力最强的结构在受限结构可靠解决的目标上失败；切换操作符或反转其梯度轮廓改变结果。"
conclusion: 梯度符号回归的成功不仅取决于搜索空间的表达力，更关键的是优化景观的塑造，这一发现挑战了单纯追求表达力的设计思路。
---

## 摘要
符号回归从数据中发现数学公式。一些方法固定一个算子树，分配可学习的权重，并通过梯度下降进行训练。树的结构决定了每个位置上出现的算子和变量，该结构被选择一次并应用于每个目标。本文测试了这种选择是否会影响实际恢复的目标。比较了三种结构，它们共享相同的算子和目标语言，但变量进入树的方式不同；其中一种结构严格更具表达力。在超过12,700次训练运行中，一种结构以100%恢复目标，而另一种得分为0%，且在不同目标上排名反转。表达力保证了搜索空间中存在解，但并不能保证梯度下降能找到它：最具表达力的结构在受限替代方案可靠解决的目标上失败。改变算子会改变成功的目标；反转其梯度轮廓完全破坏恢复。平衡（非链式）树形状从未被恢复。这些发现表明，优化景观（而不仅仅是表达力）决定了基于梯度的符号回归能恢复什么。

## Abstract
Symbolic regression discovers mathematical formulas from data. Some methods fix a tree of operators, assign learnable weights, and train by gradient descent. The tree's structure, which determines what operators and variables appear at each position, is chosen once and applied to every target. This paper tests whether that choice affects which targets are actually recovered. Three structures are compared, all sharing the same operator and target language but differing in how variables enter the tree; one is strictly more expressive. Across over 12,700 training runs, one structure recovers a target at 100% while another scores 0%, and the ranking reverses on a different target. Expressiveness guarantees that a solution exists in the search space, but not that gradient descent finds it: the most expressive structure fails on targets that a restricted alternative solves reliably. Switching the operator changes which targets succeed; reversing its gradient profile collapses recovery entirely. Balanced (non-chain) tree shapes are never recovered. These findings show that the optimization landscape, not expressiveness alone, determines what gradient-based symbolic regression recovers.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：在基于梯度的符号回归（SR）方法中，固定算子树的结构（架构）一旦选定就被应用于所有目标，这种单一架构选择是否会影响不同目标公式的恢复成功率？现有研究（如SRBench）通常同时改变架构、语法和搜索策略，无法隔离架构本身的影响。
- **整体意义**：解的存在性（表达力）并不等于梯度下降能找到解；优化景观的几何性质（而非仅表达力）才是决定恢复能力的关键因素。论文通过严格控制的对比实验，揭示了架构、算子、树形及变量路由之间的交互作用，挑战了单纯追求更大表达力的设计思路。

### 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程

- **核心思想**：使用一个单一二元算子EML（exp(a) - ln b），该算子是一个Sheffer算子，可用常数1和二叉树表示所有初等函数。通过固定算子而改变树结构的变量输入方式，排除语法差异，单独研究架构影响。
- **关键技术细节**：
  - 三种测试架构（深度3的完美二叉树）：
    - **Eq.6**：每个内部节点用softmax从{1, x, f_child}中选择，x可出现在任何层级，变量y仅出现在叶子节点（通过叶子处4路softmax）；严格更富表达力。
    - **V16**：每个内部节点用sigmoid从{1, f_child}中选择，x和y仅通过叶子处3路softmax进入，对称设计。
    - **Hybrid**：内部节点使用V16的sigmoid，仅根节点使用4路softmax同时支持{x, y, 1, f_child}。
  - 额外引入两个算子SML（sinh(a) - arctan(b)，左输入放大、右输入有界）和RML（arctan(a) - sinh(b)，左输入有界、右输入放大）以验证梯度方向的影响。
- **算法流程**：
  - 每个节点输入为可选项的加权组合，权重通过可学习参数控制。
  - 训练使用Adam（lr=0.01），在[-3,3]^2上的21×21网格（441点，过滤溢出后≥378点）进行。
  - 分两阶段：搜索阶段（6000次迭代，温度τ=2.5保持软选择）→ 硬化阶段（2000次迭代，τ从2.5退火至0.01，加入熵惩罚和二值化惩罚，最后硬化为独热选择）。
  - 恢复判定：硬化后树与目标公式精确匹配（在500随机点上RMSE < 1e-6）。

### 3. 实验设计：使用了哪些数据集 / 场景，它的 benchmark 是什么，对比了哪些方法

- **数据集/场景**：由EML算子生成的人造目标公式（深度3的二叉树），共有5种不同形状（4种链式：LR、RL、RR、LL；1种平衡式；LL因双指数溢出被排除），每种形状分配不同的叶子变量(x,y)组合（如yx、xy等）。没有使用标准SR Benchmark（如SRBench或Feynman方程），因为EML树深度足够表达这些目标需指数级深度。
- **基准对比**：三种架构（Eq.6、V16、Hybrid）相互对比，同时在不同算子（EML、SML、RML）下重复实验。此外还测试了不同初始化策略（V16初始化为randn×0.1无偏置）。
- **对比方法**：无外部基线，因为控制变量法仅比较架构。

### 4. 资源与算力：如果文中有提到，请总结使用了多少算力（GPU 型号、数量、训练时长等）。若未明确说明，也请指出这一点。

- **未明确说明**：论文未提及使用的GPU型号、数量或总训练时长。仅提到总共进行了超过12,700次训练运行（每个trial独立），且每trial包含8000次迭代（6000搜索+2000硬化）。具体算力消耗未给出。

### 5. 实验数量与充分性：大概做了多少组实验（如不同数据集、消融实验等），这些实验是否充分、是否客观、公平。

- **实验数量**：
  - 主实验：3种架构 × 3种算子 × 多种目标形状/变量分配，每组合进行64或256次独立trial（V16/Hybrid: 256次；Eq.6: 64次），总计>12,700次训练。
  - 额外实验：初始化影响（V16改用Eq.6的初始化，0/64恢复）；学习率/迭代/温度调优（6种配置×192 trials）；超参数敏感性（仅针对平衡树）；梯度测量（10种子）。
  - 平衡树测试：3,776次trial全面失败。
- **充分性**：
  - **充分**：通过大量重复和不同算子验证了架构-目标交互的统计显著性和稳健性；控制了表达力变量；进行了初始化、超参数敏感性等附属实验。
  - **客观公平**：公平比较了三种架构（共享相同目标语言），且报告了95% Clopper-Pearson置信区间（±5pp）；但仅限于深度3，深度4仅对V16做了有限测试（RL从99.6%跌至0.8%），完整深度4矩阵缺乏。平衡树只有失败结果，缺乏梯度测量分析。因此实验覆盖深度不足，但针对问题的核心结论有说服力。

### 6. 论文的主要结论与发现

- **架构-目标交互**：恢复成功与否取决于架构与目标的具体组合，而非单独因素。例如Eq.6在LR(yx)上100%成功，在RL(xy)上0%成功；V16则相反（RL 99.6%，LR 17%）。表达力更强的Eq.6反而在V16可以解决的目标上彻底失败。
- **算子交换逆转偏好**：从EML换为SML后，Eq.6的最佳形状从LR变为RL和RR，恢复率从100%变为0%（Fisher test p<10^-15）。梯度方向差异（右端衰减方式不同）正确解释了该逆转。
- **反转梯度方向导致崩溃**：使用RML（右输入放大）后，所有架构在链式目标上的恢复率降至接近0%（Eq.6 0%，V16最高2%）。
- **平衡树永远无法恢复**：所有架构、算子、变体上平衡树恢复率均为0%（0/3,776 trial，95%上限0.10%）。作者推测平衡分支使梯度信号均分，无法沿单路径集中。
- **梯度测量揭示机制**：对于Eq.6，叶节点梯度比‖∇x‖/‖∇y‖<1时（y得到更强梯度）恢复100%，>1时恢复0%；梯度轨迹显示成功案例中有一段短暂反转使y占优。
- **核心结论**：优化景观（而非表达力）决定梯度符号回归的恢复能力。单一固定架构方法可能导致0%恢复率，而使用不同的架构可能100%恢复。

### 7. 优点：方法或实验设计上有哪些亮点

- **严格控制对比变量**：通过使用共享同一算子（EML）和目标语言的架构，隔离了架构差异的影响，避免了以往研究混淆语法、搜索、架构的问题。
- **引入梯度不对称算子**：设计SML和RML验证梯度方向的作用，增强了结论的普遍性。
- **大量重复试验**：每个配置至少64次独立trial（部分256次），提供统计可信度；报告置信区间。
- **梯度测量的因果线索**：测量叶节点梯度比并展示训练轨迹，直接关联恢复成功与否。
- **超参数敏感性检验**：针对平衡树测试了6种训练配置，确认失败不是参数选择所致。
- **初始化控制**：排除了初始化差异导致不同架构结果不同的可能性。

### 8. 不足与局限：包括实验覆盖、偏差风险、应用限制等

- **深度局限**：完整的三架构比较仅限深度3；深度4仅测试了V16，发现其一般性崩溃，但缺乏其他架构数据，无法形成完整结论。
- **算子局限性**：所有测试算子均为减法形式 f(a)-g(b)，未覆盖乘法、复合算子等常见操作。结论是否能推广到更丰富的算子集未知。
- **目标集局限**：目标完全局限于EML语法内的简单链式/平衡树，未使用标准SR Benchmark（如Feynman方程、SRBench），无法直接对接实际应用场景。
- **SML输出范围差异**：SML在[-3,3]^2上的输出范围比EML窄，可能部分解释V16在SML下的更高恢复率，但作者指出形状特异性模式无法用整体条件改善解释。
- **梯度测量仅为相关性**：论文承认梯度比与恢复率的关联是相关性，而非因果性；缺乏损失景观的机制性分析（如Hessian、鞍点等）。
- **平衡树失败机制未验证**：虽然假设梯度均分导致失败，但未实际测量平衡树的梯度分布，缺乏直接证据。
- **变量路由与梯度方向交互的复杂性**：RML下预期反转却整体崩溃，表明交互比简单模型预测的更复杂，理论理解尚不完整。

（完）
